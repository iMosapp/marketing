# i'M On Social — Relationship OS (PRD)

## Original Problem Statement
Phase 2 "Relationship OS" UX enhancements: robust backend security, intent-based detection engine, live inventory tracking, ADF/XML lead intake. Native iOS/Android UX (Expo), Twilio voice/SMS routing, long-term campaign tracking, AI personalization, strict RBAC, and "no brain power" UI layouts for sales reps.

## Environments
- Preview: REACT_APP_BACKEND_URL in /app/frontend/.env (backend under test)
- Production: https://app.imonsocial.com — user pushes frontend via `eas update --branch production --message "msg"` (give ONLY the one-liner). Backend goes live when user hits Deploy.
- IMPORTANT: EXPO_PUBLIC_BACKEND_URL in frontend/.env must point to https://app.imonsocial.com before any `eas update` (it gets baked into the OTA bundle). Temporarily point it at the preview URL only for screenshot testing, then revert + restart frontend.

## Implemented (key items)
- AI auto-replies (per-chat + global AI ON/OFF master switch), Twilio routing, voice memos → AI profile extraction (newest memo wins)
- Live inventory webhooks, keyword auto-tagging, call playback, ADF/XML lead intake
- Contact profile refactored into /app/frontend/components/contact/*
- Touchpoints swipe actions (right=Done, left=Snooze) + Undo snackbar (backend undo in tasks.py)
- Send Photo to many (quick-send/photo.tsx), industry-neutral naming
- QR everywhere: login screen "My Card QR", Home header 1-tap QR, Card tile long-press
- Wallet pass pipeline (backend/routers/wallet_pass.py): Apple .pkpass signing + Google save-link — returns 503 until certs are in .env (APPLE_TEAM_ID, APPLE_PASS_TYPE_ID, APPLE_PASS_P12_B64, APPLE_PASS_P12_PASSWORD, APPLE_WWDR_PEM_B64)
- **QR Scan Counter (June 2026, this session)**: `card_scans` collection — unique visitor/day dedupe (sha256 of IP+UA), owner self-views excluded via Bearer token check. `GET /api/card/scan-stats/{user_id}` → {week, total}. Displayed as "X scans this week · Y all-time" pill in UniversalShareModal QR view (home header QR / Card QR). Tested: dedupe, self-view exclusion, UI render all verified on preview.
- **Contact Picker 50-Cap Fix (June 2026)**: All contact pickers previously loaded only the first 50 contacts and filtered locally — contacts beyond 50 were unfindable (user hit this on Voice Note picker). New shared hook `/app/frontend/hooks/useContactSearch.ts` does debounced server-side search (mirrors dialer's proven pattern; handles numeric queries + multi-word names). Wired into 9 pickers: home.tsx (ContactActionModal call picker, action picker/Voice Note, Send-a-Card picker), quick-send/photo.tsx, touchpoints/add-task.tsx, tasks/new.tsx, inbox.tsx new-conversation, contact/new.tsx referral, contact/[id].tsx referral. Verified: contact #62 "jimmy" found via Voice Note picker; clearing search restores default list.
- **"What to Send" Draft Sheet (June 2026)**: Home "Do This Next" / My-3 taps previously dumped users on the contact page with no guidance. Now they open a bottom sheet showing full "Why now" reason + an AI-drafted ready-to-send text (GET /api/home/draft/{user_id}/{contact_id}?reason=<reason_key> in home_intelligence.py, gpt-5.2 via Emergent LLM key, per-reason template fallbacks). Buttons: "Use This Message" (opens thread with composer prefilled, marks My-3 complete), "Different Message" (regenerate), "View Contact". Done My-3 cards still open contact page. Verified e2e on preview: sheet, AI draft (~2s), thread prefill all working.
- **Inbox tab reorder (June 2026)**: Waiting chip moved next to Hot (Hot → Waiting → All → AI → Active → Closed) per user request. Verified on preview.
- **Em-dash ban (June 2026)**: User NEVER wants em/en dashes in AI output (dead AI giveaway). Hard filter `no_em_dash()` in /app/backend/utils/text_sanitize.py (em/en dash → comma; digit ranges → hyphen) applied to EVERY AI text path: ai_reply.py (auto-reply + 2 follow-up writers), ai_campaigns.py (generate/reply/preview), messages.py (suggested reply), chat_widget.py, jessie_service.py, ai_outreach_service.py (suggestions + fallbacks), home_intelligence.py draft (+ prompt rule + fallbacks cleaned), va_profiles.py preview, home.tsx frontend fallback. RULE FOR FUTURE AGENTS: any new AI text generation MUST wrap output in no_em_dash(). Verified: unit tests + 3 live draft generations all clean.
- **Draft Everywhere / Write It on Touchpoints (June 2026)**: Draft sheet extracted to shared /app/frontend/components/DraftMessageSheet.tsx (home.tsx now uses it too). Touchpoints task cards gained a gold "Write It" button (Call | Write It | Text) that opens the sheet with reason 'touchpoint' (or birthday/anniversary by task type) + task title/description passed as `context` to the draft endpoint (new optional param). Backend: DRAFT_BRIEFS/DRAFT_FALLBACKS gained 'touchpoint' key. Verified e2e on preview: overdue task "Bud" → context-aware draft referencing his 2024 Tahoe; home sheet regression OK.
- **Auto-Complete on Send (June 2026)**: Draft sheet passes task_id → thread ?taskId=; thread/[id].tsx completes the task (PATCH /tasks action=complete, once via ref) after a successful send (both personal-SMS and regular flows). ALSO FIXED pre-existing bug: opening /thread/{contact_id} set actualConversationId to the contact id because GET /messages/thread/{contact_id} returns [] (200) — threads showed "No messages yet" despite history and sends failed "Conversation not found". Fix: if no messages AND conversation info 404s, fall through to find-or-create conversation. Verified e2e: Bud's history loaded, message sent, task auto-completed in DB.
- **Banned Words (June 2026)**: New persona.banned_words field (UserPersona model + persona wizard step 3 UI box, red label). Enforced two ways: injected into AI clone prompt + hard-stripped via strip_banned()/clean_ai_text(user_id) in utils/text_sanitize.py, applied at ai_reply (3), ai_campaigns (3), messages suggest, jessie, outreach, home draft. Verified: filter strips phrases (3/3 clean live drafts), settings box renders + loads saved value.
- **My VA rename + go-to phrases optional (June 2026)**: "Virtual Assistant" renamed to "My VA" everywhere (VA screen header, card name, Hub menu, my-profile button, persona wizard link). custom_phrases REMOVED from VA completeness scoring (now X of 11 — user's real missing item was go-to phrases, NOT payment link; payment link never counted). Root cause of user deleting phrases: AI overused them — clone prompt now says use sparingly, never force. persona.tsx labels go-to phrases "(optional)". Verified on preview.
- **Global iOS Dynamic Type cap (June 2026)**: User's iPhone large system font blew up layouts everywhere ("Next" wrapping to "Nex/t", phone numbers wrapping). Text.defaultProps is DEAD in RN 0.81/React 19. Fix: custom Babel plugin /app/frontend/babel-plugin-max-font.js registered in new /app/frontend/babel.config.js (babel-preset-expo + plugin) — injects maxFontSizeMultiplier={1} (user chose full 1.0 lock: app ignores phone text-size entirely) into EVERY <Text>/<TextInput>/<Animated.Text> at compile time; explicit props and spreads still win. All older per-element maxFontSizeMultiplier props (leads, home, dialer, report-bug, bug-reports, date-recipients, inventory, NotificationBell) removed via sed so nothing overrides the global 1.0. Verified via babel transform test + web smoke + home renders post-edit. NOTE: babel config changes require Metro cache clear (rm -rf node_modules/.cache) + frontend restart. Real effect only visible on iOS device after eas update.

## Pending / Backlog
- **4x6 Leave-Behind Card PDF (June 2026) - DONE**: Per-rep print-ready card generated server-side with PURE PILLOW (no browser; Playwright/Chromium is NOT available in production so the earlier HTML template idea was dropped and /app/marketing/print/card-template.html + road assets deleted). `services/print_card.py` renders front+back at 1875x1275 (4x6 landscape + 1/8in bleed, 300dpi) supersampled 2x -> 600dpi JPEG pages in a 6.25x4.25in PDF via fpdf2. FRONT = reference design (app icon on white rounded iOS-style tile w/ shadow, no divider line - user request; I'M ON SOCIAL / THE RELATIONSHIP OS, 4 props w/ FontAwesome glyphs, "Your CRM remembers the sale. / We remember the person." Caveat hook, tags row, "See -> I'M ON SOCIAL IN ACTION", conic-rainbow QR frame w/ the rep's tracked QR (imonsocial.com/go/<slug>), "IMONSOCIAL.COM" + "Text <First>: (435) 220-3414" line using link.sms_number else user's twilio_number/mvpline_number; generic 'card' slug shows "Text us:" only if admin set a number, else no line). BACK = WHITE stock (user: cheaper printing, "same message just reverse coloring": ink-black text, blue/green/orange/red words kept, yellow deepened to (232,176,0) for contrast, faint blue glow) with headline/bar/body/script/brand-left + two fanned iPhone frames (Inbox behind, People to Talk To Today in front); screenshots in /app/backend/print_assets/*.jpg, fonts bundled in print_assets/fonts (Montserrat 800/900, Inter 500/600/700, Caveat 700, fa-solid-900). PDF LAYOUTS (June 2026, user: "way too big when I print"): `?layout=exact` (DEFAULT, true 6x4in, bleed cropped - for 4x6 photo paper / Actual Size printing), `?layout=bleed` (6.25x4.25 for print shops), `?layout=letter` (card centered on 8.5x11 with cut marks + "Print at Actual Size" note, back page aligned for duplex). "Fit to page" on a home printer is what blew it up. Endpoints (public like qr.png, 404 on unknown slug, asyncio.Semaphore(1) + disk cache in tempdir keyed by code/assets mtime + params): `GET /api/go-links/{slug}/print-card.pdf` (attachment) and `GET /api/go-links/{slug}/print-card.png?side=front|back&width=` (preview). _serialize adds print_pdf_path/print_png_path. UI: my-print-qr.tsx new "Your 4x6 leave-behind card" section (Front/Back toggle preview + gold "Download Print Card (PDF)"); admin/print-qr.tsx adds outlined "4x6 Leave-Behind Card (PDF)" per link (rep name suffix for rep links). Verified: PDF 2 pages 6.25x4.25in w/ 3750x2550 DCT images, QR decodes (pyzbar) from the rasterized PDF at 300 and 100 dpi, 404/400 guards, both screens render on preview. SHIP: backend Deploy + `eas update`.
- **QR Landing Page + Rep Print Cards (June 2026) - DONE**: (1) marketing/build/card/index.html = mobile-first dark/gold landing (imonsocial.com/card, ?rep=<slug> personalizes): 46s ad autoplays MUTED w/ gold "Tap for sound" pill (restarts w/ sound if >2s in), mute toggle, progress bar, poster+play fallback when autoplay blocked, on `ended` -> cover "Watch again" + Book-a-Demo bottom sheet slides up (name/mobile/email/dealership + SMS consent, POST https://app.imonsocial.com/api/demo-requests w/ lead_source qr_card_<slug>_<origin>, ref=<rep ref_code> so referred_by = rep + rep gets the lead notif, utm qr/print/<slug>; lead-retry.js included), sticky bottom bar Text <Rep> (sms: link w/ prefilled body, iOS uses & separator) + Book a Demo. Config from public GET /api/public/go-card/{slug} (added to PUBLIC_CORS_PREFIXES) -> {rep{first_name,name,title,photo_url,store_name}, sms_number, sms_body, ref_code, demo_source}; unknown slug -> generic 'card'. `?api=` query override exists for preview testing. Avatar falls back to initial when photo 404s (preview photos live on preview host, prod URLs 404 there = expected). (2) go_links now has kind ('campaign'|'rep'), user_id, sms_number. GET /api/go-links/mine (auth) lazily creates the rep link: slug = first name (collision -> forest2), destination SITE_URL/card?rep=<slug>, sms_number = user's twilio_number/mvpline_number, ensures user.ref_code. Admin POST accepts sms_number (validated >=10 digits). Generic 'card' link sms_number is EMPTY until admin sets it (user wants a number other than Forest's; set via Print QR Codes > card > edit "Text-me number"; Text button hidden until set). (3) demo_requests.classify_channel: utm_source qr / utm_medium print / source qr_card* -> channel 'print_qr' (+ label in admin/lead-tracking.tsx). (4) app/my-print-qr.tsx (My Brand > "My Print QR": QR, short link copy, PNG/SVG download, share, scans, Preview your page) + admin/print-qr.tsx shows REP badge, rep name, text-me line, sms field in edit modal. Verified on preview: config generic+rep, redirect w/ utm, mine 401/200, admin sms validation, landing page autoplay->sound->end->sheet->submit (demo doc referred_by Forest, channel now print_qr), both app screens render. Test data cleaned. SHIP: backend Deploy + marketing redeploy (card/ page) + eas update.
- **Print QR links (June 2026) - DONE**: User needed a real QR for a 4x6 leave-behind card; chose a tracked redirect on the marketing domain. QR encodes https://www.imonsocial.com/go/card. Chain: marketing /go/:slug -> https://app.imonsocial.com/api/go/:slug (vercel.json in marketing/ + build/, netlify.toml, build/404.html JS fallback, AND a real static build/go/card/index.html meta-refresh page so it works on any host) -> backend routers/go_links.py logs db.go_scans {slug, platform, ip_hash, ua, referer, scanned_at} and 302s to db.go_links.destination (+utm_source=qr&utm_medium=print&utm_campaign=slug). Unknown slugs still land on the homepage (never dead-end print). Admin (super_admin/admin only): GET/POST /api/go-links (upsert slug/label/destination; slug regex ^[a-z0-9][a-z0-9-]{1,39}$), GET /api/go-links/{slug}/scans (per-day + platform), public GET /api/go-links/{slug}/qr.svg (SvgPathFillImage, white bg) and /qr.png?size<=4000 (ECC H, 4-module quiet zone, 300dpi). Screen app/admin/print-qr.tsx (Hub > Account Management > "Print QR Codes", super_admin) shows QR, short URL copy, All time/This week/Today scans, PNG 3000px + SVG download, edit destination, add new slug. Print files also saved to /app/marketing/print/imos-qr-card-3000px.png + .svg. Verified: redirect + utm, QR decodes (pyzbar) at 3000px and 240px, 401 unauth, admin list/create via UI on preview (test link + scans cleaned). NOTE: qr paths returned WITHOUT /api prefix because api.defaults.baseURL already ends in /api. SHIP: backend Deploy (redirect must exist before cards are scanned) + redeploy marketing (go/ page + vercel redirect) + eas update (admin screen).
- **Social Ad video on marketing site (June 2026) - DONE**: User's 66 MB 9:16 "Social Ad Sept 8" (46s, VO, closing line "Your CRM remembers the sale. I remember the person.") compressed to marketing/build/video/social-ad.mp4 (6.4 MB, 720x1280 H.264 faststart) + social-ad.webm (7.5 MB VP9 fallback) + social-ad-poster.jpg. Shared player: video/ad-player.css + ad-player.js (phone frame, gold play button w/ pulse, tagline + "Watch · 46 sec · sound on", click = unmuted play + native controls, pause returns cover, ended shows replay icon, pauses other players, gtag video_play if present). Placement: homepage hero converted to 2-col grid (copy left / phone right, stacks <=900px); /dealers/ (cs-hero) and /automotive/ (hero) get the phone centered under hero copy. Verified via local static server: layouts desktop/tablet/mobile + play/pause/ended/replay flow (headless Chrome lacks H.264 so it used the webm source; real browsers use mp4). SHIP: redeploy marketing site (Vercel, marketing/build). Snippet to reuse elsewhere: copy the `.ad-phone` block + the two head tags.
- **Face ID Prompt after login (June 2026) - DONE**: login.tsx now shows a gold "Turn on Face ID?" card (biometric-offer-card / -enable / -skip testids) right after a successful PASSWORD login on native when biometrics are available but not enabled. Shown INSTEAD of navigating (never during) to avoid the old EXC_BAD_ACCESS crash; navigation happens 150ms after the modal closes. Enable = enableBiometricLogin({email, token}) (same token mechanism as Settings > Security). "Not Now" (or a cancelled Face ID confirm) snoozes 7 days via SecureStore key imos_biometric_offer_snoozed_until (utils/biometrics.ts shouldOfferBiometricSetup/snoozeBiometricOffer). Skipped on web and for needs_password_change users. Verified visually on preview via temporary web hook (removed). Frontend-only -> `eas update`.
- P0 Wallet Activation: user gathering Apple certs — step-by-step instructions provided (Pass Type ID + Team ID + .p12 + WWDR G4 PEM). When received: base64 into backend .env, test /api/wallet/{id}/download-token flow.
- P1 Google Play launch: code prep DONE June 2026 (see /app/memory/ANDROID_PLAY_STORE.md: blockedPermissions for READ_MEDIA_*/WRITE_CONTACTS, write-only photo saving on Android, notification icon/color, app.config.js googleServicesFile hook). Remaining = user actions: Firebase google-services.json + FCM V1 key in EAS, Play Console listing/data safety/reviewer login, `eas build -p android --profile production` + submit, then set PLAY_STORE_URL on backend.
- P1 HomeNet/vAuto live inventory feed (awaiting dealer API credentials).
- P2 Rotate production secrets (Twilio, Stripe, Resend, Mongo, VAPID, OpenAI) before contractor handoff.
- P2 Zapier integration for generic lead payloads.
- P3 WhatsApp, Stripe partner invoices, enterprise DMS (CDK, Reynolds, Dealertrack).

- **Bulk CRM Push (June 2026)**: Contacts tab select mode gained a purple cloud-upload header button → "Push N Contacts to CRM" sheet (intake email + remember toggle). Backend POST /api/crm-push/{user_id}/bulk (max 50, one ADF email per contact via Resend, tags + activity per contact, returns {sent, failed}); GET /api/crm-push/{user_id}/settings returns saved crm_intake_email. Bulk route registered BEFORE /{user_id}/{contact_id} to avoid shadowing. Verified: validations (400s) + full UI flow on preview; live email delivery same caveat as single push.

## Notes for agents
- **TIMEZONE RULE (June 2026, user: "push at 8am but task says 9am, time discrepancies through the app")**: ROOT CAUSE - two resolvers: call_followup used `resolve_user_tz` which preferred `user_schedules.timezone` (Forest's production schedule tz was a Central zone) while task reminder pushes/most of the app used `users.timezone` (device, Denver) -> "Try again tomorrow 9:00 AM" was 9:00 Central = 8:00 on his phone. FIX: `routers/user_schedule.py::resolve_user_tz` is THE ONLY resolver: `users.timezone` (phone, refreshed on password login, Face ID `/auth/refresh` {timezone}, and every app open via `/push/subscribe-native` {timezone}) -> schedule tz -> store tz -> America/Denver; 'UTC' = unknown. `set_device_timezone()` validates IANA names. `GET /schedule/me` returns the effective tz (`timezone_source: device`) and the Schedule screen labels it "taken from your phone". Also unified stray defaults: scheduler task reminders use the resolver, ai_outreach_service (was New_York), calendar.py Google events (was New_York) use resolver, lead_intake store default Chicago -> Denver. RULE FOR ANY NEW CODE: never read users.timezone / schedule tz directly for rep-local math; call `await resolve_user_tz(user_id)`. Store-hours logic stays on `stores.timezone`.
- **False "2x buyer" (June 2026, user: Mike Wolthuis is not a repeat buyer)**: ROOT CAUSE - three Sold paths each inferred "date_sold already exists => repeat" and `$inc sold_count`: sold-quick calls `date-sold` FIRST, then the congrats card with `tags:['Sold']` (congrats_cards.py) and the fallback `tags` PATCH both saw date_sold and incremented (+ pushed a bogus "Previous purchase" purchase_history entry). FIX: new `services/sales.py::record_sale(db, contact_id, sale_date, only_if_unsold)` is the ONLY writer of date_sold/sold_count/purchase_history (first | same-transaction [same day or recorded <7d ago, e.g. date correction] | repeat [archives previous sale incl. `prev_vehicle` stashed by the vehicle PATCH]). `date-sold` endpoint = the only place repeats are recorded; tag paths (tags PATCH, congrats card) call it with only_if_unsold=True. Startup migration `sold_count_repair_2026_09` (`repair_false_repeats`) removes same-day bogus history entries and recomputes sold_count (ran on preview: fixed 2; will fix Mike on production after Deploy). sold-quick fallback tag patch no longer wipes existing tags (merges Sold). Verified with a scripted sequence (first/skipped/skipped -> 1; date fix -> same; 400-day-old sale -> repeat 2; repair keeps legit repeats).
- **Blank full-size profile photo (June 2026, user: Chris Harley thumbnail opens black viewer)**: the header avatar renders `photo_thumbnail || photo_url || photo` (often legacy base64) while GalleryModal loads `/api/images/{photo_path}`; on production some CSV-imported contacts carry the migration sentinel `photo_path: "skipped_too_large"` / `"error:..."` (bulk migration skipped >3MB base64) -> `/api/images/skipped_too_large` 404 -> blank. FIX: `GET /contacts/{u}/{c}/photos/all` now treats sentinel/missing photo_path as invalid and SELF-HEALS: uploads the base64 `photo` to object storage on the spot, updates photo_path/thumb/avatar/photo_url/photo_thumbnail, returns the real URL (verified end to end on preview); else falls back to photo_url/photo_thumbnail/base64. Frontend GalleryModal: `onError` fallback chain full -> thumbnail_url -> `fallbackPhoto` (avatar source, passed from contact/[id].tsx). Ship: backend Deploy + eas update.
- **Lead ring quick fixes (June 2026, user: "not auto ringing me / I have to accept then push call")**: (1) `start_call_workflow` fires attempt 1 INSTANTLY via asyncio task when not deferred (job inserted with next_attempt_at=now+30s guard so the 15s scheduler tick can't double-ring). (2) CLAIM = CALL ME: `lead_sources.claim_lead` on a Text+Call source (or auto_call_on_claim) now calls `lead_call_engine.call_rep_now(conv, user)` -> rings the rep's cell (kind 'callback'); `/answer` webhook for the owner returns `twiml_connect` ("You claimed X. Press 1 to connect now"), `/claim` webhook bridges without re-claiming when claimed_by == user; other reps hear "already claimed". Creates a claimed job on the fly when no ladder job exists (text-only source). Response includes `calling_rep`. (3) New `POST /api/lead-sources/ring-me/{conversation_id}?user_id=` (self/manager; 409 if another rep owns it) used by the "Ring me & connect to this lead" gold button in `components/LeadCallTimeline.tsx` (card auto-opens when job active/exhausted; callback rows labelled "Rang X to connect (claimed in app)"; timeline API now returns `kind` per call). (4) Push copy: "Ringing your phone now: answer and press 1" vs "Store closed: ... your phone rings at 9:05 AM. Tap to text now" (no more "tap to claim" on Text+Call). (5) `LeadCallLadder` warns per attempt when a selected rep has no cell number (`ladder-{i}-no-phone`). Deferred-to-opening behaviour kept by design (user: "only ring during hours but call first thing next morning"). `_dial_rep` helper extracted from `_run_attempt`. Verified on preview with Twilio test creds: instant attempt within 2s, claim -> callback ring, TwiML paths, 409. Preview "Website" source ladder rings test user activation-tester (+15005550006).
- NEXT (approved order): Phase A Tag Workflows DONE (see below). Phase B Lead Flows library DONE (see below).
- **Tag Workflows / Workflows screen (June 2026, Phase A)**: `services/tag_workflows.py` + `routers/workflows.py` (GET/PUT/DELETE /api/workflows/{user_id}[/{tag}], campaign-options) = store-wide rulebook for "when a contact is tagged X": stop competing nurtures (Sold removes Working/Met/Lost Contact/Hot + cancels their enrollments), clear hot flag, enroll in the tag's campaign(s) (auto by trigger_tag with store/org copies winning, or manager-picked list), set Jessi mode (contact.ai_default_mode + all existing conversations; `initial_ai_state()` seeds new conversations). UI `app/workflows.tsx` (Hub > Manage > Workflows): plain-English timeline per tag, managers edit / reps view. `apply_tag_workflows(user_id, contact_id, tags_added, source)` is THE hook every tag-adding path must call.
- **Lead Flows Library (June 2026, Phase B) - DONE, tested (backend e2e 22/22 `backend/tests/lead_flows_e2e.py`, testing_agent iteration_305 backend 10/10 + frontend UI; blank-flow 400 bug found + fixed)**: CallRevu-style reusable "what happens when a lead comes in" playbooks. BACKEND `services/lead_flows.py` + `routers/lead_flows.py` (managers/admins only, `/api/lead-flows`): list (returns flows + store reps + store_hours + templates), POST (body or `template_key` = ring_all_then_manager | text_first_push_only | after_hours_jessi; templates pre-fill the store's reps/managers by role), GET/PUT/DELETE/{id} (DELETE 409 while attached unless `?force=true`), POST /{id}/duplicate, PUT /source/{source_id} {flow_id|null} attach/detach. MODEL: flow = Steps (call_attempts up to 6, each {user_ids, delay_seconds, delivery call|push}) + Settings (contact_mode, after_hours_mode, text window, caller_id_mode rep|store, auto_call_on_claim, notify_all_on_intake) + Automations (intake_text, after_hours_text [used instead of intake when store closed], no_answer_text, va_enabled, inquiry_context, tags_on_claim, tags_on_no_answer, exhausted_text_lead, exhausted_push_manager). "FLOW WINS": attaching MIRRORS the flow's FLOW_FIELDS onto lead_sources (flow_id/flow_name set) so every existing intake/ladder/queue path keeps reading source keys; saving a flow re-syncs all sources using it (`sync_flow_to_sources`, response `synced_sources`); `apply_flow()` re-merges at intake (process_inbound_lead), in the call engine (`_job_source`) and claim; PUT /lead-sources/{id}/workflow strips FLOW_FIELDS while attached; detach keeps the last copy. ENGINE: MAX_ATTEMPTS 4->6, per-attempt `delivery: push` (`_push_rep`, outcome 'pushed'), caller_id_mode honoured in `_from_number_for`, `_after_last_attempt` waits RING+10s then (if still unclaimed) pushes reps AND runs `on_ladder_exhausted` (no-answer text from conv.rep_phone/rep line/env number, tags, manager push+notification; lead stays in queue; actions stored on job.exhausted_actions and shown in LeadCallTimeline); `on_lead_claimed` (claim_lead, claim_and_call, phone press-1) adds tags_on_claim + runs tag workflows. FRONTEND: `app/lead-flows/index.tsx` (Hub > Leads > Lead Flows, managers: cards w/ summary, sources chips, Edit/Duplicate/Delete, template sheet), `app/lead-flows/[id].tsx` editor (Steps / Settings / Automations tabs, header Save, summary), `components/admin/LeadFlowPicker.tsx` on Lead Source > Response Workflow (Use a flow / Edit flow / Change / Detach; hides intake, inquiry, reps, contact mode, ladder, after-hours, auto-call, VA toggle while attached), `components/admin/LeadFlowSummary.tsx`, LeadCallLadder gained `max` + `allowPush` props. SAFETY NOTE for testing: the ring_all/after_hours templates pre-fill real store managers (Forest Ward has a real cell in preview) - never run a test lead through a flow that includes them; use Activation Tester (+15005550006) only. Ship: backend Deploy + `eas update`.
- **Image storage self-heal (June 2026, PRODUCTION report: photos missing on web, fine on native = device cache)**: prod `/api/images/*` all 404 while the objects exist (verified via preview key). `utils/image_storage._storage_request` re-inits a stale storage key on 401/403 for reads/lists too (uploads already did); `serve_image` logs the storage status and returns 503 when storage is unreachable; diagnostic `GET /api/images/_health` (key_configured, init_ok, sample_status). After deploy check `_health` in prod; if init fails there the deployment's EMERGENT_LLM_KEY env is wrong (Emergent Support).
- **Vanishing leads fix (June 2026, PRODUCTION report)**: root cause = leads for phone numbers that already exist as the rep's contacts route as `returning_owner` (claimed by the owner -> "Mine"); the customer's reply to the intake text landed in the OLD personal thread (webhook `find_one` on (rep_phone, contact_phone) returned the oldest), so the rep replied there, the lead thread had no "human reply", `process_returning_lead_escalations` auto-released it after 30 min, and a released lead was only visible if the source doc matched the manager's `store_id` -> gone from Leads. Fixes: (1) `lead_queue._lead_scope_or`: managers see every internet lead whose `conversation.store_id` is in their scope (`_me` builds `scope_store_ids` from store_id + store_ids + org stores for org_admin; super_admin `sees_everything`); reps also see leads released FROM them (`prev_owner_id`, set in `release_to_queue`); `_store_sources` uses the same scope. (2) `_owner_replied_elsewhere`: the owner's human reply to the same contact in a sibling thread counts, no auto-release. (3) `twilio_webhooks` thread lookup sorts by `last_message_at` desc so the reply lands in the freshest (lead) thread. (4) Website "New Lead" alerts carry `conversation_id` (alert opens the lead thread); legacy `/demo-requests/{id}/claim` returns `{status:"routed", conversation_id}` instead of minting a duplicate contact for source-routed leads. Regression: `backend/tests/vanishing_leads_regression.py` (12/12). Needs backend Deploy + `eas update`.
- **Lead Threads Merge + Release Warning + Flow Analytics (June 2026) - DONE, tested (`backend/tests/returning_lead_merge_e2e.py` 40/40; regressions vanishing 12/12, lead_flows 22/22, ai_routing 41/41; UI verified on preview)**: (1) MERGE: `process_inbound_lead` for a `returning_owner` lead REUSES the owner's freshest open conversation with that contact (`lead_merged: True`, `lead_count` +1, `lead_created_at` = now, release/alert timers re-armed, `routing_resolved: False`, thread's `created_at`/`ai_mode`/`rep_phone` kept; after-hours + Jessi flips ai_mode auto_reply) and inserts a system message `is_lead_marker: True` ("New Website lead from this customer · <vehicle>"); thread renders `sender:'system'` as a centered chip (gold when lead marker) instead of a contact bubble (`system-event-row`). LEAD CLOCK RULE: any "since the lead landed" math must use `lead_created_at or created_at` (`lead_queue._window/_lead_query/_build_items`, `_owner_replied_elsewhere`, `_first_human_replies` + `_message_stats` via `lead_intake.lead_floors()/lead_floor_stages()` which drop pre-lead messages on merged threads, `timeline_for_conversation`, messages.py `lead_received_at` x2, `/leads/awaiting`). (2) RELEASE WARNING: `process_returning_lead_escalations` (every minute) pushes + notifies (`lead_releasing_soon`, once via `owner_warned`) the owner `RELEASE_WARNING_MINUTES`=5 before `release_at`; `POST /api/leads/queue/{user_id}/keep/{conv_id}` ("I've got this", owner or manager, 403 otherwise, idempotent `already`) sets `routing_resolved/kept_at/kept_by`, clears timers, dismisses the warning, logs a system message; escalation job skips `routing_resolved` convs. Timeline API gained `returning{is_returning, merged_thread, lead_count, release_at, resolved, kept_at, released_at, release_reason}` and its `iso()` now emits tz-aware UTC strings (was naive -> JS parsed as local). `components/LeadCallTimeline.tsx`: returning title/tone, T-minus banner (`lead-release-warning`, red <=5 min) + gold `lead-keep-btn`, `lead-kept-note`. (3) FLOW ANALYTICS: `services/lead_flows.flow_stats(db, flow_ids, days)` (leads excl. is_test, claimed %, median speed-to-claim from claimed_at-lead_created_at, reply %, median first human reply, ladder_runs/no_answer % from lead_call_jobs status exhausted) attached as `stats` on `GET /lead-flows` (list), `GET /lead-flows/{id}` and new `GET /lead-flows/{id}/stats?days=` (managers only); intake stamps `flow_id` on conversations + inbound_leads. `components/admin/LeadFlowSummary.tsx` `FlowStatsStrip` on library cards (`lead-flow-stats-{id}`) and editor (`lead-flow-stats`). SHIP: backend Deploy + `eas update --branch production --message "Lead merge, release warning, flow analytics"`.


- **Native date picker contrast (June 2026, USER-REPORTED)**: Jess saw near-invisible dark spinner text in Add Birthday: iOS UIDatePicker follows the PHONE's light/dark setting unless `themeVariant` is set, so light-mode phones got black text on our dark modal. RULE: every `<DateTimePicker>` MUST pass `themeVariant={mode}` (+ `textColor={colors.text}` for spinners) with `mode` from useThemeStore. Applied to DetailsTab BirthdayModal, DateTimeField (was hardcoded "dark"), AppointmentModal Android pickers, PurchaseHistorySection, broadcast/new, campaigns/new. Ships via `eas update`.
- **AI ROUTING (June 2026) - MANDATORY**: before finishing ANY change to `routers/ai_reply.py`, `routers/twilio_webhooks.py`, `routers/messages.py` or `scheduler.py`, read `/app/memory/AI_ROUTING_RULES.md` and run `cd /app/backend && python tests/ai_routing_matrix.py` (28 cases; must be N/N). The app is live; the user was burned by regressions here. Never simulate the Twilio webhook with real numbers in preview (fires real SMS).
- **ADF alignment + Push to CRM (June 2026)**: (1) INBOUND fixes in lead_intake.py — ADF <street> parsed, contact now gets address_street/address_city/address_state/address_zip (was wrongly city/state/zip), `vehicle` set (was only vehicle_interest), VIN/Stock/Trade-in appended to notes, portal <id> → external_id (fixed ElementTree falsy-element gotcha: never use `el.find(x) or el.find(y)`, childless elements are falsy). (2) OUTBOUND new /app/backend/routers/crm_push.py — GET /api/crm-push/{user_id}/{contact_id}/preview returns ADF 1.0 XML + saved crm_intake_email (on user doc); POST sends the XML by email via Resend to any CRM intake address, tags contact "Pushed to CRM", logs contact_events. Frontend: components/contact/CrmPushSection.tsx in DetailsTab (anyone can push; email input + remember toggle + Copy ADF XML). Round-trip tested: ADF in → contact fields correct → ADF out valid. Actual Resend delivery NOT live-tested (user has no CRM intake address yet; same Resend pattern as 5 working email paths). Also fixed pre-existing server.py lint bugs: duplicate "$ne" dict keys (referral queries matched None) and shadowed get_db in startup index block. Test contact: Sally Tester 6a9079eff2de796445f2a0e9 (preview).
- **Scan Trend Graph (June 2026)**: /api/card/scan-stats/{user_id} now also returns `days` (last 7 days, {day, label, count} via aggregation on card_scans.day). UniversalShareModal QR view renders mini bar chart under the scan pill (today highlighted, counts above bars). Verified: backend buckets + UI render on preview with seeded data (seed removed after).
- User language: English. Keep replies short; for production pushes give ONLY `eas update` one-liner.
- Metro bundler can serve stale bundles — restart frontend supervisor if changes don't appear.
- RN-web may not expose data-testid on TouchableOpacity in header; click by coordinates in screenshot tests.
- Admin login: forest@imosapp.com / Admin123! (see /app/memory/test_credentials.md)
- **Contact Profile Redesign + Auto-Updating Intel (June 2026, this session)**: User approved mockup blend A ("Briefing First") + C (quick actions row). (1) BACKEND: GET /api/contact-intel/{user_id}/{contact_id} now returns `stale` flag — compares cached generated_at vs newest of messages.created_at / contact_events.timestamp / voice_notes.created_at (_latest_activity + _to_utc_dt helpers handle str/datetime mix). No cache + activity → stale:true; no activity → stale:false. (2) FRONTEND contact/[id].tsx: cached intel loads instantly on open; if stale, refreshIntel() silently regenerates (intelBusyRef guard, "Updating…" spinner → "Updated just now"). Removed dead GET /api/intel/ 404 effect (was the pre-existing console 404), old generateIntel/showIntel/intelGenerating. (3) NEW components/contact/IntelBriefingCard.tsx: gold-bordered card under hero — RELATIONSHIP INTEL header + live status, parsed Quick Take (bold), 2 Key Facts bullets, gold "Next:" flag row (from Before Your Next Interaction), "Full briefing" expand → IntelRenderer + data points + manual Refresh. Empty state: "Intel builds itself…" + Generate now. (4) NEW components/contact/QuickActionsRow.tsx: 5 round gold-icon buttons Text/Call/Email/Note/Task (Note = voice memo record toggle, red while recording; Text/Email set composer mode + focus via new inputRef prop on ComposerBar; Task opens AddTaskModal). (5) HeroSection: removed blue call + green mic buttons (now in QA row) and italic intel one-liner. FeedTab: removed old intel pill + Update button. Verified e2e: stale→auto-regen→fresh briefing on Bud, empty state on Sally, Task modal, Email mode switch, expand/collapse.
- **AI Auto-Reply Suppression Bug FIXED (June 2026, this session — PROD RCA)**: User reported AI auto-replies not sending in PRODUCTION despite "Jessi is handling this". Deployer debug confirmed: NOTHING was queued (no ai_reply_queue docs), "You're Needed" escalation fired instead. ROOT CAUSE (reproduced in preview): triple suppression in twilio_webhooks.py + ai_reply.py — (1) `conv_ai_off` treated stale `ai_enabled: False` as AI-off even when ai_mode='auto_reply' explicit; (2) broken `enrollment_ai_queued` any() comprehension forced campaign=None and read stale enrollment ai_assist_mode, wrongly skipping the conversation-level AI block; (3) same stale-flag bug in process_ai_reply_queue's rep-override check cancelled queued items with cancel_reason='ai_disabled_by_rep'. FIXES: explicit active ai_mode (auto_reply/draft_only/auto_with_approval) now ALWAYS wins over stale ai_enabled=False in all 3 places; conversation-level 'auto_reply' also overrides campaign mode in the enrollment loop (no approval gate); enrollment_ai_queued now tracked from actually-fired modes; added diagnostic logs ("No AI queued for conv...", "AI suppressed...", "Mode off — not queuing"). Verified e2e in preview via simulated Twilio webhook (POST /api/webhooks/twilio/incoming, no signature check): before fix = 0 queue docs (bug reproduced); after fix = queued auto_reply → scheduler sent REAL Twilio SMS (mocked:False) → ai message logged. You're-Needed escalation still fires in parallel (unchanged). Also HARDENED the Home master AI switch (was a suspect, not the cause): confirm dialog before pausing, red "AI PAUSED" pill (was subtle gray "AI OFF"), amber "All AI is paused (Home switch)" banner in thread with Resume AI button, and backend now sends notification+push when a customer replies while master-paused (was a silent drop in queue_ai_reply). NOTE: FIX IS IN PREVIEW ONLY — needs production Deploy to take effect. Test data cleaned up, forest's default_campaign_id restored.
- **Home Lead Alert + Thread Gold Polish + Weekly Wins (June 2026, this session)**: (1) HOME LEAD ALERT: GET /api/leads/awaiting/{user_id} (lead_intake.py, before ROI section) returns {count, oldest:{conversation_id, contact_name, received_at}} for active is_internet_lead convs with no human reply. components/home/LeadAlertCard.tsx = red card top of Home ("1 lead waiting 6m" + Respond btn → oldest thread), refetch on focus + 30s. (2) THREAD GOLD CONVERSION: thread/[id].tsx file-local COLORS.accent AND light/dark theme branches → '#C9A962'; userBubbleBg gold + BLACK text; header call/mic → colors.surface circles w/ gold icons (recording stays red); avatar initials gold on surface; Take Over banner, send button (gold when twilio number, black icon), intel refresh, SMS-prompt (green) all de-blued. MessageBubble.tsx media text uses colors.userBubbleText. (3) WEEKLY WINS: GET /api/home/weekly-wins/{user_id} (home_intelligence.py top) = prev Mon-Sun counts {sold: contacts.date_sold, texts: messages sender=user, scans: card_scans.scanned_at, new_contacts: contacts.created_at}. components/home/WeeklyWinsCard.tsx shows ONLY on Mondays (or /home?wins=1 override; hidden when all zeros), placed after quick-action tiles. (4) SPEED LEADERBOARD: already existed as 'Speed' tab in leads.tsx (uses /api/leads/analytics/response-times) — verified renders; converted ALL leads.tsx raw data-testid → testID+dataSet. (5) FIXED phantom bubble bug (testing_agent find): GET /messages/thread excludes sender='ai_draft' + timestamp:None docs (sibling-conv drafts rendered as live-clock inbound bubbles). (6) conversations list now also returns lead_received_at. Tested via testing_agent iteration_287 (backend 10/10, frontend 90%) + all reported issues fixed and re-verified (thread header fully gold, drafts filtered, gold bubble + black text confirmed). Test data cleaned up.
- **Speed-to-Lead Timers (June 2026, this session)**: User-selected scope: Inbox rows + Thread banner (5/15 min thresholds, HUMAN-reply-only stops the clock — AI/auto texts sender='ai' never stop it; matches existing _first_human_replies logic in lead_intake.py). Push alerts already existed (instant rep push + workflow blast + 15-min manager slow-lead alert). NEW: (1) messages.py get_conversations enriches is_internet_lead convs with `awaiting_first_reply` (batch via _first_human_replies); get_conversation_info returns is_internet_lead/awaiting_first_reply/lead_received_at/lead_source_name. (2) components/LeadWaitTimer.tsx: self-ticking LeadWaitChip (30s tick, inbox nameRow) + LeadWaitBanner (15s tick, thread under search bar) — green <5m / amber <15m / red 15m+. (3) thread/[id].tsx: leadWait state set from convInfo, banner hidden on send success (both personal-SMS and regular flows setLeadWait(null)); backend flips flag permanently once human message stored. Verified e2e on preview with seeded 8-min-old lead: chip amber "Waiting 10m" in inbox, banner in thread, AI auto-reply did NOT stop timer, human send cleared banner + awaiting→false. Test data cleaned up. NOTE: /inbox URL on web serves a MARKETING page — the app inbox tab must be reached via tab bar navigation in tests. Home alert card built in the following task (see Home Lead Alert entry).
- **Contacts Page Redesign (June 2026, this session)**: Full modern rebuild inspired by Attio/Clay/Dex (user approved "blend A+C" from 3 generated mockups). Smart List cards with live counts (Needs Attention = last activity 14-90d ago, Hot Leads = tag 'hot', New This Week, Birthdays next 30d) via GET /api/contacts/{user_id}/smart-lists + smart_list= filter param (route defined BEFORE /{user_id}/{contact_id}). Rows: recency dot on avatar (green<=7d/amber<=21d/red>21d from last_activity_at), relationship subtitle "2d ago · 2023 F-150" (phone fallback), status pill (VIP/Sold/Hot/Referral/Prospect), gold sparkle AI-draft button -> DraftMessageSheet. Swipe left = Text/Call/Email (native: RNGH Swipeable, web: WebSwipeableItem + wasRecentSwipe() press guard). Filters collapsed into bottom sheet (Mine/Team, Recent/A-Z, CRM status, tags) with gold count badge on search-bar icon. Tag + CRM filters now SERVER-SIDE (tag=/crm= params) fixing count mismatch; A-Z uses case-insensitive collation; last_activity_at enriched in ALL branches; ran POST /api/admin/backfill-last-activity on preview (MUST run once on production after deploy). Smart bar hidden in team view. RN-web drops raw data-testid props -> use testID + dataSet={{testid}} pattern (KEY LEARNING for all future components). Files: app/(tabs)/contacts.tsx (rewritten), components/contacts/{SmartListBar,ContactRow,ContactFilterSheet}.tsx (new), routers/contacts.py. Tested via testing_agent iteration_286 + all reported issues fixed and re-verified. (Old GET /api/intel/ 404 console noise FIXED in Contact Profile Redesign session — dead effect removed.)
- **Lint cleanup (June 2026, this session)**: deleted dead server_old_backup.py/server_v1_backup.py, fixed missing get_db/os imports in scheduler.py, removed shadowing asyncio import in voice_notes.py, added missing useRouter in app/opt-in/[cardId].tsx.
- **Birthday Capture (June 2026, this session)**: PATCH /api/contacts/{user_id}/{contact_id}/birthday (lightweight set/clear, stores BSON date, 400 on bad format). Contact profile Details tab: "Important Dates" section now ALWAYS renders with an orange "Add birthday — unlocks auto birthday texts" row when missing (opens BirthdayModal in DetailsTab.tsx: web <input type=date>, native DateTimePicker spinner, Save/Clear); existing birthday row is tappable to edit. Contacts list: purple gift chip "🎁 Xd / Today!" on rows when birthday <=30d away (daysUntilBirthday exported from ContactRow.tsx); sparkle AI-draft auto-switches to 'birthday' reason ("Why now: Birthday in X days", gift icon) — verified real LLM birthday message. Once birthday is set, the pre-existing "Birthday text + card" auto-send opt-in toggle appears. Demo birthdays set on preview: Sally Tester, Bud, John Dealer. Verified e2e via curl + screenshots.
- **Birthday Import + Campaign Split (June 2026, this session)**: (1) Device import now pulls birthdays: added Contacts.Fields.Birthday to expo-contacts fetch in app/contacts/import.tsx, maps {day,month(0-based),year||1900} -> ISO, shows purple gift line in preview + "auto texts stay OFF" note, sends in import payload (backend ContactCreate already had birthday; stores BSON date; tags stay [] so NOTHING auto-sends — user explicitly required this). CSV/VCF already parsed birthdays (csv_import.py _parse_birthday/BDAY). (2) Split view: smart_list values birthdays_on (birthday + tag ~birthday case-insens) / birthdays_off (birthday + no tag) added to _smart_list_filter + counts. Contacts page: tapping Birthdays card shows sub-chips "Upcoming | Auto-text ON · n | No auto-text · n" + gold "Manage" link -> existing /settings/date-recipients bulk opt-in screen (backend GET/POST /{user_id}/date-optins[,/bulk] pre-existed). ContactRow gift chip shows green bell when enrolled. (3) FIXED recency bug: stored last_activity_at could be stale (some event writers bypass utils/contact_activity.log_contact_event) — page enrichment in get_contacts now always takes max(stored, computed-from-events) for the page ids, and backfill_last_activity_at also repairs stale (not just missing) values ($lt filter). Re-ran backfill on preview (repaired 7). REMINDER: run POST /api/admin/backfill-last-activity on production after deploy. All verified via curl + screenshots (split chips, Manage nav, import w/ birthday tags=[]).
- **Dates Calendar (June 2026, this session)**: User-approved hybrid design (mockups first this time — user was frustrated previous feature was built without mockup signoff; ALWAYS mock up before building for this user). New screen app/dates-calendar.tsx reachable from Hub > Manage > "Dates Calendar". Backend GET /api/contacts/{user_id}/dates-calendar?year&month returns all birthday/sold/anniversary events for the month (skips anniversary rows duplicating date_sold same month+day; years computed when source year 1930<y<now). Screen: month nav chevrons, custom month grid with colored dots (purple birthday / green sold / pink anniversary), tap day filters agenda (banner to clear), legend, filter chips with counts (All/Birthdays/Sold/Anniv), day-grouped agenda rows (type icon + avatar + "Birthday · Sep 2 · turns 41" / "Sold 2023 F-150 · Jun 9 · 2 yrs"), TAPPABLE "AUTO ON/OFF" pill = optimistic enrollment toggle via existing POST date-optins/bulk (occasion birthday|anniversary; sold events map to anniversary occasion), row tap -> /contact/{id}. Verified e2e: month nav, dots, day select, pill toggle round-trip persisted, contact navigation.
- **Home Redesign Hybrid (June 2026, this session)**: User approved via mockups (option C hybrid). app/(tabs)/home.tsx: (1) Greeting header "Good morning/afternoon/evening, {first_name}" + left-aligned Home title. (2) HomeSmartBar (components/home/HomeSmartBar.tsx, same style as contacts SmartListBar): Today X/3 (scrolls to Your 3 via scrollRef+my3Y onLayout), Overdue (-> touchpoints), Hot Leads (-> /contacts?smart=hot; contacts.tsx now accepts `smart` route param), Sold This Mo (-> /sales-list?type=sold). Hot count from contactsAPI.getSmartLists. (3) Old 6-tile grid + Make a Call bar REMOVED; user then requested visible quick actions back -> compact 1-row tiles under hero: SOLD! (gold hero tile), Contact, Card, Review. (4) QuickActionsFab (components/home/QuickActionsFab.tsx): gold + FAB bottom-right (MUST have zIndex:100 — styles.scroll has zIndex:1 which otherwise covers it) opening speed dial: Dates Calendar, Send Photo, Voice Note, Make a Call (user: "quick action dropdown is a killer idea", wants users kept on Home/Contacts/Inbox, out of Hub). All verified via screenshots: tiles render, FAB opens 4 actions, Dates Calendar navs, hot card navs to filtered contacts. USER PREFERENCE: keep daily tools reachable from Home/Contacts/Inbox, avoid burying in Hub.
- **Calendar + Tasks/Appointments (June 2026, this session)**: "Dates Calendar" renamed to **Calendar** (Hub + Home FAB labels already said Calendar). app/dates-calendar.tsx REBUILT: merges date events (birthday/sold/anniv) with tasks from GET /api/tasks/{user_id}/calendar?year&month (tasks mapped UTC→local day client-side; gold dots on grid; Tasks legend + "Tasks · n" filter chip; agenda task rows show time · type · contact with gold-bordered card, complete-circle → PATCH /tasks {action:'complete'} optimistic w/ strikethrough). Gold + header button + empty-state "Add a task" open components/CalendarTaskModal.tsx (title, ApptTypeRow, DateTimeField date+optional time, debounced contact search). VERTICAL-AGNOSTIC types per user (revised twice, final): APPT_TYPES = **call | text | appointment | task** (user: "meeting is a lot like an appointment" — delivery/meeting/test_drive removed from UI+AI prompt; legacy stored values still display their labels, test_drive shows as "Appointment"). AI extraction enum call|text|appointment|task, invalid → 'task'. Backend: get_calendar_tasks now EXCLUDES type campaign_send/campaign_step (was polluting calendar with 16+ auto campaign sends). AI call extraction (tasks.py extract_appointment_from_call, hooked in twilio_webhooks + calls.py) verified live: transcript "Wednesday at 2pm works" → gpt-5.2 → task Wed 2:00 PM local (user tz from login Intl auto-detect), dedupe ±45min, push+notification. send_task_reminders (scheduler, 60s): 15-min-before + at-time pushes for has_time tasks, reminded_15/reminded_due flags. AddTaskModal (contact page) de-blued to gold. RN-web NOTE recurrence: raw data-testid on TextInput gets DROPPED — must use testID+dataSet (fixed in CalendarTaskModal). All flows verified e2e on preview: add w/ contact+time+type, agenda render, complete, month nav, AI extraction. Test data cleaned.
- **Alerts Center Redesign + Cleanup (June 2026, this session)**: User picked agent's blend (mockups generated + approved "whatever is cleanest"). BACKEND notifications_center.py REWRITTEN: feed split — GET /api/notification-center/{uid}?feed=for_you|activity&category=all|urgent|leads|replies|appts. FOR_YOU_TYPES map classifies db.notifications (you_are_needed/slow_lead→urgent; jump_ball/new_lead/lead_assigned/new_demo_request/engagement_signal/keyword_alert→leads; customer_reply*→replies; appointment_extracted/task_reminder→appts) + overdue/due-soon tasks (campaign types EXCLUDED)→appts + unread convs→replies. ACTIVITY feed = contact_events (sms/email sent, link clicks, cards), campaign pending sends, flagged convs, date_trigger/milestone/ai_outreach/call_recorded/etc. UNREAD COUNT FIX: /unread-count now derives from same feed build (cached 30s per-user, filtered per request) — badge ALWAYS matches visible For You unread. read-all no longer sets dismissed (items stay visible as read). FRONTEND app/notifications/index.tsx REWRITTEN as "Alerts": gold Read All pill, For You·n | Activity·n segmented control (gold active/black text), smart filter cards (All/Needs You/Leads/Replies/Appts w/ icon circle+count, HomeSmartBar pattern), day-grouped (TODAY/YESTERDAY/date) gold uppercase headers, rounded card rows w/ gold left border + dot when unread. NotificationBell.tsx restyled gold (title "Alerts", chips/dots/markAll de-blued, new categories) — dropdown auto-clean via default feed=for_you. Verified e2e: feed split, counts, mark-read round trip, read-all→count 0, bell dropdown render (bell needs coordinate click in tests ~354,32).
- **Reply Health Check (June 2026, this session)**: GET /api/home/reply-health/{user_id} (home_intelligence.py) counts ai_reply_queue status='failed' last 24h ($or assigned_user_id/user_id). components/home/ReplyHealthCard.tsx = amber warning card on Home under LeadAlertCard ("N AI replies didn't send · tap to review" → thread of latest failure or inbox). Hidden when 0 failures. Verified with seeded failure.
- **"All Good" Waiting Clear (June 2026, this session)**: User couldn't exit the Waiting queue without Take Over→re-enable AI dance. NEW: green "All Good ✓" button (1) on inbox Waiting-tab rows next to YOU'RE NEEDED/Take Over badges, (2) in thread gold "Jessi is handling this" banner (shown when convInfo.needs_assistance). Calls PUT /api/messages/conversations/{uid}/{convId} {needs_assistance:false} — backend (messages.py update_conversation) now on explicit needs_assistance:false also zeroes unanswered_customer_replies + dismisses you_are_needed notifications for that conversation; AI mode/enabled UNTOUCHED. get_conversation_info now returns needs_assistance + unanswered_customer_replies. Verified: authed PUT round-trip (flag+counter cleared, ai_mode auto_reply preserved, notif dismissed), UI buttons render on waiting rows.
- **Waiting Digest (June 2026, this session)**: db.waiting_clear_log audit {user_id, conversation_id, reason 'satisfied'|'expired', cleared_at} written by BOTH auto-clear paths (twilio satisfied block + scheduler expire job). weekly_wins endpoint returns waiting_cleared (last Mon-Sun count); WeeklyWinsCard shows green footer "N waiting alerts cleared themselves — no action needed" (testid wins-waiting-cleared); Monday wins push body appends "N waiting self-cleared" (doesn't gate the push). Verified: endpoint returned 3 w/ seeded logs, card footer rendered on /home?wins=1, seeds cleaned.

- **Waiting Auto-Expire (June 2026, this session)**: scheduler expire_stale_waiting_flags (hourly IntervalTrigger, id expire_stale_waiting): stamps you_are_needed_at=now on flagged convs missing it, then clears needs_assistance+counter on convs where you_are_needed_at < now-3d and dismisses their you_are_needed notifs. Quiet — no pushes. Verified: preview run cleared 3 stale flags immediately, stamped 11 legacy unstamped (expire in 3d). Backend-only → next Deploy.

- **Auto-Clear Waiting on Happy Reply (June 2026, this session)**: twilio_webhooks.py — new _is_satisfied_reply(text) heuristic (≤80 chars, no '?', no _NEG_WORDS pushback; _SAT_WORDS tokens via regex word-split + _SAT_PHRASES substrings; "no problem/worries" stripped before neg check; 15/15 unit cases). In /webhooks/twilio/incoming after unanswered increment: if satisfied AND conv AI active (ai_enabled≠False, ai_mode not off/draft_only/assisted) → needs_assistance=False, unanswered=0, dismiss you_are_needed notifs, conv_unanswered=0. Escalation block gated `and not is_satisfied`; generic notify-rep block: notif_type/priority/push icon follow satisfied state (happy reply logs normal "X replied" not urgent "needs you"). Verified via live webhook simulation (curl POST /api/webhooks/twilio/incoming, From=forest's cell +18016349122 To=+14352203414): flag+counter cleared, ai_mode auto_reply preserved, 0 new you_are_needed, AI reply still queued (cancelled in test cleanup to avoid real SMS). Backend-only → ships with next Deploy.

- **Alert Quiet Hours (June 2026)**: Overnight push silencing w/ morning summary. BACKEND: user_schedules gained overnight_quiet/overnight_start("21:00")/overnight_end("07:00") (ScheduleUpdate + PUT/GET /api/schedule/me + serialize). New is_quiet_now(user_id) in user_schedule.py = overnight window (local tz, handles cross-midnight) OR legacy notification_quiet off-shift; available_override_until wins. send_push_to_user (push_notifications.py) now HOLDS pushes into db.held_pushes (cap 50/user) during quiet instead of silently dropping (old is_user_available gate REPLACED — schedule quiet mode also gets summaries now). scheduler deliver_held_push_summaries (every 10 min): quiet ended → 1 held = original push as-is; 2+ = "While you were away — N alerts" w/ first 3 titles → /notifications; marks delivered; prunes delivered >7d. FRONTEND settings/notifications.tsx: gold "QUIET HOURS" card (moon icon, Silent overnight toggle, Quiet from 8/9/10/11 PM + Until 6/7/8/9 AM gold chips) — saves via PUT /schedule/me with Intl-detected timezone. Verified e2e: quiet window → 2 pushes held → window ended → summary job delivered both (web-push key errors in preview = forest's stale test subscriptions, not pipeline). Forest's schedule state restored, test held_pushes cleaned.
- **Weekly Wins Push (June 2026)**: scheduler send_weekly_wins_push — Mondays 14:45 UTC (~8AM MT), pushes "Your week in review is ready" (body = "X sold · Y texts · Z card scans · N new contacts last week") → /home?wins=1 (opens WeeklyWinsCard forceShow). Skips all-zero weeks; dedupe via weekly_wins_push_log {user_id, week}. Job ran clean in test (1 push logged, log cleaned after).
- **Digital Card Photo + Black Bar + Inbox Bubble Fixes (June 2026, this session — USER-REPORTED w/ screenshots)**: (1) vCard "Save My Contact" now EMBEDS the profile photo as base64 JPEG (digital_card.py generate_vcard fetches bytes from http://127.0.0.1:8001{/api/images path}?format=jpeg via httpx, ≤900KB; iOS ignores PHOTO;VALUE=URI — that was 'photo not attaching'). (2) card/data absolutizes photo_url/cover/logo via _abs_url (PUBLIC_FACING_URL/APP_URL); card/[userId].tsx Image has onError→placeholder (was empty gold box when image 500/404'd). (3) Black bar on public card page: document.body/documentElement backgroundColor now synced to theme.bg on web (browser chrome/overscroll showed mismatched #000). (4) Inbox rich-card bubbles (thread MessageBubble.tsx): text was colors.userBubbleText (DARK) on dark contactBubbleBg → unreadable; now isRichContent uses colors.text; Digital Card label #5856D6 → #6FA8FF. SHIP: card page + vCard = backend/web Deploy; bubble fix = eas update. Verified: curl (abs URL + PHOTO;ENCODING=b in vcard) + screenshots (body bg synced, placeholder fallback, thread renders).
- **Broadcast Delivery Results (June 2026, this session)**: NEW `GET /api/broadcast/{id}/recipients?user_id=` (broadcast.py) — per-recipient results from campaign_pending_sends: name/phone/status(sent|failed|queued|unconfirmed)/error/deferred_reason; ownership-checked. broadcast/[id].tsx: 'Delivery Results' card (shown for sending/sent/failed) with color badges, failed rows show exact Twilio error, queued rows show 'Held: quiet hours — sends at 9 AM', refresh button (refresh-results-btn), rows tap → contact page; Sent/Failed stats now show for sending+failed statuses too (was sent-only). 'unconfirmed' = legacy pending_user_action docs (pre-fix, outcome unknown). Verified: curl endpoint + screenshot with seeded sent/failed/queued docs (cleaned up after).
- **Broadcast MMS Failure Fix (June 2026, this session — USER-REPORTED, PRODUCTION)**: "Blast to Jessica and Forest didn't go out." Deployer RCA: Twilio error 21620 — the composer pushed the RAW LOCAL iOS file:// path into media_urls (upload code was a stubbed TODO: `uploadedUrls.push(img.uri)`), Twilio can't fetch device paths; both sends attempted+rejected, docs marked pending_user_action with NO error stored, broadcast stuck status='sending' forever. FIXES: (1) broadcast/new.tsx now actually uploads photos to POST /api/images/upload (public, auth-exempt, Twilio-fetchable) and uses original_url; upload failure aborts with a clear alert. (2) broadcast.py send endpoint sanitizes media_urls (keeps http(s), prefixes /api/ paths with PUBLIC_FACING_URL/APP_URL, DROPS file:///data: junk) so old drafts self-heal to text-only. (3) scheduler.py: Twilio failures now persist status='failed' + error text on the pending doc, skip fake message/event logging; new _update_broadcast_progress() increments broadcasts.sent_count/failed_count and finalizes status sent/failed + completed_at when queue drains; broadcast sends marked 'sent' (was mislabeled pending_user_action since delivery_mode 'auto'≠'automated'). E2E-verified in preview with unroutable +19999999999 (no real SMS): file:// dropped, quiet-hours gate confirmed (deferred at UTC midnight → tz temporarily Auckland to test), Twilio 21211 → doc failed w/ error, broadcast finalized 'failed', all test data cleaned + tz restored. PRODUCTION NOTE: their stuck 'Test' broadcast stays status='sending' (harmless); after backend redeploy + eas update, user should DUPLICATE it and resend (sanitizer will drop the bad URL; re-attach photo for MMS).
- **Timezone Month-Rollover Fix (June 2026, this session — USER-REPORTED)**: "3 August sold deals vanished from Home (0) but sold screen shows them." ROOT CAUSE: `/users/{id}/sold-performance` anchored 'current month' to SERVER UTC (rolled to Sept 1 while user's phone was still Aug 31); sales-list uses client-local date → mismatch. FIX: sold-performance + sold-monthly-summary accept optional `month`/`year` params anchoring the series to the CLIENT's local date; home.tsx (2 call sites) + sales-list.tsx now pass local month/year. Verified via curl: unanchored=Sep 0, anchored Aug=correct counts. RULE: any 'this month/this week' server counting must accept a client-local anchor. NOTE: needs BOTH backend redeploy AND eas update to fully fix production.
- **Twilio Broadcast Replaces Quick Broadcast (June 2026, this session)**: User: "Quick Broadcast sends from my phone natively, not my tracking number; want to attach a list + blast to tags or hand-picked contacts." CHANGES: (1) Entry points rerouted — Hub 'Send a Blast' tile → /broadcast/new, My Brand card + my-profile row → /broadcast ('Mass text from your tracking number'); Quick Broadcast (/broadcast-message) has NO entry points now (file kept). (2) NEW 'Pick Specific Contacts' section in broadcast composer (search+checkboxes+chips, testids pick-contact-search/pick-result-*/picked-*); backend get_filtered_contacts: picked-only = just those ids, picked+tags = UNION; preview accepts contact_ids param; malformed ids → 400 (blast-radius guard). (3) CSV list upload: /contacts/import CSV flow has 'Tag this list' input (list-tag-input) → confirm endpoint ?list_tag= creates the tag doc + tags new AND matched-duplicate contacts ($addToSet via get_data_filter scope) so whole list is targetable in broadcast; composer refetches tags on focus. (4) Contacts search now matches full 'First Last' queries (contacts.py get_contacts). (5) Save Draft: canGoBack fallback → router.replace('/broadcast'). (6) Exclude-tags list collapsed by default (toggle-exclude-tags). (7) TESTER FIX kept: import.tsx was hardcoding prod URL + raw axios (CORS/401 on web) → now web-relative base + shared api instance; same guard applied to help.tsx and admin/brand-assets.tsx. Stagger stays 10s; sends go from rep's Twilio number via campaign_pending_sends. Tested: testing_agent iteration_289 (backend 10/10 pytest, frontend E2E all flows, NO real sends) + all follow-up fixes self-verified (400 on bad id, full-name search, exclude collapsed, pytest re-run 10/10). Needs `eas update` for phones. WARNING for future agents: never call POST /broadcast/{id}/send in tests — real SMS.
- **Sold Units Tracker (June 2026, this session)**: User asked for month-by-month sold history with MoM + YoY comparisons, all clickable from Home. Picture avatars added: sold list rows render contact photo_thumbnail over the gold initial (initial shows if photo missing/fails; avatar has overflow:hidden). iOS FIX (June 2026): photo_thumbnail is now ABSOLUTIZED server-side in sold-contacts (relative /api/... paths work on web but NOT native iOS — no origin to resolve against); backend-only fix, needs Deploy not eas update. BACKEND (server.py): new `GET /api/users/{id}/sold-monthly-summary?filter_type=&scope=me|team` (24-month aggregation on date_sold + year_totals; team scope for MANAGER_ROLES = super_admin/admin/manager/store_manager/org_admin — admins see all stores, managers their store via shared `_team_user_ids` helper); extended `GET /users/{id}/sold-contacts` with `scope=team` + `rep_name` per contact. FRONTEND: rewrote `app/sales-list.tsx` → ‹month› arrows with big total, compare chips (vs last mo +/-, same-month-last-year "15 → 20 (+33%)", year total), tappable 12-month bar chart (jump to month), Me|Team segmented toggle (managers only), team rows show rep name, contact tap → profile. Home entry points: SmartBar "Sold This Mo", "August Sales" Sold/Referrals/Repeats tiles (already routed), + WeeklyWinsCard "Sold" stat now tappable → /sales-list. Self-tested: curl both endpoints (me/team/list w/ rep names) + browser E2E (month nav, team toggle, contact open). Needs `eas update` to reach phones.
- **Copy-Link Fix Everywhere (June 2026, this session — USER-REPORTED BUG)**: "Copying links (error report / showcase / digital card) doesn't copy the actual link." ROOT CAUSE: ~20 copy handlers were WEB-ONLY (`navigator.clipboard`/`execCommand` gated behind Platform.OS==='web') — on the native iOS/Android app they copied NOTHING but still showed "Copied!". FIX: new shared helper `/app/frontend/utils/clipboard.ts` → `copyToClipboard(text)` (expo-clipboard `setStringAsync`, works native+web, web execCommand fallback). Rewired 22 files: UniversalShareModal (Copy Link + Share Link — native now opens real RN Share sheet with the URL), admin/error-reports (Copy All Reports), more.tsx, my-profile share chips (native = share sheet w/ actual link, was SMS-only), brand-assets, discount-codes, lead-sources x2 (webhook URL copy), ai-outreach, imos hub+app-directory, my-account, partner/onboard, quick-send copy method, settings/link-page (was OPENING the url on native instead of copying), broadcast-message, broadcast-card, settings/create-card success share, UniversalComposer, PresenceLinks, StoreManagement, contact/[id]. RULE FOR FUTURE AGENTS: NEVER write `navigator.clipboard` directly — always `import { copyToClipboard } from 'utils/clipboard'`. Tested via testing_agent iteration_288: 8/9 flows verified with real clipboard READ-BACK on web (correct URLs). ALSO: removed 597 lines of DEAD CODE in more.tsx (3 unreachable share modals — review/showcase/birthday — plus their handlers/states/styles/match-modal; no trigger ever opened them; Hub verified renders after); fixed latent crash: Delete Account used showAlert without import; added testID+dataSet to share-copy-link/share-via-link/error-reports-copy/header-qr-btn/my-profile share chips. KNOWN COSMETIC: RN-web console warning "Unexpected text node" on quick-send card screen (couldn't locate statically, no user impact). Frontend-only → needs `eas update` to reach phones.
- **Hub UI Formatting Cleanup (June 2026, this session)**: Clicked through Hub screens fixing theme inconsistencies in BOTH light+dark modes: email-signature.tsx, seo-health.tsx, geo-health.tsx, admin/hot-leads.tsx, admin/team-engagement.tsx, settings/schedule. Fixed hardcoded text/background colors + the recurring `colors.background` vs `colors.bg` bug (theme store exposes `colors.bg` — colors.background is undefined and renders white/transparent). Verified via screenshots both modes. USER CONFIRMED closed. Frontend-only → user runs `eas update --branch production` to ship.
- **Inbox Polish (June 2026)**: User approved hybrid via mockups. app/(tabs)/inbox.tsx: ROOT CAUSE of old clash was file-local COLORS_DARK.accent '#2E5CFF' (bright blue) -> changed to '#C9A962' gold (also COLORS_LIGHT.accent + borderFocus) which auto-fixed compose btn, checkboxes, toggle, empty states. Avatars: muted '#2C2C2E' bg + gold initials (was solid blue). Unread: gold left border on row (conversationItemUnread) + gold count bubble w/ black text. Rows: borderRadius 16, no bottom border. My/Team toggle: gold active + black text/icons. Tab pill bar REPLACED with smart-card filter bar (icon circle + count + label, same as Home/Contacts): Hot, Waiting, NEW 'unread' filter (added to activeTab union + filter switch + counts), All, AI, Active, Closed. AI-mode chip on rows: subtler ('off' = transparent + hairline border; assist = gold not blue). Converted conversation-item / my-inbox-toggle / team-inbox-toggle data-testid -> testID+dataSet. Verified: filters work, unread filter, row->thread nav, team view, AI toggle tap. NOTE: renderTeamConversation styles inherit the gold accent automatically.

## Stripe Integration (Resolved — June 2026)
- Stripe was already fully implemented (checkout, quote payments, /checkout/status, webhook, pricing UI).
- Root cause of "Payment system not configured": backend process (port 8001) did not have STRIPE_API_KEY in its env.
- Fix: added STRIPE_API_KEY="sk_test_emergent" (Emergent managed claimable sandbox) to /app/backend/.env and restarted backend.
- Verified live: POST /api/subscriptions/checkout returns a real https://checkout.stripe.com/... URL.
- Go-live: user claims the sandbox into their own Stripe account (no code change needed).

## iOS Avatar Rendering Fix (June 2026)
- Root cause: several screens used React Native core `Image` for REMOTE photo avatars. Renders on web, unreliable on iOS with newArchEnabled=true. Working iOS avatars (inbox/thread/more) all use `expo-image`.
- Switched to `expo-image` (contentFit + memory-disk cache): app/sales-list.tsx, app/l/[username].tsx, app/p/store/[slug].tsx.
- Left on core Image intentionally: local require() logo assets (imos/_components.tsx), import-guide screenshots, white-label (Image import unused).
- DELIVERY: frontend/JS change — requires `eas update --branch production` (+ app reopen). Backend deploy alone does NOT update the installed app's JS.

## Keyboard Covers Date/Time Picker Fix (June 2026)
- Bug: New Task form (calendar) auto-focuses title TextInput; tapping date/time button opened spinner picker WITHOUT dismissing keyboard -> picker hidden behind keyboard. iOS-only (web uses HTML date inputs).
- Fix: added Keyboard.dismiss() to date/time picker open handlers in:
  - components/DateTimeField.tsx (covers CalendarTaskModal + AddTaskModal)
  - components/AppointmentModal.tsx (date + time buttons)
  - app/contact/sold-wizard.tsx, app/sold-quick.tsx (sale date buttons)
- DELIVERY: JS change -> requires `eas update --branch production` + app reopen.
- Not reproducible in web preview / automated tester (native iOS keyboard behavior).

## Last Week's Wins — Tappable Drill-downs (June 2026)
- Before: only the "Sold" tile navigated (to /sales-list?type=sold); Texts/QR Scans/New were disabled.
- Now: all 4 tiles tap to a new screen /weekly-wins-list?type=sold|texts|scans|contacts.
- Backend: GET /api/home/weekly-wins/{user_id}/list?type=... (home_intelligence.py) — same Mon-Sun window as /weekly-wins so counts match.
  - sold: contacts with date_sold in window
  - contacts: contacts with created_at in window (shows source)
  - texts: distinct conversations user messaged in window (shows msg count), resolved to contact name via conversations.contact_id
  - scans: card_scans events in window (ANONYMOUS — no contact identity captured; listed by date)
- Frontend: app/weekly-wins-list.tsx (clone of sales-list pattern, expo-image avatars); WeeklyWinsCard.tsx tiles wired.
- Verified: backend curl all 4 types OK; frontend bundles clean. NOTE: screenshot tool cannot render this Expo web app headless (blank) — device confirmation after eas update.
- DELIVERY: backend deploy + `eas update --branch production`.

## Relationship Intelligence — Scope C (June 2026)
Built the "IMOS remembers the person" layer. 5 items + score:
1. Relationship Profile at top of contact card — ALREADY existed (IntelBriefingCard @ contact/[id].tsx line ~2539); verified in place.
2. People to Talk To Today feed — NEW screen app/people-today.tsx + endpoint GET /api/home/people-to-engage/{user_id}?limit (widened get_my_3 via top_n param). Home "See all →" link added to the "Your 3 for Today" header.
3. Relationship Health Score + Book of Business rollup — NEW router routers/relationship_health.py:
   - GET /relationship-health/{user_id}/summary  (bucket rollup)
   - GET /relationship-health/{user_id}/contacts?bucket=  (drill-down, worst-first)
   - GET /relationship-health/{user_id}/contact/{contact_id}  (badge, scoped by user_id)
   Buckets: opportunity(🔥 recent engagement/sold<30d) / at_risk(🔴 90d+) / cooling(🟡 45-90d) / advocate(💙 review or referrer) / connected(🟢 <45d).
   Last-touch derived from contact_events (real event types), NOT the never-written last_contacted_at. Advocate = review events + advocate/referral tags + name appears as a referrer (referred_by_name reverse lookup). 30s TTL cache.
   Frontend: app/book-of-business.tsx, components/home/BookOfBusinessCard.tsx (home), components/contact/HealthBadge.tsx (contact card).
4. Human-readable timeline — Feed tab already renders human event titles/descriptions; no change needed.
5. Touch tagging (Transactional/Promotional/Relationship) — GET /api/home/touch-mix/{user_id}?days heuristic classifier; surfaced as "X% real relationship touches this week" line in BookOfBusinessCard.

SECURITY FIXES (from test iteration 290 — BOLA/PII leak found & fixed):
- server.py enforce_user_ownership: added '/api/relationship-health/' to PROTECTED_PREFIXES AND changed extraction to scan the path for the FIRST ObjectId segment (covers /api/home/people-to-engage/{uid} & /touch-mix/{uid} & weekly-wins which were previously unauthenticated).
- health_one now filters db.contacts by user_id (was cross-tenant readable).
- Query validation: days ge=1 le=365, limit ge=1 le=50 (422 on bad input).
Verified: tests/test_relationship_health_290_auth.py 10/10 PASS (401 unauth, 403 non-owner). Functional contracts verified via curl with token.

DELIVERY: backend deploy + `eas update --branch production`. Frontend not visually testable in headless preview (Expo web renders blank in automation) — confirm on device.

## Next Action Items build (June 2026)
2. Voice "Remember This" auto-follow-up: services/voice_intel.py process_voice_note_intelligence now calls _ensure_followup_from_voice — after a voice note is transcribed+extracted, auto-creates ONE follow-up task (due +3 days, type=follow_up, source=voice_note) if none pending. Smart title from extracted interests/spouse/referral. (One-tap mic already existed in QuickActionsRow.)
3. Smarter feed reasons: home_intelligence.get_my_3 now projects personal_details and adds a `hook` field via _personal_hook() (e.g. "Loves Golf", "Spouse: Sarah"). Rendered in app/(tabs)/home.tsx My-3 cards and app/people-today.tsx.
4. Advocate Spotlight: GET /api/relationship-health/{user_id}/advocates (auth-protected). New screen app/advocates.tsx ("Say Thanks" flow), linked from book-of-business.tsx via a "You have N advocates" banner.
Verified: advocates endpoint 401 unauth / returns 5 with token; people-to-engage hook="Loves Golf"; frontend bundles clean (full rebuild after metro-cache purge). Voice auto-follow-up verified by parse + logic (not E2E — needs a real recording).
DELIVERY: backend deploy + eas update --branch production.

## Marketing: Relationship OS deck (June 2026)
- New static presentation at frontend/dist/relationship-os/index.html (marketing site, imonsocial.com — NOT the Expo app).
- Reuses existing marketing chrome (nav/footer/demo-modal/reveal-scroll, Inter font) with gold (#C9A962) accent.
- Narrative: hero ("CRM remembers the deal, IMOS remembers the person") -> problem (74% vs 44% loyalty) -> 4 CSS phone mockups (Book of Business, People to Talk To Today, Voice Remember This, Advocate Spotlight) -> flywheel -> stats -> CTA. Demo form posts to app.imonsocial.com/api/demo-requests.
- Linked from marketing.html Resources dropdown + deck footer.
- Screenshots are hand-built HTML/CSS (accurate text), not AI images. Verified renders 200 via local static server.

## STYLE RULE (June 2026) — NEVER USE EM DASHES
- User directive: NEVER use em dashes (—, &mdash;, &#8212;, &#x2014;) anywhere — marketing site, app UI, or generated content. Use a spaced hyphen " - " or restructure with commas.
- Swept clean on this date: all of marketing/build/*.html (49 files), marketing/generate_pages.py (28), and the new app screens (book-of-business, people-today).

## Auth flows + Impersonation fix + AI phone guard (June 2026)
- See CHANGELOG "Auth + Impersonation + AI phone guard". Key endpoints: POST /api/admin/users/{id}/impersonate (super_admin JWT), /api/auth/activate/{request,verify,complete}, /api/auth/forgot-password/{request,verify,reset}. Screens: /auth/activate, /auth/login link, /auth/change-password (no ToS gate).
- USER TESTING PENDING (frontend): impersonate flow, /auth/activate e2e, first-login Set Password, admin create-user modal copy, thread popup suppression, marketing section.
- Backlog: rotate production secrets; Wallet cert (.p12) parked; HomeNet/vAuto feed; Zapier instructions.

## Home "Do This Next" / "3 for Today" Skip + auto-complete (June 2026) - DONE, verified in preview, awaiting production deploy
- Backend home_intelligence.py: db.home_daily_picks (stable per-day picks), db.home_action_state {user_id, day, key, status done|dismissed}. POST /api/home/{uid}/my3/done and /my3/dismiss; dismiss pulls in a replacement pick. `done` auto-derived from _touched_today (outbound contact_events + outbound messages today) so texting from contact card/inbox/cards completes the item. GET /api/home/{uid} returns my_3[].done + dismissed_keys.
- Frontend home.tsx: Skip on each My-3 card (my3-skip-*), Done check (my3-done-*), Skip on Do This Next (next-move-skip; keys hot:{id} / overdue / pending_tasks / contact_id). Re-verified via curl June 2026 (dismiss -> replacement filled; done flag from outbound text).
- Layout fix (June 2026, user screenshot from prod: text squeezed to 5-char column): My-3 card is now two rows - info row (icon + name + reason numberOfLines=2 + hook, full width) then right-aligned action row (action button my3-action-*, done check, skip). Verified via 390px screenshot on preview. Needs eas update.
- User chose to deploy + test on phone. Ship: backend Deploy + `eas update --branch production`.

## Push alerts investigation (June 2026) - OPEN
- iPhone alerts stopped (prod). Built Push Health Check (Settings -> Notifications) + /api/push/diagnose endpoints so the cause can be read off the device after deploy. See CHANGELOG. Root cause on production not yet confirmed.

## Lead Call Engine + website form routing (June 2026) - DONE (backend tested 20/20, UI smoke-tested)
- Marketing Book a Demo forms -> Lead Source (page-specific or catch-all) -> contact/thread with attribution -> intake text -> push -> CallDrip ladder (<=4 attempts, press 1 to claim, whisper, bridge). Config in Admin -> Lead Sources -> Response Workflow.
- Also fixed: CORS for marketing forms (PathAwareCORS), lead-sources auth.
- Next ideas: business-hours gate for ladder, attempt timeline on the thread, per-source response-time analytics using attribution.
- USER STATUS (June 2026): user wired the Website catch-all + ladder in PRODUCTION. Marketing form -> /api/demo-requests -> _find_website_lead_source (page-specific, else website_default, else legacy sales@ email path).
- iOS crash scare (June 2026): after eas update "My 3 cards stack actions" the app closed on launch; only home.tsx layout had changed; user confirmed it was a phone issue, resolved by itself.

## After-Hours Rule + TCPA window + Test Lead + Attempt Timeline (June 2026) - DONE, tested (iteration_293: 26/26 new pytest + 20/20 ladder + 28/28 AI routing + frontend flows)
- USER DECISIONS: Jessi answers ANY inbound customer text 24/7 (consumer-initiated, no consent tiering); only BUSINESS-INITIATED contact (intake text, rep call ladder) respects a texting window in the CUSTOMER's local time; anything outside the window releases next morning STAGGERED 1 lead/min per store. Jessi answers lead replies whenever the source's "Enable AI Auto-Reply" toggle is on (hours or not) OR the store is closed under the after-hours rule.
- services/lead_timing.py: build_contact_plan(db, source, store, phone) -> plan {customer_tz (phonenumbers area code, ambiguous -> store tz), window{start,end,inside,opens_at}, store{configured,open,opens_at}, intake_at/intake_deferred/intake_reason, ladder_at/ladder_deferred/ladder_reasons ['texting_window'|'store_closed'], jessi_on}. Stagger via db.lead_release_slots {key scope:ISOminute, count}. Store hours = stores.business_hours + timezone (Store Profile screen); opening buffer +5 min.
- Lead Source workflow fields: after_hours_mode 'text_and_ai'|'ring_anyway' (default text_and_ai), text_window_start/end HH:MM (default 09:00-20:00 = strict-state preset; federal preset 08:00-21:00; PUT validates HH:MM -> 400). GET /workflow also returns store_hours {timezone, configured, open_now, opens_at, hours}.
- lead_intake.process_inbound_lead: computes plan, stores conversation.routing_plan, after_hours_lead, ai_mode 'auto_reply'+ai_enabled when plan.jessi_on (else legacy inert 'assist'), sms_consent (from demo form sms_opt_in -> also contact.sms_consent*), is_test + 'Test Lead' tag. Legacy AI first message (inbound_leads.status) is now 'skipped' when the source has an intake_text (was double-texting). _fire_intake_workflow: intake from ASSIGNED rep's Twilio number first (else on-shift pool), sets conversation.rep_phone so the customer's reply routes into the lead thread; deferred intake -> db.lead_deferred_actions (scheduler process_lead_deferred_actions every 30s; cancelled 'rep_replied' if a human already texted); rep push body gains "After hours: Jessi is replying, calls ring at 9:05 AM". Ladder: start_call_workflow(not_before, deferred_reasons) -> job.deferred/deferred_until; process_lead_call_jobs on a deferred first rung skips if conversation claimed (any source) or a rep already texted (status 'handled', handled_reason rep_replied); on fire sets deferred False/was_deferred/released_at; answer prompt says "Overnight lead".
- twilio_webhooks.py: when no contact in the rep namespace, reuse the internet-lead contact from the conversation matched by rep_phone+contact_phone (no more duplicate 'Lead (xxxx)'); verified by stubbed probe (tests/manual_lead_reply_probe.py) + ai_routing_matrix 28/28.
- Timeline: webhooks record answered_at (/answer CallSid), passed (Digits!=1), late; record_call_status keeps keypress outcomes. GET /api/lead-sources/call-timeline/{conversation_id} (manager, or owner/assignee/claimer/ladder member; else 403) -> {plan, intake{sent_at,scheduled_for}, jessi_on, sms_consent, claimed_*, job{status, deferred, was_deferred, deferred_until, attempts, calls[{attempt,name,outcome claimed|passed|late|answered|no_answer|ringing|no_phone|failed}], time_to_claim_seconds (from first ring for deferred jobs), exhausted_at}}.
- POST /api/lead-sources/{id}/test-lead {phone, first_name, last_name, include_ladder} (manager) runs the REAL pipeline (intake SMS, pushes, ladder) tagged Test Lead; returns plan + flags. Frontend components/admin/LeadTimingControls.tsx: AfterHoursRule (mode toggle, store-hours line w/ Edit -> /settings/store-profile, TCPA presets + custom inputs) and TestLeadCard (phone prefilled with admin's cell, name, ladder checkbox, ARM-THEN-TAP confirm "Tap again to text ••••1234 and ring reps", result lines, Open thread). components/LeadCallTimeline.tsx on internet-lead threads (thread/[id].tsx isInternetLead) with header state (Claimed by X in m:ss / Ringing attempt n of N / rings at 9:05 AM / skipped / exhausted) + expandable rows + consent line.
- Also converted raw data-testid -> testID+dataSet on admin/lead-sources/[id].tsx and auth/login.tsx (login-email-input/login-password-input now in DOM). serialize_lead_source now falls back webhook_url to /api/lead-sources/inbound/{id}.
- Test helpers: tests/test_lead_after_hours_293.py (run with -p no:randomly), tests/cleanup_test_leads.py (purges is_test data + slots), tests/manual_*_probe.py. SAFETY unchanged: 500-555 numbers only, Activation Tester only on preview ladders.
- SHIP: backend Deploy (all rules) + `eas update` (Lead Source screen + thread timeline). Production stores need business_hours + timezone set in Store Profile for the after-hours rule to engage (no hours = always "open").

## Contact "Up Next" Tasks Card (June 2026) - DONE, self-tested on preview
- USER: "tie the task queue in better with the contact... task date time and purpose need to be exposed, AI draft sms related to that task, call button". Approved mockup C (hybrid) + auto-complete task when the call connects.
- BACKEND tasks.py: `GET /api/tasks/{user_id}/contact/{contact_id}` -> {tasks, count}; open (pending/snoozed, not completed, excludes campaign_send/step), `is_overdue` (has_time: due<now; else due<user-local today start), sorted overdue-first then due asc. `complete_task_from_call(user_id, task_id, call_sid, duration)` marks completed + completed_via 'call_connected' + task_completed contact_event. twilio_webhooks.py: POST /webhooks/twilio/call accepts `task_id` (stored on pending_calls); /recording-complete (fires only when the customer answered, record-from-answer) auto-completes pending.task_id when RecordingDuration>0. Native dialer path: call-screen saveNativeCallLog PATCHes complete when logged minutes>0. Probe: tests/manual_call_task_autocomplete_probe.py (dur 0 -> stays open, dur 42 -> completed; cleans up). ai_routing_matrix 28/28 after the webhook edit.
- FRONTEND components/contact/ContactTasksCard.tsx (under QuickActionsRow on contact/[id].tsx): featured task (route ?taskId= wins, else first overdue, else soonest) with "UP NEXT"/red "OVERDUE" label, when-label (Today/Tomorrow/weekday/MMM d · h:mm a, "Overdue · N days"), title, description, buttons: call-type -> [Call now (gold) | Write It | Done], else [Write It (gold) | Call | Done]. Others as slim rows (tap = feature it, circle = Done), "Show N more", "Add task +" (opens AddTaskModal; tasksRefreshKey bumps on save + on composer-send auto-complete). Done = optimistic + inline Undo bar (6s, PATCH reopen). Write It -> shared DraftMessageSheet (new hideViewContact prop; reason touchpoint/birthday/anniversary, context = title+description+schedule, task_id -> thread auto-completes on send). Call -> /call-screen?task_id=. Card refetches on focus +5s +15s (webhook lag). Hidden when no open tasks. dates-calendar task rows now push /contact/{id}?taskId=. DraftMessageSheet testids converted to testID+dataSet.
- SHIP: backend Deploy + `eas update`.

## Marketing form auto-retry (June 2026) - DONE, verified in browser
- User hit "Something went wrong" once on the live demo form right after a Deploy (backend restart window); later worked. Production checks from preview: /api/health 200, OPTIONS preflight from www.imonsocial.com OK (allow-origin *), validation POST 200.
- Fix: `marketing/build/lead-retry.js` wraps window.fetch for POST */api/demo-requests: 20s timeout per try, retries after 2s and 5s on network error / 5xx, never on 4xx, returns last response or throws. Added `<script src="/lead-retry.js">` before </head> in all 61 built pages that post leads (+ generator templates) and replaced the alert with "We couldn't reach our server just now... email sales@imonsocial.com" (112 occurrences + generator).
- WARNING: `generate_pages.py` NAV is OUT OF SYNC with built pages (built pages have Solutions dropdown Featured/By Role groups hand-edited in a later commit). Do NOT run generate_pages.py blindly; it drops those nav changes. Edit built HTML via a transform script instead (as done here). frontend/dist/* and marketing/build-preview/* are stale copies, not deployed.
- Verified with Playwright route mocks on a copy served from /api/marketing-preview: fail,fail,200 -> success modal after ~7s; 503x3 -> friendly alert + button re-enabled; 400 -> 1 attempt.
- SHIP: redeploy marketing site (Vercel, marketing/build). No app/backend change.

## Lead form confirmation + Nudge + Morning Brief (June 2026) - DONE, self-tested on preview
- PUSH P0 RESOLVED: user confirmed test push arrives on iPhone (diagnostics card works).
- LEAD FORM CONFIRMATION (generic, per user): lead_intake.py `DEFAULT_FORM_CONFIRMATION` = "Hi {{first_name}}, we got it! Someone from {{store_name}} will reach out shortly. Reply STOP to opt out." Applied in process_inbound_lead when lead has sms_opt_in AND the source has no intake_text (source dict copied with intake_text -> status skipped/intake_text_workflow, so the legacy AI first text never doubles). Respects the same after-hours/texting-window deferral. Preview Website source intake_text set to the generic wording; PRODUCTION: user should paste the same in Admin > Lead Sources > Website > Intake text (or clear it so the default applies).
- NUDGE OVERDUE REPS: POST /api/tasks/{user_id}/team/nudge/{rep_id} (manager roles, same store unless super/org admin) -> push + notification type manager_nudge "Heads up from Forest / You've got N overdue tasks. Oldest: ..." deep-link /touchpoints?filter=overdue; 4h cooldown per rep (429 "Already nudged Xm ago by ..."), 400 when nothing overdue; logged in `task_nudges`. Team endpoint returns `last_nudged_at`; team-tasks.tsx shows red "Nudge" pill on rep header (hidden for self / no overdue) + "nudged 2m ago".
- MORNING TASK BRIEF: push_notifications.send_daily_task_digest rewritten: scheduler runs it every 15 min (was Cron 14:00 UTC); each active user (morning_brief_enabled != false) gets ONE push/day when local time is 07:00-07:14 (user tz -> store tz -> Denver; dedupe users.morning_brief_sent_on = local date). Title "Good morning, Forest - 4 tasks today" body "Call Bud 9:00 AM · Text Amy · ... +N more · 2 overdue" (excludes campaign sends + system dormant follow_ups; adds contact first name when not already in title). Quiet-hours hold still applies (delivered when shift starts). Preview endpoint POST /api/push/morning-brief/{user_id}/send-now (ownership-protected) + "Preview my 7 AM task brief" button in PushDiagnosticsCard.
- SHIP: backend Deploy + `eas update`.

## NEXT (approved, mockup pending): Internet Leads shared queue
- User answers: Q1 ALL of (Inbox "Leads" segment + bottom-bar tab + Home "Leads waiting" strip); Q2 c (manager alert 10 min, auto-release to queue 30 min for returning-customer leads whose owner doesn't reply); Q3 editable per-source timer thresholds (defaults green<5/amber 5-15/red>15); Q4 daily 6 PM manager digest of leads that went red per rep.
- Existing plumbing: lead_sources.workflow_user_ids + call_attempts ladder, claim (/lead-sources/claim/{conv}), claim-and-call, slow-lead alerts (send_slow_lead_alerts), Internet Leads report (/leads), Inbox Team toggle (team-inbox by team_id; sources have NO team_id so website leads are only reachable via push today), conversations.user_id = store_id when jump ball (invisible in inboxes). GAP: existing-contact owner routing not implemented (process_inbound_lead ignores existing_contact.user_id).

## Task Snooze + Tasks-from-Texts + Team Tasks (June 2026) - DONE, self-tested on preview
- SNOOZE: contact card featured task has "Snooze: Tomorrow | Next week" chips (contact-task-snooze-tomorrow/next_week) -> local 9:00 AM tomorrow / next Monday; optimistic re-sort + gold undo bar ("Snoozed to Tomorrow 9 AM: ..." / Undo = PATCH reopen w/ original due_date). Backend PATCH /tasks action=snooze now accepts `snooze_until` ISO (falls back to snooze_hours) and resets reminded_15/reminded_due.
- TASKS FROM TEXTS: tasks.py refactored `_extract_commitment(mode='call'|'text')`; `extract_appointment_from_call` = thin wrapper (unchanged behaviour). New `extract_task_from_text(user_id, contact_id, name, text, message_id)` with cheap regex pre-filter `text_has_schedule_hint` (weekday/tomorrow/time/"call me"/etc; greetings "good morning" stripped) -> gpt-5.2 prompt tuned for customer-speak ("call me Thursday" -> Call Bud Thu 09:00 has_time false; "swing by Saturday at 2" -> Meet Bud Sat 14:00; "are you open Saturday?" -> found false; verified live). Task: type appointment, source text_extraction, source_message_id (dedupe per message + existing ±45min dedupe), description 'From their text: "..."'. Notification type appointment_extracted (Alerts > Appts) + push "Task added - Bud" deep-link /contact/{id}?taskId= (featured on the card). Hooked in twilio_webhooks incoming handler right after keyword tagging as asyncio.create_task (fire-and-forget, NO routing changes; ai_routing_matrix 28/28). Timezone lookup now user.tz (ignores 'UTC') -> store.tz -> America/Denver. NOTE: headless preview logins set forest.timezone='UTC' (login auto-detect); reset to America/Denver.
- TEAM TASKS: GET /api/tasks/{user_id}/team -> {is_manager, totals{open,overdue,today}, reps[{user_id,name,photo_url,open,overdue,today,tasks[]}]}; manager roles super_admin/admin/manager/store_manager/org_admin (admins all active users, others same store_id); excludes campaign tasks + system dormant follow_ups; reps sorted overdue desc. Screen app/team-tasks.tsx (HomeSmartBar totals, All/Overdue/Due today chips, per-rep collapsible cards w/ Avatar, task rows w/ when-label + contact + "from text"/"from call" badge -> /contact/{id}?taskId=). Hub > My Performance > "Team Tasks" (manager roles only). New route files need a frontend restart in preview (expo-router require.context).
- SHIP: backend Deploy + `eas update`.

## Shared Internet Leads Queue (June 2026) - DONE, tested (iteration_294 + fixes re-verified: pytest tests/test_lead_queue_294.py 12/12, ai_routing_matrix 28/28)
- USER DECISIONS: Inbox "Leads" segment + Home "Leads waiting" strip (NO bottom-bar tab); Home strip REPLACES the old red LeadAlertCard (deleted) and covers both unclaimed queue leads and my own claimed leads awaiting a reply; returning customers (existing contact owned by an active rep in the store) skip the queue and go straight to their rep, manager alert after 10m, auto-release to queue after 30m; per-source editable timers (green<5 / amber<15 / red); daily 6 PM manager red-leads digest.
- BACKEND routers/lead_queue.py (prefix /api/leads/queue, router-level `require_queue_user` dependency: JWT caller must equal path user_id or be a manager; server.py ALWAYS_SKIP '/api/leads/' now EXCLUDES '/api/leads/queue/' so enforce_user_ownership applies too): GET /{uid}/summary (Home strip + badge: waiting, oldest, names, red, mine_waiting, mine_oldest, mine_names, heat), GET /{uid} (unclaimed / mine / claimed(managers) + counts + can_claim_source_ids), GET /{uid}/reps (managers; on_workflow flag, on_shift, open_leads; sorted workflow-first), POST /{uid}/reassign/{conv} {to_user_id} (managers, same store unless super/org admin; moves conv+contact, system message, push+notification lead_reassigned), POST /{uid}/release/{conv} (claimer or manager; back to queue, pushes workflow members "Up for grabs", type lead_released), POST /{uid}/digest/send-now (manager preview). Scheduler: process_returning_lead_escalations (1 min), send_red_leads_digest (15 min, store digest_hour local, dedupe stores.red_digest_sent_on). lead_sources.py claim: workflow-membership 403 for non-managers, sets user_id/routing_kind, quiet lead_claimed notice to other members; WorkflowConfig + serialize gained timer_green_minutes/timer_amber_minutes/returning_alert_minutes/returning_release_minutes/digest_hour (clamped; 400 if amber<=green). lead_intake.process_inbound_lead: owner_id routing (routing_kind returning_owner|assigned|queue, owner_alert_at/release_at), returning-owner leads skip jump-ball blast + ladder. Push deep links use "/(tabs)/inbox?segment=leads" (plain /inbox on WEB serves the marketing page from frontend/public/inbox/index.html - pre-existing; native resolves either).
- FRONTEND: components/inbox/LeadsQueuePanel.tsx (chips Unclaimed/Mine/(mgr) Claimed/All, live-ticking cards w/ heat border + status pill, Claim / Claim & Call, "..." menu Open/Reassign/Release, reassign sheet w/ search + "Show everyone" toggle, workflow members first). app/(tabs)/inbox.tsx: 3-way toggle My Inbox | Leads (heat-colored badge = unclaimed + mine waiting; segment shown only when summary.visible or mine_waiting>0) | Team Inbox, `?segment=leads&t=nonce` deep link. components/home/LeadsWaitingStrip.tsx (queue>0 -> Inbox Leads; only mine -> oldest thread). components/admin/LeadTimingControls.tsx QueueTimers (steppers snap to step; digest hour chips 4-8 PM) wired into admin/lead-sources/[id].tsx workflow section.
- Test helpers: tests/seed_lead_queue_demo.py seed|clean (5 demo leads, 500-555 numbers, no SMS). Preview left clean.
- SHIP: backend Deploy + `eas update`. NOTE: forest is super_admin w/o store so preview queue shows every store's sources; production reps see only sources they're on.

## Relationship Intel now reads texts + calls (June 2026) - DONE, verified e2e in preview (test_contact_intel.py 13/13)
- USER: "when updating the relationship intel on opening a contact, review the text conversation in the inbox and contextually update info just like a voice note or phone call". AUDIT FOUND: briefing queried messages by contact_id+created_at but inbox texts are stored by conversation_id+timestamp (Bud: 19 texts, briefing saw 1); new texts did not flag stale; personal_details (from voice notes) were not in the prompt; call transcripts not included; no extraction from texts/calls.
- FIX (backend only, routers/contact_intel.py + services/voice_intel.py): (1) `_message_match` = messages by contact_id OR any conversation of the contact, sorted by timestamp||created_at (aggregation), labeled "← Customer" / "→ You" / "→ Jessi (AI assistant)" / "· Call"; drafts + system events excluded. (2) Prompt adds KNOWN PERSONAL DETAILS (contact.personal_details + vehicle/occupation/employer/city) and RECORDED PHONE CALLS (call_logs transcript[:600] + ai_summary). (3) `_latest_activity` covers conv messages + call_logs; ignores event_type intelligence_extracted (AI's own event, else self-perpetuating stale). (4) `process_conversation_intelligence(user_id, contact_id)` in voice_intel.py: runs `extract_personal_details(text, source='text'|'call')` (EXTRACTION_PROMPT now has __SOURCE__ placeholder; SOURCE_DESCRIPTIONS) over NEW customer texts (>=15 chars) + new call transcripts since contacts.conv_intel_cursor.at, merges via merge_personal_details (newest wins), logs contact_event "Personal Details Updated / AI updated from texts: ...", advances cursor. Kicked off as asyncio task in generate_contact_intel in PARALLEL with the briefing (~10s total, briefing dominates); response gains `details_updated` list; data_points gains `calls`. Briefing output wrapped in no_em_dash(). No voice-note-style auto follow-up task from texts (avoids task spam).
- Verified: seeded inbound text on Bud -> GET stale:true -> POST regen: messages 20 (was 1), briefing cited job change + Bear Lake + brother Tom, personal_details.employer -> Fidelity, event logged, cursor set; GET stale:false; 2nd regen details_updated [] (no re-extraction). Test data restored.
- SHIP: backend Deploy only.

## Detail Change Alerts + Ask Before Overwrite + Queue Sound Alert (June 2026) - DONE, self-tested on preview
- **Ask Before Overwrite** (services/voice_intel.py): merge_personal_details(contact_id, details, confirm_conflicts, source) now returns {applied, suggested}. With confirm_conflicts (texts/calls path ONLY; voice notes keep "newest memo wins"), identity-like CONFIRM_FIELDS (spouse_name, occupation, employer, vehicle_purchased, vehicle_color, trade_in, neighborhood, pets, favorite_restaurant, communication_preference) that contradict a saved value are parked in contacts.detail_suggestions [{id, field, label, old, new, source, created_at}] instead of overwritten; _is_refinement (token subset, "2024 Tahoe" -> "2024 Chevrolet Tahoe") applies silently; list merge dedupes kids by name / strings case-insensitively; rejected pairs remembered in detail_suggestions_rejected. Top-level vehicle/occupation/employer only updated for APPLIED values. `resolve_detail_suggestion(user_id, contact_id, sid, accept)` applies or records rejection + contact_event detail_confirmed/detail_kept. Endpoint POST /api/contact-intel/{uid}/{cid}/suggestions/{sid} {action: accept|reject} (400 bad action, 404 unknown id) -> {suggestions: remaining}.
- **Detail Change Alerts**: contacts.conv_intel_last {at, source, fields, labels, values} written when texts/calls extraction applies fields. GET + POST /api/contact-intel return `last_learned` + `suggestions`. IntelBriefingCard.tsx: amber "JESSI NOTICED A CHANGE · from texts" rows (Spouse: ~~Sarah~~ -> Karen, [Update to Karen] [Keep Sarah]) + purple "Updated from texts: Spouse details, Vehicle, Pets +1 · 5m ago" chip (7-day window, tap expands field: value list). Props userId/contactId/onUpdate/onDetailsChanged wired in contact/[id].tsx (accept reloads contact). _latest_activity ignores intelligence_extracted/detail_confirmed/detail_kept events. data_points footer now "texts · calls · voice notes · events".
- **Queue Sound Alert**: push_notifications.send_push_to_user(..., sound="default", channel_id=None) -> Expo payload sound + channelId, data.sound; LEAD_SOUND="lead_chime.wav", LEAD_CHANNEL="leads" used by: lead_intake rep push + jump-ball blast, lead_queue reassign ("Lead handed to you") + release ("Up for grabs"), demo_requests admin "New Lead" pushes. Frontend: assets/sounds/lead_chime.wav (generated C6->G6 two-note chime, 0.95s), app.json expo-notifications plugin `sounds` (NATIVE BUILD REQUIRED for the lock-screen custom sound; iOS falls back to default sound until then), utils/leadChime.ts (playLeadChime via expo-av / web Audio, 4s debounce; noteLeadQueueCount chimes when unclaimed count grows; isLeadPush), _layout.tsx registers Android channel 'leads' (MAX importance, custom sound, vibration) + addNotificationReceivedListener plays chime in-app for lead pushes while open; LeadsWaitingStrip + inbox summary poll call noteLeadQueueCount. Chime audio itself not verifiable headless.
- Verified: seeded conflicting text on Bud -> POST regen: applied spouse_details/vehicle(refinement)/pets/notes, Employer Goldman->Fidelity parked as suggestion; accept -> personal_details+top-level employer updated, event logged, 404 on repeat, 400 bad action; UI screenshot: suggestion row + Keep -> row removed, learned chip expands. Bud restored. test_contact_intel 13/13, test_lead_queue_294 12/12 (with seed), ai_routing_matrix 28/28.
- SHIP: backend Deploy + `eas update` (chip/suggestions UI + in-app chime + Android channel). Lock-screen custom chime needs a new EAS native build + store submission.

## Contacts swipe past row ~9 + Thread header layout (June 2026) - DONE (header verified via screenshot; swipe fix is native-only, user to verify after eas update)
- ROOT CAUSE swipe: app/(tabs)/contacts.tsx FlatList had `removeClippedSubviews` on iOS (only list in the app using it). Rows rendered off-screen at mount get detached, and RNGH `Swipeable` loses its pan handler when they re-attach -> only the rows visible at first paint could swipe. Fix: removeClippedSubviews={false}, windowSize 7. Inbox never had the prop, which is why it worked.
- Thread header (app/thread/[id].tsx): row 1 = back · avatar · name (+ formatted phone, or Closed/Reopen) · more; Search / Call / Voice Memo moved to a pill strip underneath (styles.headerActions/headerActionPill/headerActionLabel, testids thread-search-btn / thread-call-btn / thread-mic-btn / thread-more-btn). Mic pill turns red "Stop" while recording.
- SHIP: eas update only (no backend change).
- OPEN QUESTION parked by user: voicemail -> auto follow-up task (AMD vs manual outcome chips); user will circle back.

## Voicemail Follow-Up + Lead Handoff Note (June 2026) - DONE (backend verified via scripts + webhook curl; UI screenshots for release sheet/handoff card)
- **Detection (NO AMD)**: Twilio `<Dial>` AMD is synchronous only (would delay/clip every answered call), so we use (1) `<Dial action=/api/webhooks/twilio/call-bridge-result>` -> DialCallStatus busy/no-answer/failed/canceled or completed<8s => immediate miss; completed => (2) the transcript we already make: services/call_followup.detect_outcome = VOICEMAIL_HINTS heuristic -> gpt-5.2 one-word classify -> duration fallback (>=60s connected). 240s fallback timer if no recording callback. pending_calls gains dial_status/dial_duration_s/customer_outcome/outcome_source/outcome_applied (idempotent claim). call_logs.outcome set; ai_summary prefixed "[Voicemail]"/"[No answer]"; appointment extraction skipped for voicemail. Task-you-called-from now completes ONLY on connected (was: any recording).
- **Cadence** (Forest): miss #1 -> +30 min (after 8 PM -> next business 10 AM); #2 -> +3h same day (after 7 PM -> next morning); #3 -> next business morning 10 AM + "just tried you" text tip; #4 -> +2 days 10 AM; #5 -> single final "{n} tries, no connection: text or park" task (auto_kind call_retry_final); then stop. Sundays skipped. Streak resets after 7 days idle or on any connected call (closes open call_retry tasks). State on contacts.call_retry {attempts,last_at,last_outcome,next_due,task_id}. Retry task reuse order: open call_retry task -> the task the rep called from (rescheduled + tagged auto_kind call_retry) -> new task (type call, source voicemail_followup, title "Try {First} again"). Push "{First} went to voicemail / didn't answer" -> /contact/{id}?taskId=; notification type call_retry_scheduled; contact_event call_voicemail|call_no_answer|call_busy.
- **Manual path**: POST /api/calls/{uid}/outcome {contact_id, outcome connected|voicemail|no_answer|busy, duration, conversation_id?, task_id?} (services.call_followup.record_manual_outcome). "/api/calls/" added to PROTECTED_PREFIXES (401 without token). call-screen.tsx native "Log Call" modal gained "How did it go?" chips (call-outcome-*) + cadence hint; Talked completes the task; misses schedule the retry and show "Follow-up set" alert.
- **Lead Handoff Note**: POST /leads/queue/{uid}/release/{conv} body {note} (<=200 chars) -> conversations.handoff_note {text,by,by_name,at,kind release|reassign}, system message "... · Note for the next rep: "..."", "Up for grabs" push + notification body = `Forest: "note"`. Reassign note also persisted as handoff_note. `_build_items` returns handoff_note; LeadsQueuePanel shows amber "↳ Forest: ..." block (lead-handoff-{id}); Release now opens a sheet (lead-release-sheet / lead-release-note / lead-release-confirm "Release with note|without note" / lead-release-cancel); reassign sheet has an optional note field (lead-reassign-note).
- Tests: /tmp scripts (engine cadence, idempotent webhook replay, manual endpoint 401/400/200), test_lead_queue_294 12/12, ai_routing_matrix 28/28. NOTE: engine test sent 2 real pushes to Forest ("Bud went to voicemail") - explained to user.
- SHIP: backend Deploy + eas update.

## Retry Cadence Settings (June 2026) - DONE (API + UI verified: save persists across reload, clamping works)
- users.call_retry_cadence {enabled, first_minutes 5-240, second_hours 1-8, morning_hour 7-12, fourth_days 1-7, evening_cutoff 16-22, max_auto 1-6}; services/call_followup.DEFAULT_CADENCE / CADENCE_LIMITS / normalize_cadence / cadence_for / preview_schedule; next_retry_due(attempt, now, tz, cadence) (attempt > max_auto -> next business morning). enabled=false -> misses are logged on the timeline but no task/push.
- GET/PUT /api/calls/{uid}/retry-cadence (PUT = full replace, clamped; returns preview list [{attempt, final, due, label}] in the rep's tz).
- UI: app/settings/call-retries.tsx (steppers cad-first/second/morning/fourth/cutoff/max, toggle call-retries-enabled, save call-retries-save, preview call-retries-preview, reset call-retries-reset); entry in More > Settings "Call Retries" (below My Schedule). Client-side preview uses phone time until saved (server preview after).
- SHIP: backend Deploy + eas update.

## Manager Cadence Defaults (June 2026) - DONE (API verified end-to-end; UI screenshot of team screen)
- Resolution order services/call_followup.resolve_cadence: personal (users.call_retry_cadence) -> store (stores.call_retry_cadence) -> org-wide (settings key call_retry_cadence_default; used by store-less super/org admins like Forest) -> built-in DEFAULT_CADENCE. Returns {cadence, source personal|store|global|default, store_cadence, global_cadence}.
- Endpoints (routers/calls.py): GET /api/calls/{uid}/retry-cadence now includes source/is_manager/has_store; DELETE drops the personal override ("Use team default"); GET/PUT /api/calls/{uid}/retry-cadence/store (managers only, 403 otherwise; scope store vs global; PUT body cadence + apply_to_all -> $unset every team rep's personal override; returns reps_total / reps_with_override / overrides_cleared).
- UI: app/settings/call-retries.tsx handles ?scope=store ("Team Call Retries": scope info card store-cadence-info, "Also reset those N reps" switch store-cadence-apply-all). Personal screen: source banner (cadence-source-banner) "You're using your store's/company default" or "These are your personal settings · Use team default" (cadence-use-team-default); managers see a "Team default" card (cadence-edit-store) linking to the store screen.
- SHIP: backend Deploy + eas update.

## Full-screen Modal safe-area fix (June 2026) - DONE (native-only; web smoke ok)
- ROOT CAUSE: react-native-safe-area-context `SafeAreaView` inside a full-screen RN `<Modal transparent={false}>` gets 0 top inset (modal renders in its own window outside the provider) -> header under the iPhone status bar, back chevron untappable. Fix pattern (already used in GalleryModal): `const insets = useSafeAreaInsets()` at screen level + `<View style={{ paddingTop: insets.top }}>` instead of SafeAreaView inside the Modal; back buttons got padding 8 + hitSlop; onRequestClose added (Android back).
- Fixed: home.tsx (Voice Note / action picker "pick a person", Send Card picker, ContactActionModal), contact/new.tsx referral picker, touchpoints/add-task.tsx contact picker, components/account/ProfileGallery.tsx viewer. Remaining SafeAreaView-in-Modal cases are all presentationStyle="pageSheet" (rendered below the status bar) -> fine.
- SHIP: eas update only.

## Swipe-to-dismiss pickers + "Just tried you" one-tap text (June 2026) - DONE (backend verified via scripts; UI screenshots)
- **Swipe to dismiss**: the 5 full-screen pickers (home Voice Note/action picker, Send Card, ContactActionModal, contact/new referral picker, add-task contact picker) now use presentationStyle="pageSheet" on iOS (native card sheet, swipe-down closes; onRequestClose + onDismiss keep state in sync) with a grabber bar; Android stays fullScreen with insets.top padding. ProfileGallery viewer left full-screen (close button).
- **Just tried you text**: services/call_followup.DEFAULT_JUST_TRIED; template resolution `just_tried_template(db, user_id, conversation)`: lead source workflow `just_tried_text` (WorkflowConfig + serialize + PUT, <=320 chars; UI card in admin/lead-sources/[id].tsx with {first_name}/{sender_name}/{company} chips, testid just-tried-text-input) -> cadence `just_tried_text` (personal/team/global via normalize_cadence; UI card in settings/call-retries.tsx testid just-tried-input) -> built-in. `send_just_tried_text(user_id, task_id)`: texting window = lead source text_window (or default 09:00-20:00) in the CUSTOMER's tz (customer_timezone by area code, store tz fallback); inside -> messages.send_message_simple (Twilio, logs message + event "Just tried you text sent", task.just_tried_sent_at); outside -> campaign_pending_sends doc (type direct_scheduled, campaign_name "Just tried you", send_at = opens_at, template pre-substituted) + task.just_tried_scheduled_for + event; opted-out contacts refused. Endpoints: POST /api/calls/{uid}/just-tried/{task_id} (400 send failure / 404 task), GET /api/calls/{uid}/just-tried-preview/{task_id}.
- UI one-tap: ContactTasksCard featured call_retry task -> "Just tried you" button (contact-task-just-tried-btn; becomes "Text sent"/"Text queued"); touchpoints task card Text slot -> "Just tried you" for call_retry tasks (task-just-tried-{id}).
- Tests: retry task on narrow-window source -> scheduled 9:00 AM Fri in customer tz, pending send + event written; personal + source template precedence verified; Twilio rejection surfaces as 400. test_lead_queue_294 12/12.
- SHIP: backend Deploy + eas update.

## Call Retries report + Just-tried follow-through (June 2026) - DONE (backend probe + routing matrix 28/28; UI screenshot with seeded data, seeds cleaned)
- **Call Retries card (Leads > Speed)**: `GET /api/leads/analytics/call-retries?store_id=&days=` (lead_intake.py, JWT via _require_auth; non-managers see only their own row, managers the store or everything when store-less). Counts per rep in window: misses (contact_events call_voicemail/no_answer/busy), retries (auto_kind call_retry tasks created/completed in window or still open), connected (completed_via call_connected), replied (completed_via customer_replied), open, gave_up (call_retry_final created), just_tried (just_tried_sent_at set); reach_rate = (connected+replied)/(closed retries + gave_up). Frontend components/leads/CallRetriesCard.tsx under the Speed leaderboard (also shows when speed data is empty); days chips gained "This Week" (7). Seed helper tests/seed_call_retries_demo.py seed|clean.
- **Just-tried follow-through**: services/call_followup.on_customer_replied(user_id, contact_id, conversation_id, body) - fired from twilio_webhooks incoming (fire-and-forget after task extraction, skipped for STOP words): when the customer texts while a call_retry/call_retry_final task is open -> tasks completed (completed_via customer_replied, completed_reply preview), queued "Just tried you" campaign_pending_sends (type direct_scheduled, task_id) cancelled (cancel_reason customer_replied), contacts.call_retry reset, contact_event call_retry_replied, notification type call_retry_replied (Alerts > Replies, links to thread) + push "{First} replied to your just-tried text" (or "texted back after your voicemail" when no just-tried was sent) -> /thread/{conv}. Idempotent (second text = no-op). Probe: tests/manual_retry_reply_probe.py.
- SHIP: backend Deploy + `eas update`.
- **Reply Timing Insight + Retry Coaching Tip (June 2026)**: call-retries endpoint now also returns `by_attempt` [{attempt, retries, connected, replied, just_tried}] (grouped by tasks.retry_attempt at close), `insight` {best_attempt (most texts back), replied_total, share_pct, median_reply_minutes (completed_at - just_tried_sent_at)}, `my_tip` (caller) and reps[].tip via _retry_coach_tip: with/without-text reach split when >=2 closed each ("doubles"/"lifts your reach rate: X% without, Y% with"), else rep's own best attempt, else "no just-tried texts this period", else team best attempt, else generic. CallRetriesCard: "TEXTS BACK BY MISS #" mini bars (best gold) + sentence, gold "YOUR TIP" box, italic tip under each rep row. Verified via curl + screenshot with seeded attempts; seeds cleaned.

## Contact page order + typography QA (June 2026) - DONE (screenshots: contact Details tab + Hub at 390px)
- **P0 fix**: ContactTasksCard had `useState(sendingJT)` declared after an early `return null` (added with the Just-tried button) -> "Rendered more hooks than during the previous render" crashed Contact Detail whenever the card toggled between empty/non-empty. Hook moved above the early return. RULE: never declare hooks after a conditional return.
- **Order**: DetailsTab now renders Personal Intelligence ABOVE Relationship Voice Memos (header -> PersonalIntelSection -> voice memos -> purchases -> notes -> dates -> referrals -> campaigns).
- **Type scale** `constants/typography.ts` (FS: display 28, title 20, nav 17, heading 16, body 15, secondary 13, caption/eyebrow 12, micro 11; EYEBROW = 12/700/ls 0.8/uppercase). Applied across contactStyles.ts (all ~110 text styles), contact components (IntelBriefingCard, ContactTasksCard, PurchaseHistorySection, FeedTab, CallsTab, PersonalIntelSection) and the Hub (more.tsx: section titles + menu rows 16, subtitles 13, eyebrows 12, tiles 15/12, profile name 20, legal/version 13). Rule for new UI: pick sizes only from FS; uppercase section labels always use EYEBROW. Other tabs (Home, Inbox, Contacts) not yet swept.
- **Type-scale sweep of the 4 main tabs (June 2026)**: Home, Inbox, Contacts, Activity (+ components/home, inbox/LeadsQueuePanel, contacts/*) now on the same FS scale: tab headers all 28/800 (Home was 22, Contacts 24), Home section titles 17/700 ("Your 3 for Today" was 22), row titles 16, body 15, secondary/subtitles 13, eyebrows 12/700/ls 0.8, badges/pills 11 (tiny tags 10 min). Inbox row: name 16, preview 15, timestamp 13. Hub tile subtitles shortened ("Mass text", "Text your card", "Send review link") + numberOfLines=1 so they never wrap. Verified via screenshots at 390px. NOTE (pre-existing, web only): /inbox URL is shadowed by public/inbox/index.html marketing page; use the tab bar on web.

## Auto Just-Tried + detail screens sweep + tab order + Hub Leads section (June 2026) - DONE
- **Auto "just tried you" text**: cadence keys `auto_just_tried` (bool, default off) + `auto_just_tried_from` (1..6, default 1) in DEFAULT_CADENCE/normalize_cadence (personal, store and global cadence all support them). `_on_missed` calls `send_just_tried_text(user_id, task_id, auto=True)` once per miss streak (only if the retry task has no just_tried_sent_at/scheduled_for) for voicemail/no_answer when attempt >= from; push body gets " · text sent" / " · text queued for 9:00 AM" / " · auto-text skipped" (opted out, no phone, send failure). Task gets `just_tried_auto`. Events titled "auto-sent"/"auto-scheduled". UI: Settings > Call Retries "Send it automatically" switch + Miss #1/#2/#3 chips (testids auto-just-tried-toggle, auto-just-tried-from-N). Probe: tests/manual_auto_just_tried_probe.py (5 cases pass).
- **Type scale sweep of detail screens**: scripts/normalize_type_scale.py (heuristic normalizer; run on files, then review 19+ sizes) applied to app/touchpoints/*, app/thread/[id].tsx, components/thread/*, AISuggestion, all app/settings/*. All settings/touchpoints custom header titles are 17; card previews (create-card previewHeadline 32 / previewName 24, card-templates previewHL 22, brand-kit previewCompany 22) intentionally kept as card design sizes.
- **Tab order**: Home, Contacts, Inbox, Activity, Hub (Inbox moved to center slot).
- **Hub reorganized per design agent (/app/design_guidelines.json)**: new first section **Leads** (flame, red, expanded by default, "N waiting" red pill from GET /leads/queue/{uid}/summary): Internet Leads (badge), Call Retries, and for admins with contact_tags/users perms: Lead Source Queue, Lead Source Config, Team Availability. Removed those from Manage / Settings / Set Up. Tile grid: Internet Leads (with waiting badge) replaces Find a Word (still in Manage > Keyword Search). Settings "Calendar" renamed "Calendar Sync" (integration) to stop confusion with Manage > Calendar. Super-admin items renamed "iMOS Website Leads" / "iMOS Hot Leads". Em dashes removed from touched Hub copy. RULE: user forbids em dashes anywhere (responses and app copy).

## Stop-the-clock timers + Proof tab (June 2026) - DONE (curl + screenshots with seeded leads, seeds cleaned)
- `services/lead_clocks.py`: per lead (inbound_leads with conversation_id): first_call_at (calls/call_logs outbound by contact + lead_call_jobs press_1 bridge), first_human_text_at (sender user, not auto_sent), first_ai_text_at (sender ai), first_outbound_at, first_customer_reply_at (first inbound after first outbound, only when a text was sent), outbound_texts, inbound_texts, calls. `summarize_clocks` -> clocks {call, human_text, ai_text: measured/avg/median/fastest} + customer {texted, replied, reply_rate, avg/median reply secs}.
- `GET /api/leads/analytics/response-times` now returns `clocks`, `customer`, and per rep call_avg_seconds, reply_rate, replied, texted, reply_avg_seconds, leads (old fields kept). Speed tab: StopTheClockCard (3 clocks + customer reply card) replaces the old 3 tiles; rep rows show call avg, % replied, reply time.
- Thread lead banner (LeadCallTimeline): expanded view shows CALL / HUMAN TEXT / AI REPLY / THEY REPLIED clocks (timeline endpoint returns `clocks`); durations now "2h 40m" style instead of 160:52.
- `GET /api/leads/analytics/proof?store_id=&days=` (default 365, excludes is_test): reply vs silent close rates + lift, buckets by first-touch speed, first-human-text speed, touchpoints (texts+calls), reply depth; time_to_sold (contacts.date_sold minus received_at, days); per source: leads, sold, close_rate, reply_rate, first_touch_avg_seconds, avg_touches, avg_days_to_sold, monthly_cost prorated period_cost, cost_per_lead, cost_per_sale; headlines sentences; HBR benchmark line. Leads screen 4th tab **Proof** (ProofPanel) with Copy button (clipboard) for pitching. Seed helper tests/seed_lead_proof_demo.py seed|clean.
- **Proof share image / source cost prompt / Monday proof push (June 2026)**: `GET /api/leads/analytics/proof-card.png?store_id&days` renders a 1080x1350 branded PNG (services/proof_card.py, Pillow, Liberation fonts: store name, close rate tiles, headlines, footer). ProofPanel "Share image" downloads via api (arraybuffer) -> expo-file-system File in Paths.cache -> expo-sharing (web: blob download). `compute_proof()` is the reusable function; proof sources now include source_id and response has `unpriced_sources` (leads but no monthly_cost). ProofPanel shows an OPTIONAL "what do these sources cost per month?" card for managers with inline $ inputs (PATCH /lead-sources/{id} monthly_cost) and Skip (AsyncStorage proof_cost_prompt_dismissed_until, 30 days). Scheduler `send_weekly_proof_push` Mon 14:50 UTC: managers with a store_id get "Last week's lead proof" push (leads/sold/close, under-5-min vs slower close, best cost per sale) linking /leads?tab=proof (leads.tsx reads ?tab=). Opt-out: notification_settings.weekly_proof_push=false via Settings > Notification Preferences > Manager digests toggle (default on, skipped on weeks with 0 leads, logged in weekly_proof_push_log).
- **Proof card styles / prospect link / rep rows (June 2026)**: proof-card.png and public card take `theme=dark|light` + `format=portrait|square` (1080x1350 or 1080x1080; services/proof_card.THEMES). ProofPanel "Share image" opens a 4-chip picker (Dark 4:5, Light 4:5, Dark 1:1, Light 1:1). compute_proof returns `reps` (per rep: leads, sold, close_rate, replied{leads,sold,close_rate}, silent{...}, reply_rate, first_text_avg_seconds) shown as "REPS: SPEED PAYS". Prospect link: `GET/POST /api/leads/analytics/proof-link?store_id=` (managers; token on stores.proof_share {token, enabled, views, last_viewed_at}; body {enabled, rotate}); public `GET /api/public/proof/{token}?days=30|90|365` (+ `/card.png`) via routers/public_proof.py (no auth, 404 when disabled, increments views, strips source_id/user_id/unpriced). Public page app/proof/[token].tsx (dark theme, days chips, ProofPanel publicToken mode, CTA to imonsocial.com); 'proof' added to publicRoutes in _layout. URL base from PUBLIC_FACING_URL/APP_URL. Prospect link card in Proof tab needs user.store_id (message otherwise). COLORS now exported from store/themeStore.

- **Ship checkpoint (June 2026)**: User verified the Proof batch was ready and asked to finish for GitHub save + Deploy. Pre-ship smoke on preview: /api/health 200, /api/public/proof/{bad} 404, /api/leads/analytics/proof 200, proof-card.png?theme=light&format=square 200 image/png, supervisor backend+frontend RUNNING, no backend tracebacks. SHIP: backend Deploy (public_proof router, proof-link, proof-card themes, weekly proof push, auto just-tried) + `eas update --branch production` (Hub Leads section, tab order, typography sweep, ProofPanel picker, /proof/[token] page). Open items unchanged: push notifications prod RCA (needs user's diagnostic result), Wallet certs (parked), HomeNet/vAuto creds, Zapier screen (P2).

## Proof: per-source spend editor + returning-customer clock fix (June 2026) - DONE (probe test 9/9, UI round-trip verified)
- USER (prod screenshot): could not edit cost per source (prompt vanished once priced; Lead Source Config field hidden behind Edit) and saw "first touch -6792999s" + "67.7 touches avg".
- ROOT CAUSE of the bad numbers: services/lead_clocks.clocks_for_leads matched messages by conversation_id with NO time floor, so a returning customer routed into an existing thread counted months of old texts/calls (negative first-touch, inflated touch counts). FIX: events (messages, calls, call_logs, press-1 bridges) are now filtered PER LEAD to >= received_at - 5 min in Python (mixed str/datetime timestamps make a Mongo-side floor unsafe). compute_proof also drops negative touch deltas. Test: tests/test_clock_returning_customer.py (seeds + cleans; 9/9 PASS).
- UI (ProofPanel): every source row now has a gold pill for managers: "Add spend" (no cost) or "$1,500/mo" (pencil) -> inline SpendEditor ($ input "/mo", Save, live math line "$X over N days ÷ leads = $/lead · ÷ sold = $/sale"); PATCH /lead-sources/{id} monthly_cost (0 clears). Old SourceCostPrompt (Skip/AsyncStorage) REMOVED - single way to price a source. Footnote rewritten. testids spend-edit-{id}, spend-input-{id}, spend-save-{id}, spend-math-{id}, spend-editor-{id}. Sources with 0 leads in the window are not listed (price them when they start producing).
- SHIP: backend Deploy (clock fix) + eas update (spend editor).

## Connect Zapier / Make screen (June 2026) - DONE (e2e verified: test lead turned the status green in <5s)
- Hub > Leads > "Connect Zapier / Make" (admins with contact_tags perm) -> /admin/lead-connect. 4 steps: pick source (chips from /lead-sources?store_id=, "+ New" -> Lead Source Config), copy Webhook URL (universal /api/leads/webhook/{source_id}, POST, JSON) + X-API-Key (masked, show/hide, copy), field map (sample JSON copy + accepted names/aliases), live "Listening for your test..." card that polls GET /api/leads/connect/{source_id} every 5s and turns green ("It landed · name · ***last4 · vehicle · Xs ago · test lead") when a new inbound_leads doc appears; "Open the latest lead's thread" -> /thread/{conversation_id}. Collapsible guides: Zapier, Make, website form/Facebook/ADF.
- Backend: GET /api/leads/connect/{source_id} (manager auth via _caller_store; 403 for another store's source; super_admin ok). Universal webhook now also accepts ?api_key= query param (Zapier/Make users without header support) and honours is_test / test = true|1|yes in the payload (marks Test Lead, excluded from Proof). testids: connect-step-N, connect-source-{id}, connect-copy-url, connect-copy-key, connect-toggle-key, connect-copy-sample, connect-test-status, connect-open-thread, connect-guide-zapier|make|other.
- NOTE: webhook_url uses PUBLIC_FACING_URL (app.imonsocial.com), so on preview you must post to the preview host manually.

## Metro stale-bundle ROOT CAUSE FIXED (June 2026)
- frontend/watch-patch.js (loaded via NODE_OPTIONS in supervisor) replaced fs.watch with polling on the watched path's OWN mtime. A directory's mtime only changes on add/remove, so EDITS to existing files were never seen -> stale bundles / lint loops (15+ recurrences). Patch now snapshots directory entries (name -> mtime) for dirs under /app/frontend outside node_modules/.git/.metro-cache/.expo and emits change/rename per file every 2s. Verified: sed edit to lead-connect.tsx appeared in the served bundle within 8s without restart. New route files still need one `supervisorctl restart frontend` (expo-router route map), edits do not.

## Facebook Lead Ads recipe on Connect screen (June 2026) - DONE (screen verified, FB payload attribution verified)
- components/leads/FacebookRecipe.tsx rendered between step 1 and step 2 on /admin/lead-connect: detects a Facebook/Meta/Instagram-named source ("Use it" selects it) or offers "Create a Facebook Lead Ads source first" -> /admin/lead-sources/new?name=...&description=... (new.tsx now prefills from params). Blue "Open the Zapier template" button opens Zapier template 10288 (POST new Facebook Lead Ads to a webhook, verified live on zapier.com). 5 steps + copy URL + Data-rows table (phone, full_name, email, comments, form_name, campaign_name, ad_name, platform).
- Backend: universal webhook builds attribution when campaign_name/adset_name/ad_name/form_name/platform are present: {kind: ad, source: meta_lead_ad, page: form, source_label: "Facebook ad: <campaign>" (Instagram when platform ig), campaign, adset, ad, form}. Lead Source Queue already shows attribution.source_label, call-ladder whisper uses it.
- testids: connect-fb-recipe, connect-fb-use-source, connect-fb-create-source, connect-fb-open-zapier, connect-fb-copy-url, connect-fb-row-{key}.

## Source Health Alerts (June 2026) - DONE (tests/test_source_health.py 10/10, UI + toggle verified)
- services/source_health.py: source_health(db, store_id) classifies every active source: quiet / slow (<4 leads in 28d) / healthy / new. Watched sources (4+ leads/28d) are quiet when hours since last lead >= max(48h, 3x usual gap). Sources with 0 in 28d but 4+ lifetime are quiet after 28 days. run_source_health_alerts() (scheduler every 6h, id source_health_alerts) pushes store managers (roles manager/store_manager/org_admin/admin/super_admin, opt-out notification_settings.source_health_alerts) "X has gone quiet · No leads in N. It usually gets one every G. Check the Zap or the vendor feed", url /admin/lead-connect, icon pulse. Dedupe: once per quiet streak + one reminder after 7 days (lead_sources.health_alert_sent_at/health_status, source_health_alert_log).
- GET /api/leads/sources/health (manager, store scoped) -> {sources, rule, quiet_count}. UI: components/leads/SourceHealthCard.tsx at top of /admin/lead-connect (quiet rows red-bordered, never-produced sources collapsed to one line, Refresh); tapping a row selects that source. Settings > Notification Preferences > Manager digests: "Quiet source alerts" toggle (testid source-health-alerts-toggle).

## Ad Level Proof (June 2026) - DONE (tests/test_ad_level_proof.py 11/11, UI round trip verified)
- compute_proof now attaches inbound_leads.attribution to clock rows and returns "campaigns": one row per attribution.campaign (Facebook/Instagram via Connect): leads, sold, close_rate, reply_rate, first touch, days to sold, ads[] (top 5 by sold then leads, ad = attribution.ad or form), ad_count, cost. Cost: campaign_costs {store_id, campaign_key (lower), campaign, monthly_cost} when set (cost_mode "set"), else the source's period cost allocated by lead share (cost_mode "estimated"), else None. Headline "Ad that sells: X closed a of b (c%) at $ per sale (est.), while Y sent n leads and sold none". Shared group_stats() helper now feeds both sources and campaigns; cost_by_name is store scoped (was cross-store). Public proof strips campaigns + that headline.
- PUT /api/leads/campaign-costs {campaign, monthly_cost, store_id?} (manager; 0 deletes). UI: ProofPanel "WHICH AD ACTUALLY SELLS" card (managers only, hidden on public page) with CampaignRow: "$X / sale est." badge, "Ad spend" / "$1,200/mo" pill -> shared SpendEditor (generalised with onSave), chips, "Best ad: ... · a of b sold · <worst ad> sold 0 of n". testids proof-campaigns, proof-campaign-{key}, campaign-spend-edit-{key}, proof-campaign-best-{key}, spend-input-{key}, spend-save-{key}.

## Spend math fix: only months actually paying count (June 2026) - DONE (tests 7/7 + 12/12, UI verified)
- USER (prod): $1,000/mo Website, 2 sold, 90-day view showed $3,000 spent / $1,500 per sale. Cause: monthly cost was prorated across the whole window even though the source only started paying this month.
- FIX compute_proof.months_charged(started): full window (days/30) when the source was paying before the window; otherwise min(days/30, max(1, ceil(overlap_days/30))) -> $1,000/mo started this month = $1,000 in 30/90/365-day views, 7-day view still prorates ($233). started = lead_sources.spend_started_at (ISO, user override via PATCH /lead-sources) else the source's first-ever real lead. Same for campaigns (campaign_costs.spend_started_at via PUT /leads/campaign-costs, default first lead of that campaign). Rows now return months_charged + spend_started_at.
- UI: SpendEditor has PAYING SINCE chips: "Since first lead (Aug 27)" (default when no override) / This month / Last month / 2 months ago / Longer (13 months back); math preview uses the same formula. Source and campaign rows show the SpendMath line "$1,000/mo x 1 mo paying in this window = $1,000 ÷ 2 sold = $500 / sale · paying since Sep 2026". Footnote rewritten with the $1,000 example.
- Tests: tests/test_spend_months_charged.py, tests/test_ad_level_proof.py (expectations updated for 1-month charge).

## Dialer keypad enlarged (June 2026) - DONE (375x812 web check: 84pt buttons, keypad fixed while typing, search view unaffected)
- app/(tabs)/dialer.tsx: button size now measured from the real keypad column height (onLayout -> btnSize(padH)) instead of a guessed CHROME_H constant; cap raised 80 -> 84, width cap (PAD_SIDE-64-28)/3, floor 52. Reserved column: status line 24 + number 52 + match strip 44+6 + paddings; ROW_GAP 12. Digits/icons scale with the button.
- Contact matches while dialing became a single fixed-height horizontal chip strip (up to 4 chips + "N more" which drops the digits into the search box to show the full list) instead of a 90pt two-row list, which is where the extra height came from.
- Also fixed a pre-existing hooks-order violation: the "Access Pending" early return sat above useMemo/useEffect; moved below all hooks.
- NOTE: dialer.tsx uses data-testid props (not dataSet), so they do not reach the web DOM; use text selectors when screenshot-testing it.

## Hub -> "Apps" iPhone-style home screen (June 2026) - DONE (web verified: folders open, jiggle edit mode, drag reorder, move sheet, persistence)
- Tab renamed Hub -> Apps (icon apps). app/(tabs)/more.tsx keeps ALL section-building/role-gating logic and the search bar, profile card, rep preview, legal footer; the task grid + pinned chips + collapsible section lists + Admin block were replaced by <AppHome/>.
- Catalog: every section item -> HubApp {id: slug(title), folder: section id} with FOLDER_FOR remap (account_mgmt+internal_ops -> "admin", setup_manage -> "setup"); My Brand _brandItems included; the 6 task-grid quick actions become loose apps by default (hubDefaultLoose).
- components/hub/: layout.ts (HubLayout {v:1, home: ["f:<folder>"|"a:<app>"], folders:{id:{title,items}}}, reconcile() drops hidden apps / adds new apps to their default folder / removes empty folders; loadLayout (AsyncStorage hub_layout_v1:<uid> vs users.hub_layout by updated_at), saveLayout (debounced 400ms, PATCH /users/{id} {hub_layout}), clearLayout). Tiles.tsx (AppTile glassy dark tinted tile, FolderTile 3x3 mini icons, Badge, Jiggle via reanimated withRepeat rotate). SortableGrid.tsx (absolute slots + LinearTransition; RNGH Pan enabled only in edit mode: onBegin lifts a ghost overlay, onUpdate computes target slot -> onReorder, onFinalize springs to slot; Pressable handles tap / long-press-to-edit; onDragging disables the outer ScrollView). FolderModal.tsx (blur backdrop, ZoomIn card, Edit/Done, rename input, inner SortableGrid, tap-in-edit -> MoveSheet). MoveSheet.tsx (Home / any folder / New folder). AppHome.tsx orchestrates (ARRANGE header with Reset + Done, folder rename/remove sheet).
- Backend: server.py PATCH /api/users/{id} allows hub_layout.
- **Apps Phase 2 gestures (June 2026) - DONE, verified on web preview at 390px**: (1) Hold an app over another app (dwell 600ms, centre zone = 32% of tile) -> new folder (title from the target's default section if that folder does not exist yet, else "New Folder", auto-suffixed -2); over a folder -> files it (folder tile goes gold + scales while hovered). Lingering off-centre shifts tiles out of the way like iOS (SETTLE_MS 170 then decide). (2) Drag out of a folder: FolderModal grid passes onDragOutside (outsideMargin 48px past the grid); AppHome.dragOut moves the app home, closes the folder, gold Landing glow on the tile (~1.7s); empty folders are deleted, one-app folders remain. (3) RECENT row (device-local AsyncStorage hub_recent:<uid>, last 3 launched incl. launches from inside folders, hidden while arranging). Verified: app-on-app folder, app-onto-folder, drag-out, recent row, Reset, layout persisted after reload. Frontend-only -> `eas update`.
- **Folder badge tap + Recent sync (June 2026) - DONE, verified on preview**: (1) Red badge on a folder tile is now a Pressable (Tiles.tsx Badge onPress, testid tile-badge-btn, hitSlop 10, stopPropagation so the folder does not open; disabled while arranging/dragging). AppHome.badgeTap: exactly one badged app in the folder -> launch it directly (counts as recent); several -> open the folder. Verified: Leads folder badge -> /leads directly. (2) Recent row synced to the account: layout.ts RecentEntry {id, at ISO}, mergeRecent (union, newest per id, max 6 stored / 3 shown), local cache AsyncStorage hub_recent_v2:<uid>, remote via GET /api/users/{id}.hub_recent on focus (merge + cache) and debounced PATCH /api/users/{id} {hub_recent} on launch. Backend server.py PATCH allows hub_recent with shape validation (list of {id:str, at:str}, max 12 -> 400 otherwise). Verified: fresh browser (empty localStorage) shows the account's recent row; bad payloads 400. SHIP: backend Deploy + `eas update`.
- **No-bounce motion + back-into-folder (June 2026, USER FEEDBACK) - DONE, verified on web**: All springs removed from the Apps grid (SortableGrid lift/drop/reflow -> withTiming / LinearTransition.duration(220) with Easing.out(cubic); Landing glow no longer scales; FolderModal open = ZoomIn.duration(180) eased, no springify). Arrange-mode wiggle (Jiggle) kept. Back navigation: AppHome.open() stores the folder id in returnFolder ref before launching; useFocusEffect reopens that folder (restore prop = quick 80ms fade instead of zoom) when the Apps tab regains focus; apps launched from the home grid / recent row / badge do not reopen anything. Verified: Leads folder -> Internet Leads -> back lands inside Leads; home app round trip stays closed.
- **Tab renamed Apps -> Tools + Folder pages (June 2026) - DONE, verified on web**: (1) User picked "Tools" over Apps/Hub/Settings/More: (tabs)/_layout title 'Tools' + icon 'grid'; AppHome header TOOLS; all hint copy says "tool" (AppHome, FolderModal, MoveSheet counts, remove-folder note). (2) FolderModal REWRITTEN with iPhone-style pages: PAGE_SIZE 8 (4x2), horizontal pagingEnabled ScrollView (testid folder-pager, pages folder-page-N, grids folder-grid / folder-grid-N), gold dot indicator (folder-dot-N tappable), scroll disabled while arranging. Per-page SortableGrid; reorderPage() splices the page back into the full id list. Cross-page move in arrange mode = "edge hop": SortableGrid new props onEdgeHold(key, dir) fired after the tile hangs >=24px off the left/right grid edge for 450ms (cancelDrag ends the drag, no drop anim), outsideAxis 'vertical' on paged folders so left/right pulls never trigger drag-out (up/down still does). hop(): app becomes first tool of next page / last of previous (displaced tool swaps pages, like iOS), pager scrolls, Landing glow. initialPage prop: AppHome returnFolder now {id, page} so back lands on the same page. Verified on Admin (4 pages): dots nav, hop right (Account Health -> page 2 slot 1, Activity Feed swapped back), open on page 2 -> back reopens page 2.
- **Folder tile mini grid anchored top-left (June 2026, USER-REPORTED) - DONE**: Tiles.tsx FolderTile inner wrap now has fixed height mini*3+6 and alignContent 'flex-start', so 1-2 tools sit in the top-left corner like iOS instead of floating centred. Verified on preview (Campaigns 2, Learning 1).
- **Tools tab header condensed (June 2026, USER REQUEST "condense the space at the top, put search at the top") - DONE, verified**: more.tsx: removed the tall cover-photo profile card + two big My Profile / My VA buttons + full-width View as Rep row. New order: search bar with the notification bell beside it (hubTopRow / hubBell), then a compact profile strip (44px avatar, name, gold title or email, small Profile + My VA pills; testids profile-card, edit-profile-hint-btn, virtual-assistant-btn kept), then a tiny right-aligned "View as Rep" link (admins), then TOOLS. Grid now starts ~2.5x higher on screen. Removed unused LinearGradient / resolvePhotoUrl imports. Frontend only -> `eas update`.
- **Multi-photo replies + delivery receipts (June 2026) - DONE, verified**: Inventory now has a photo gallery per vehicle (`photos: [{full_path, thumb_url}]`, MAX_PHOTOS 6, legacy single cover folded in by _gallery(); cover = photos[0] mirrored to photo_url/photo_full_path). Endpoints: POST /inventory/{u}/{item}/photo appends (400 at 6), DELETE /inventory/{u}/{item}/photo/{index}, PUT /inventory/{u}/{item}/photo/{index}/cover. GET list returns photos. Frontend: components/inventory/PhotoGallerySheet.tsx (grid, COVER / SENT #2 / SENT #3 / EXTRA labels, tap = make cover, X = delete, Add tile), inventory.tsx tile shows a photo-count badge; first tap with no photos still adds directly. Also fixed a cold-start race: inventory.tsx redirected to login before the session hydrated (now waits for authStore.isLoading). Jessi: ai_reply._search_inventory_context -> _photos(it) (gallery, legacy cover, then feed images/primary_image; all /api/images URLs get ?format=jpeg); "specific" when exactly one candidate is named in the message (name/model/trim/stock #) -> up to 3 photos of that vehicle; comparisons / broad asks -> one cover per vehicle, up to 3; falls back to one-per-vehicle when the named car has no photos. Verified: "Do you still have the Tacoma TRD?" -> 3 photos; "Tacoma or the F-150?" -> 1 cover each with photos. Delivery receipts: MessageBubble shows green "Delivered" (checkmark-done) when status == 'delivered' (set by /webhooks/twilio/status, which now fires because send_sms passes status_callback); thread poll now re-renders on status changes, not just new messages. Verified with a simulated delivered callback. SHIP: backend Deploy + `eas update`.
- **Jessi MMS never delivered (June 2026, USER-REPORTED on PRODUCTION: Gladiator photo reply showed in the app but never reached the phone) - ROOT CAUSE + FIX, verified on preview**: Not quiet hours. Inventory photos are stored as WebP (image_storage.upload_image -> {id}.webp, served image/webp at /api/images/...). Twilio MMS only accepts jpeg/png/gif (+heic/tiff/bmp); WebP -> error 12300 Invalid Content-Type, message fails at the carrier AFTER the API accepted it. ai_reply._search_inventory_context built the media URL without ?format=jpeg (every other MMS path already used it), and process_ai_reply_queue marked the item SENT + wrote the message as "sent" without checking result.success, and send_sms never passed status_callback nor returned "sid" (ai_reply read result["sid"] -> None, so status callbacks could not even match AI messages). Fixes: (1) twilio_service.mms_safe_media() appends ?format=jpeg to every /api/images/ URL lacking format= (central safety net for ALL MMS), send_sms passes status_callback=<PUBLIC_FACING_URL>/api/webhooks/twilio/status and returns both message_sid and sid. (2) ai_reply inventory URLs explicitly ?format=jpeg. (3) process_ai_reply_queue: on send failure with media -> retry text-only (media_dropped flag); still failing -> queue FAILED, message stored status "failed" + error_message, push to rep via _notify_send_failure ("Jessi's text to X did not send"). (4) twilio_webhooks /status: on failed/undelivered -> _handle_failed_outbound: if has_media and ErrorCode in MEDIA_ERROR_CODES (11200,12300,12400,21620,21623,30008) resend text-only from the same number (new message retry_of=<sid>, media_dropped=True) and push the rep; any other failure -> push "did not deliver" (failure_handled flag prevents repeats). (5) messages API returns error_message + media_dropped; MessageBubble shows red "Not delivered · reason" and orange "Sent without the photo (carrier rejected it)" badges. Verified: /api/images/<inv>.webp -> image/webp, ?format=jpeg -> image/jpeg; fake 12300 status callback -> text-only resend SM9f20..., original marked failed; thread shows both badges. SHIP: backend Deploy + `eas update`.
- **Thank Advocates one-step flow (June 2026) - DONE, verified**: Backend relationship_health.py: GET /advocates/{contact_id}/thank returns prefilled text + card drafts (rep first name, contact first name) and via_twilio flag; POST /advocates/{contact_id}/thank {mode: text|card, message?} -> card mode builds a thankyou card via congrats_cards.auto_create_card (custom_message = the note) and appends the short link; sends from the rep's Twilio/mvpline number (502 on Twilio failure), writes the conversation message, logs thank_you_text_sent / thankyou_card_sent (new EVENT_META + label), sets contacts.last_thanked_at, busts the book cache; no rep number -> via 'native' so the app opens Messages prefilled. Rows now carry first_name + thanked_days; /advocates returns thanked_30d. Frontend: components/advocates/ThankSheet.tsx bottom sheet (text | card toggle, editable draft, Send), advocates.tsx CTA opens it, row shows "Thanked today" / "Thank again", header "N thanked this month", toast on success. Verified on preview: text + card sends via Twilio to the test advocate, both events on Activity with previews, UI state flips to Thanked today.
- **Book of Business: Advocate is a flag, not a bucket (June 2026, USER-REPORTED "7 advocates at top but 1 below") - DONE, verified**: Root cause: buckets are exclusive by priority, so an advocate 90+ days silent sat in At Risk; the tile counted warm advocates only (1) while the banner counted all (7). Fix (user chose option A): relationship_health.py BUCKET_META now 4 buckets (Opportunity / At Risk / Cooling / Connected, always sum to total); ADVOCATE_META separate; `_score` keeps is_advocate and prefixes reason "Advocate · 186 days silent"; summary returns advocate_meta; contacts?bucket=advocate kept as a legacy alias returning the advocates list (stale app builds); health_one returns advocate_meta. Frontend: book-of-business rows show a blue heart + blue reason for advocates in any group; the blue banner stays the one advocate entry point (-> /advocates); HealthBadge on the contact page shows a heart next to the health label; home BookOfBusinessCard shows "N advocates across every group - worth a thank you". Verified on preview: 4 tiles sum 62, banner 5, 2 advocate hearts inside At Risk. SHIP: backend Deploy + `eas update`.
- **Activity feed logs everything (June 2026, USER REQUEST "make sure everything is listed like new lead, text and calls") - DONE, verified**: Audit found the whole internet-lead pipeline wrote nothing to contact_events (the only source of the Activity tab). New central helper `utils/activity_log.py` (log_activity idempotent on `ref` = Twilio SID, MOCK_ refs ignored; on_lead_claimed moves store/org-owned rows for that contact to the claiming rep + logs lead_claimed; owner_for_contact / contact_for_conversation helpers; backfill_activity + run_backfill_once). Wired: lead_intake.process_inbound_lead -> new_lead / returning_lead (owner = assigned rep else store); _send_intake_sms + AI first message (process_queued_leads) -> auto_text_sent; messages /twilio-send -> event_type passed by app (personal_sms / mms_sent / vcf_sent / digital_card_sent...); /send-mms -> mms_sent; ai_reply approve -> ai_reply_sent; lead_call_engine._run_attempt -> lead_call_attempt on each rung rep's feed; lead_call_webhooks claim (press 1) -> lead_call_connected; claims (lead_sources claim + claim-and-call, lead_call_engine phone claim, chat_widget claim) -> on_lead_claimed; lead_queue reassign -> lead_reassigned, release -> lead_released. POST /contacts/{u}/{c}/events now folds a duplicate posted by older app screens into the auto-logged row (same contact+type within 90s with a ref) so quick-send flows do not double log. event_types.py: new labels + EVENT_META (icon/color/category). Master feed: description falls back to content_preview; new lead types classified as milestones. Startup migration `activity_backfill_v1` (db.migrations) backfills last 90 days: inbound_leads -> new_lead/returning_lead, messages -> sms_sent/ai_reply_sent/customer_reply/outbound_call (skips when any non-view event exists within ±120s), calls/call_logs -> outbound_call/inbound_call. Preview run: leads 5, texts 10, calls 0; second run all zeros. Frontend activity.tsx: milestone styling for new_lead/returning_lead/lead_claimed/lead_reassigned, one-line description preview under text/call/task rows and single cards. Verified end to end on preview: webhook lead -> claim -> twilio-send -> intake text -> release -> reassign all appear on Forest's Activity with previews. SHIP: backend Deploy (backfill runs itself once on prod startup) + `eas update`.
- Not built (Phase 2 candidates): true drag-out of a folder onto the grid, drop-one-icon-onto-another to create a folder, multiple pages. Moving between folders is via the Move sheet (tap while editing).
- testids: app-home, home-grid, folder-grid, grid-cell-<key>, app-tile-<id>, folder-tile-<title>, tile-badge, apps-done, apps-reset, folder-modal, folder-edit-toggle, folder-close, folder-rename-input, move-sheet, move-home, move-folder-<id>, move-new-folder(+-input/-create), folder-rename-sheet, folder-dissolve, folder-rename-save.
- **Smarter Vehicle Search for Jessi (June 2026) - DONE (ai_routing_matrix 33/33 incl. 5 new shopping cases; live LLM answers verified)**: new `services/vehicle_search.py` (pure functions): parse_query() pulls body types (trucks/SUVs/sedans/vans/coupes/convertibles/hatchbacks/wagons, "third row" -> suv+van), fuel/drivetrain (electric/hybrid/diesel hard, AWD/4x4 soft boost), price ("under $30k", "around 25k" +-12%, "between 20 and 35k", "$30k budget"), year ("2020 or newer"), mileage ("under 50k miles"), sort hints (cheapest/newest/low miles) + leftover model words; classify_body() = explicit attributes.body_type/tags first, then a model-name regex table (Ram ProMaster van before Ram truck, Corolla Cross SUV before Corolla sedan, Mach-E SUV before Mustang coupe); select_matches() returns (matches, exact) - when the body/fuel fits but price/year/miles do not, returns closest with exact=False. ai_reply._search_inventory_context now loads the store's available inventory (limit 400) and filters in Python; context header "Customer is asking for: trucks, under $30,000. MATCHES (N in stock)" or "NO EXACT MATCH ... Closest options"; prompt tells Jessi to be honest and offer the closest. Routing: is_shopping_message() (filters + intent word/question/number; "I love my truck" is NOT shopping) is OR-ed into is_fact_topic so shopping asks follow rule 5 (live answer when inventory matches, else "let me check" + pause). Inventory: ATTR_FIELDS + CSV aliases gained body_type (body/body style/type); Add Vehicle modal has optional BODY TYPE chips (testid inventory-body-{truck|suv|...}); list rows show body type. SHIP: backend Deploy + eas update.
- **Broadcast delivery receipts "3 of 4 delivered" (June 2026) - DONE (verified: simulated Twilio status callbacks, duplicate callback not double-counted, UI screenshots)**: scheduler campaign send now stores twilio_sid/has_media/status on the messages doc (thread checkmarks work for campaign + broadcast texts too) and message_sid + delivery_status 'sent' on campaign_pending_sends. twilio_webhooks /status -> _record_send_receipt(): first delivered/undelivered receipt per message_sid stamps delivery_status/delivery_error/delivered_at and $inc broadcasts.delivered_count / undelivered_count (idempotent via $nin guard). broadcast.py: serialize returns delivered_count/undelivered_count; /recipients rows now delivered | sent (awaiting receipt) | undelivered (carrier error shown) | failed | queued | unconfirmed + `summary {delivered, accepted, undelivered, awaiting_receipt}`. UI: broadcast/[id].tsx stats row Recipients/Sent/Delivered/Failed(failed+undelivered), Delivery Results header line "2 of 3 delivered · 1 bounced · N waiting on carrier" (testid delivery-summary), per-row badges; broadcast/index.tsx card "2 of 3 delivered" (testid broadcast-delivered-{id}) + red "N failed", statsRow wraps. Also fixed deep-link spinner: fetchBroadcast effect now depends on user._id (cold load never fetched). Refresh icon de-blued to gold. SHIP: backend Deploy + eas update.
- Push notifications prod RCA still OPEN: preview cannot log into production (different password); needs the user's "Send me a test push" result from Settings > Notifications > Push Health Check on the iPhone.
- **Alerts redesigned as an ACTION list (June 2026, USER: "notifications too busy, can't swipe/clear, need to direct me to what to do") - DONE (tests/test_alerts_center.py 9/9 + web flows verified: swipe dismiss, undo, clear all + undo, bell -> screen, action -> thread)**: Mockups A/B/C generated; user skipped the question so agent chose A (flat action list). BACKEND routers/notifications_center.py REWRITTEN: ACTION_TYPES map -> verb-first title ("Reply to Sarah Miller", "Approve draft for Bud", "Respond to <lead>", "Call <name>" for overdue call tasks), `action {label, icon, link}`, `bucket` now|today|later (stale alerts sink: now->today after 24h, today->later after 48h), `context` (= legacy body). SELF-CLEANING: duplicates (same type + thread/contact) collapse to newest; reply-type alerts auto-resolve when the rep texted that thread after the alert; ai_draft_approval_required resolves when the queue item is no longer pending; resolved docs get dismissed+auto_resolved. Overdue tasks: newest-due first (limit 15), call tasks deep-link /call-screen?phone&contact_name&contact_id&task_id, others /contact/{id}?taskId=. customer_reply_ai_handling and other non-action types are activity only (still served for feed=activity for old builds). New endpoints: POST /{uid}/dismiss {ids}, /undismiss {ids}, /clear-all (returns ids for Undo); virtual ids (task_/task_soon_) live in notification_reads.dismissed_ids; real docs get dismissed=True. Response adds bucket_counts. SECURITY: PROTECTED_PREFIXES had a typo "/api/notifications-center/" so the center was unauthenticated -> fixed to "/api/notification-center/" (401 unauth / 403 other user verified). FRONTEND app/notifications/index.tsx REWRITTEN: header "Alerts · N things need you" + gold "Clear all" (confirm -> clears -> Undo bar 6s); no For You/Activity toggle, no filter chips (Activity tab already exists); NOW (red) / TODAY (gold) / LATER (gray) sections with counts; components/notifications/AlertRow.tsx = icon + verb title + context + time + gold action pill, unread = colored left border, swipe left = red Dismiss (RNGH Swipeable native / WebSwipeableItem web) with Undo bar; tap or action -> deep link (demo-form new_lead claims first, as the old bell did). NotificationBell.tsx: dropdown REMOVED, tap goes straight to /notifications; useNotifications now only polls unread-count. testids: alerts-subtitle, alerts-clear-all, alerts-bucket-{now|today|later}, alert-row-{id}, alert-action-{id}, swipe-action-dismiss-{id}, alerts-undo-bar, alerts-undo-btn, notif-empty, notification-bell-btn/-badge. Legacy tests test_notifications_center_dashboard.py etc. were already failing before (unauth/stale). SHIP: backend Deploy + eas update.
- **Automatic Inventory Feeds (June 2026, USER: "path of least resistance for inventory feeds; Files.com too expensive") - DONE (tests/test_inventory_feeds.py 15/15 + UI screenshots)**: see /app/memory/INVENTORY_FEED_PLAYBOOK.md. `services/inventory_feed.py` (parsers CSV/TSV/pipe + Facebook/Google XML catalog, header alias map incl. ImageList/photos, Internet Price priority, condition New/Used/CPO, listing_url; `build_item` now shared with manual CSV upload in routers/inventory.py; transports url (httpx, sha256 change detection, Google Sheet edit link -> export csv) and sftp (paramiko, newest file by pattern, name/mtime/size change detection); `sync_feed` upserts by VIN/stock within store scope, marks dropped units sold, records `inventory_feed_runs`, `_maybe_alert` -> `inventory_feed_issue` notification after 2 consecutive failures or 3 stale days). `routers/inventory_feeds.py` (/api/inventory-feeds, manager roles only, protected prefix): GET list (+runs, live_units), POST test (dry run preview: vehicles, fields, sample, warning), POST create (+run_now), PUT, POST /run, GET /runs, DELETE (?remove_vehicles). Scheduler `hourly_inventory_feeds` at :20. Alerts: inventory_feed_issue -> "Fix" action to /admin/inventory-feed. FRONTEND app/admin/inventory-feed.tsx: feed cards (status pill Healthy/Needs attention/Paused/Waiting, live units, last pull, +added/~updated/-sold, error, recent pulls), form (transport chips Feed link (free) / SFTP drop, provider chips HomeNet/vAuto/Dealer.com/DealerOn/Google Sheet/Other, Test -> preview box, Connect and import), Pull now / Edit / Pause / Delete, dealer checklists (feed link, HomeNet SFTP, vAuto SFTP, owner SFTPGo box for $4/mo with exact apt commands and port 22 note, columns we understand). Entry points: Tools > Manage > Inventory Feed (admins) and Inventory header "Feed" button (managers). paramiko added to requirements. SHIP: backend Deploy + eas update.
- **Lot Photo Links + Snooze Alerts (June 2026) - DONE (tests/test_vehicle_link.py 3/3, tests/test_alerts_center.py 10/10, ai_routing_matrix 33/33, web swipe verified)**: LOT LINK: `_search_inventory_context(db, user_id, message, contact_id)` now returns (context, media, link); when ONE vehicle is clearly meant and it has `listing_url` (from a feed or the new "Website link" field in Add Vehicle), a per-contact tracked short link is created (`create_short_url(..., "vehicle_listing", reference_id=f"{item_id}:{contact_id}", metadata={contact_id, inventory_id, vehicle})`) and appended after the LLM reply as "All the photos and details: {short}" (LLM is told never to write URLs). Clicking it logs contact_event `vehicle_link_clicked` ("Viewed Vehicle Online", car-sport icon, description names the vehicle) + engagement signal `vehicle_viewed` -> notification "Opened the 2024 Tacoma listing" -> Alerts "Reach out to X". Inventory list: thumbnails fall back to feed `primary_image`, WEB PAGE / FEED chips. SNOOZE: POST /notification-center/{uid}/snooze {ids, until?} (default next 8:00 AM in user.timezone, fallback America/Denver; stored in notification_reads.snoozed.{id}), /unsnooze. Snoozed items hidden until `until`; when it passes they come back under TODAY (never buried in LATER) with `snoozed_until`. UI: swipe RIGHT on an alert = indigo Snooze pane (native renderLeftActions / web leftActions, testid swipe-action-snooze-{id}); undo bar "Snoozed until Sun 8:00 AM · UNDO" calls unsnooze. Hint updated. SHIP: backend Deploy + eas update.
- **Hot Vehicles (June 2026) - DONE (tests/test_hot_vehicles.py 3/3, Home + Inventory screenshots)**: GET /api/inventory/{uid}/hot?days=7&limit=8 ranks the store's vehicles by score = 2 x tracked lot-link opens (contact_events vehicle_link_clicked, metadata.inventory_id) + Jessi asks (new `inventory_interest` collection; `_search_inventory_context` inserts a row whenever ONE vehicle is clearly meant). Returns per vehicle: clicks, asks, shoppers (name, contact_id, clicks, rep first name, mine flag, last), shopper_count, score, prev_score (previous window), trend new/up/down/flat, last_activity, photo/primary_image, listing_url; plus total_shoppers. `components/home/HotVehiclesCard.tsx`: variant 'home' = "HOT THIS WEEK" horizontal strip of top 3 (photo, #rank flame badge, "4 opens · 0 asks · 2 shoppers"; tap -> /inventory) placed under Weekly Wins on Home; variant 'full' on top of the Inventory list = ranked rows with trend icon + time ago, tap expands shopper chips (gold = my contact, other reps show rep name) -> /contact/{id}. Hidden when no data. Inventory header Feed button is icon-only now (title was truncating). testids: hot-vehicles-card, hot-vehicles-shoppers, hot-vehicle-{id}, hot-shoppers-{id}, hot-shopper-{contactId}. SHIP: backend Deploy + eas update.
- **Hot Shopper Push (June 2026) - DONE (tests/test_vehicle_link.py 5/5, test_alerts_center 10/10, web e2e: Alerts NOW row -> Text -> thread prefilled)**: When a shopper opens a tracked lot link, the rep's push + alert are now vehicle-first and actionable. engagement_signals.record_signal (signal_type vehicle_viewed): title "Sally just opened the Tacoma" (repeat opens of the same inventory_id: "Sally is back on the Tacoma"), body "2024 Toyota Tacoma TRD Off-Road[, opened N times]. Text Sally while it's top of mind.", link = /thread/{conversation or contact id}?prefill=<ready-to-send text> ("Hey Sally, saw you were checking out the ... Want me to set it aside so you can see it in person?"; thread screen already honors ?prefill). notif doc gains vehicle, vehicle_short (short_vehicle(): drops year+make), inventory_id, link. short_urls passes inventory_id into the signal metadata. Alerts center: vehicle_viewed engagement signals land in NOW as "Text {first} about the {Tacoma}", action Text -> the prefilled thread. Other engagement signals unchanged. Also fixed: app/auth/login.tsx dynamic import path ../services/errorReporter -> ../../services (was unresolvable), 4 unescaped-quote lint errors in inventory.tsx. SHIP: backend Deploy (push copy + alert) + eas update (login fix only).
- **Promo videos (June 2026) - VIDEO 1 DONE, awaiting user review**: User asked for promotional marketing videos; chose "all three, start with A" (A = Relationship OS story, B = "Jessi answers in 30 seconds", C = digital card + QR + reviews). Defaults used: both 9:16 and 16:9, AI male narrator (OpenAI TTS onyx, tts-1-hd via Emergent key), audience = store first. Pipeline in /app/marketing/video: promo.html (brand black+gold scenes with CSS animations, deterministic via window.showScene(id,dur) + window.seek(ms)), specs/<id>.json (scene order, VO lines, min durations), render.py (TTS -> wav durations -> scene timeline -> Playwright frame capture 30fps -> ffmpeg from imageio_ffmpeg, loudnorm -16 LUFS). Outputs to /app/marketing/build-preview/videos/<id>-<ratio>.mp4, served at {BACKEND_URL}/api/marketing-preview/videos/index.html. Video A = 47.6s, 7 scenes (hook CRM card -> 74% stat -> Your 3 for Today phone -> voice note memory -> Sarah opened the Tacoma push + prefilled text -> Reps leave/relationships stay -> logo end card, imonsocial.com). Logo: user corrected it to the speech-bubble iM (assets/logo.png, white tile removed from public/android-chrome-512x512.png). No music (no licensed library). Re-render: cd /app/marketing/video && python render.py relationship-os 9x16 (~1 min each, TTS cached in .cache/*.mp3). NEXT: user feedback on A, then build specs+scenes for B and C.
- **Birthday off-by-one fix (June 2026) - DONE**: Contact page read "YYYY-MM-DD" via new Date(str) = UTC midnight, so Sep 11 showed as Sep 10 in US zones. parseDate in app/contact/[id].tsx now parses date-only strings as local dates (full ISO datetimes unchanged); DetailsTab birthday picker builds the string from local Y/M/D (toYMD) instead of toISOString (fixes the reverse shift in positive-offset zones). Verified with node TZ=America/Denver + Europe/Berlin and web e2e. SHIP: eas update.
- **Share the App + install tracking + marketing QR (June 2026) - DONE (tests/test_app_links.py 8/8, web e2e screen + marketing section verified)**: User wants to share the App Store link (https://apps.apple.com/us/app/im-on-social/id6774618559), know when someone installs, and put a QR on the marketing site. Decisions: every user gets a personal link (imonsocial.com/get/<firstname>, Forest = /get/forest, marketing site uses /get/site owned by the super_admin), Android/desktop: Android -> imonsocial.com/#get-the-app (no Play listing yet, PLAY_STORE_URL env when available), desktop -> Apple listing; QR + App Store badge in a new dark "Get the app" section above the bottom CTA + footer "Get the iPhone App" link (5 pages). Backend routers/app_links.py: GET /api/get/{code} (logs app_link_taps with ip_hash+platform, 302), GET /api/app-links/qr/{code}.png, POST /api/app-installs/first-open (public, once per device; matches most recent unmatched tap same ip_hash+platform within 72h -> app_installs.attributed_*; push + notification type app_install to owner), POST /api/app-installs/claim (Bearer; ties install to signed-in user; notification app_signup "{name} joined from your link"), GET /api/app-links/{user_id} (auto-creates code; stats taps/taps_week/installs/signups + recent). /api/app-links/ added to PROTECTED_PREFIXES; alerts ACTION_TYPES app_install/app_signup -> /share-app. Frontend: services/installBeacon.ts (reportFirstOpen on launch, claimInstall after auth; native only) wired in app/_layout.tsx; app/share-app.tsx screen (QR via api.defaults.baseURL + qr_path, Copy/Share, 3 stat cards, recent activity); My Brand menu entry "Share the App". Marketing: build/index.html section #get-the-app with inline SVG QR (encodes https://imonsocial.com/get/site), vercel.json redirect /get/:code -> https://app.imonsocial.com/api/get/:code (netlify.toml too). Attribution is probabilistic (same network within 3 days); sign-ups are exact. Apple campaign pt token skipped. SHIP ORDER: backend Deploy -> eas update -> redeploy marketing (Vercel).
- **Pushes mirrored into Alerts + /get link fallback (June 2026) - DONE (26 backend tests green)**: User got "Good morning, Forest - 2 tasks today" push on laptop, could not find it in-app. send_push_to_user now writes a `notifications` doc type "push" (title/body/link/icon) for every push right after the sms-only check (before quiet-hours hold, so held pushes are still visible in-app); skipped when a matching notification (same title or body) exists within 3 min; alerts center also drops push mirrors that duplicate a real alert (<180s same title). ACTION_TYPES push -> today / "{title}" / Open / category appts. Added notifications_center.invalidate_feed(user_id) (30s TTL cache) and call it from push mirror, app_links, engagement_signals so new alerts appear instantly. Marketing: imonsocial.com/get/site returned Vercel 404 (redirect in marketing/vercel.json not applied by however the site is deployed). Added build/vercel.json (redirects) + build/404.html with JS redirect for /get/<code> -> https://app.imonsocial.com/api/get/<code> (works on any static host). Production backend confirmed live: app.imonsocial.com/api/get/site -> 302 App Store. SHIP: backend Deploy + redeploy marketing build folder.

- **Training rewrite for the simplified app (June 2026) - DONE, verified on preview (rep + manager screenshots, curl, Help AI)**: `backend/content/training_2026.py` (CONTENT_VERSION 3) is applied lazily and once per environment (app_meta.training_content_version) from GET /api/training/tracks and GET /api/sop/ -> production picks it up on the first Training Hub open after Deploy. It REPLACES: Sales Team Onboarding (8 lessons: First 15 Minutes, Home in Three Cards, Meet Jessi, Inbox, Contacts + Book of Business, Your Brand, Inventory + Lot Links, Alerts), Manager's Playbook (6 lessons: Onboard a Rep in 10 Minutes, Buy and Assign a Number, Set Up the Store for Jessi, What Reps See, Weekly Rhythm, When a Rep Leaves), 5 getting_started SOPs. Old 'Onboarding Videos' track -> 'Legacy Videos (old interface)', managers+ only, order 99. Internal iMOS tracks (partners, white label, platform setup, support, billing, sales process...) no longer carry user/manager/store_manager roles, so reps see ONLY Sales Team Onboarding and store managers see Sales Team + Manager's Playbook + Legacy Videos (admins still see all 13). All paths verified against more.tsx: Inventory Feed + Review Links are under Tools > Manage, Lead Source Config / Zapier / Team Availability under Tools > Leads, Bulk Transfer + Manage Training are admin-only. Help Center: app/help.tsx articles rewritten (13 rep how-tos + 9 manager-only, gated by MANAGER_ROLES/partner_id), gold accents, testID+dataSet; help_center.py system prompt now = header + `training_2026.knowledge_text()` (navigation map + every lesson + SOP steps, ~24k chars), answers plain text, ** stripped, no_em_dash applied, user_id sent for session. Manager Playbook: /app/docs/MANAGER_PLAYBOOK.md (10 sections) rendered by `python /app/marketing/build_playbook.py` to marketing/build-preview/playbook/index.html -> served at {BACKEND}/api/marketing-preview/playbook/ (Print / Save as PDF button). Re-run the build script after editing the .md. SHIP: backend Deploy (content applies itself) + eas update (help.tsx). NOTE: /help and /inbox on web are shadowed by marketing pages; reach the app screens via Tools tab in tests.

- **Contact intel polish (June 2026)**: purple 'Updated from texts' chip in IntelBriefingCard and the HealthBadge pill were overflowing the card (row + numberOfLines Text without flexShrink) -> fixed with maxWidth 100% + flexShrink on the text. Renamed the purchase_context field label 'Why they bought' -> 'Hot buttons' everywhere (voice_intel label map + extraction prompt now covers shoppers who have not bought yet, relationship_intel briefing prompt, PersonalIntelSection form + display with flame icon). DB key purchase_context unchanged. Ships with eas update + backend Deploy.

- **Rep Welcome Tour (June 2026) - DONE, verified on preview**: `components/home/WelcomeTour.tsx` full-screen 3-slide modal (Do This Next / Your 3 for Today / Needs a Reply, each with a mock of the real card, dots, Next / Let's go, Skip). Shown once per user for simpleHome reps (role user, no partner_id) after Home's first load; persisted in AsyncStorage key welcome_tour_v1_{userId}. Replay row 'Show me around Home' at the top of Help Center (help-replay-tour). testids: welcome-tour, welcome-tour-slide-N, welcome-tour-next, welcome-tour-skip. Ships via eas update.

- **Push tap + Alerts readability (June 2026)**: Cold-start push taps were navigating at a fixed 800ms while the session was still restoring, so index.tsx's auth Redirect wiped the deep link (user landed on Home instead of e.g. /leads?tab=proof). _layout.tsx now holds the URL in pendingPushUrl and navigates 1.2s after isAuthenticated && !isLoading (also keeps ?query when stripping a full https URL). AlertRow shows the full body for mirrored 'push' items (no 2-line clamp) so weekly proof / morning brief can be read in Alerts. Mirror verified in preview: send_push_to_user -> db.notifications type push -> feed item bucket today with action link. NOTE: macOS banners with the small phone badge are iPhone Mirroring; clicking them on the Mac does not deep link. Ships via eas update.

- **'Unknown' touchpoints fix (June 2026)**: follow-up tasks auto-created by voice_intel._ensure_followup_from_voice (voice notes AND text-derived intel via process_conversation_intelligence) stored only contact_id, no contact_name/contact_phone, so Touchpoints showed '?' / Unknown and Call/Text had no number. Fixed at the source (name + phone written on insert) and GET /api/tasks/{user_id} now back-fills missing names/phones from contacts and persists them, so existing production tasks heal on first load. Verified via probe task. Backend Deploy only.
- **QA plan requested (not executed)**: user asked for a game plan to standardize every Tools screen (text sizing, layout, dark theme, older screens). See chat response; do not start without approval.

- **Tools QA pass, Batch 1 My Brand (June 2026) - DONE, testing agent iteration_295 95%**: Source of truth = /app/docs/TOOLS_QA.md (inventory of 74 screens, the visual standard, batch order, status per screen). New shared `frontend/components/common/ScreenHeader.tsx` (+ HeaderIconButton / HeaderTextButton): 52px bar, gold chevron, centered FS.nav title, right slot, canGoBack() fallback to /(tabs)/more. Converted 13 screens (broadcast list/new, templates, card-templates, review-links, store-profile, email-signature, link-page, showroom-manage, share-app, create-card, quick-send/[action]). Bugs fixed: review-links infinite Loading (useEffect before user), email-signature raw <img> (native crash) -> Image + initials fallback, store-profile bare no-store text -> empty state with Open My Profile, rep + manager 'My Digital Card'/'My Landing Page' tiles -> /my-profile, review-links Copy actually copies, no-store Save shows toast. iOS blue #007AFF primary accents -> colors.accent across the batch. Removed dead Stack.Screen names in settings/_layout. NEXT BATCH: Today + loose tiles (Touchpoints, Calendar, My Numbers, AI Follow-ups, Inventory, Internet Leads). Pattern per screen: swap header for ScreenHeader inside SafeAreaView edges top, replace #007AFF with colors.accent (black text on gold buttons), card radius 16 + 1px border, eyebrow 12/700 uppercase, subtitles 13, remove em dashes, real empty state.

- **Tools QA Batch 2, Today + loose tiles (June 2026) - DONE, iteration_296 + retest iteration_297 100%**: touchpoints/index, touchpoints/performance (now titled My Numbers), ai-outreach (AI Follow-ups), inventory (feed/CSV in manager-only toolbar), leads, dates-calendar all on ScreenHeader; light-blue/purple accents -> gold with black text on active pills. Backend: scheduler.resolve_template_variables normalizes {{var}}, falls back to 'new ride' when no vehicle ('your new .' bug), and treats phone-shaped names as 'there'; routers/tasks._serialize strips em dashes from suggested_message/description/title/campaign_name and rewrites 'Hi 5550001234,' greetings (utils/text_sanitize.no_phone_greeting). Touchpoints formats phone-only contact names as (555) 000-1234. Ships: backend Deploy + eas update. NEXT: Batch 3 Settings (my-profile, virtual-assistant, notifications, schedule, security, calendar).

- **Tools QA Batch 3, Settings (June 2026) - DONE, iteration_298 90% + 2 fixes verified**: virtual-assistant (My VA, pencil in header), notifications (Save in header when dirty, throttle chips gold), schedule (Save in header), security (modal Save gold), calendar (titled Calendar Sync, Connect gold) all on ScreenHeader; my-profile keeps its cover hero (back has canGoBack fallback + testid). Em dashes removed from all copy in these screens. Ships via eas update only. NEXT: Batch 4 Learning is mostly done (Training Hub/Help rebuilt) -> only /report-bug left, so fold it into Batch 5 Leads (call-retries, admin/internet-leads, admin/lead-sources, admin/lead-connect, admin/team-availability).

- **Tools QA Batch 4, Leads + Report a Bug (June 2026) - DONE, iteration_299 93% + 2 fixes verified**: report-bug, settings/call-retries, admin/internet-leads (Lead Source Queue), admin/lead-sources index/new/[id] (Lead Source Config), admin/lead-connect, admin/team-availability on ScreenHeader; all #007AFF -> GOLD in lead-sources screens; selection highlight gold. Backend: routers/user_schedule /schedule/team name fallback (name -> first+last -> email -> 'Unnamed user'); routers/lead_intake list_leads strips em dashes from draft_message. Ships: backend Deploy + eas update. NEXT: Batch 6 Manage (settings/tags, settings/keyword-rules, keyword-search, campaigns, settings/review-approvals, settings/showcase-approvals, admin/inventory-feed). Note tags.tsx still uses stale JESSI_BAR_HEIGHT padding.

- **Tools QA Batch 5, Manage (June 2026) - DONE, iteration_300 99%**: settings/tags (stale JESSI_BAR_HEIGHT removed, all blue -> GOLD), settings/keyword-rules (indigo -> gold incl. submit), keyword-search, campaigns/index (3 header actions, toggles gold), settings/review-approvals (subtitle), settings/showcase-approvals, admin/inventory-feed all on ScreenHeader. Frontend only. NEXT: Batch 7 My Performance (reports/team-performance, team-tasks, touchpoints/customer-performance, admin/leaderboard, reports/activity, settings/email-analytics, seo-health, geo-health, admin/va-library).

- **Tools QA Batch 6, My Performance (June 2026) - DONE, iteration_301 pass + GEO em dashes fixed same turn**: reports/team-performance (titled Team Sales), team-tasks, touchpoints/customer-performance (titled Customer Engagement), admin/leaderboard, reports/activity (titled Activity Reports), settings/email-analytics, admin/va-library on ScreenHeader. seo-health + geo-health collapsed into shared `components/health/HealthScoreScreen.tsx` (theme colors instead of hardcoded #000/#FFF). Real bugs fixed: GEO guide link went to /seo-guide; VA Library delete used window.confirm (crashes native) -> showAlert; Email Analytics range pills did nothing -> client-side filter; leaderboard alert() -> showAlert and core Image -> expo-image. Backend: seo.py Good tier + geo.py Building tier grade_color blue -> gold, geo.py tip strings em dashes removed. Category chart colors intentionally kept (Referrals blue / Repeats purple match Home tiles; SMS Sent blue stat). yarn.lock REGENERATED: the committed one failed `yarn install --frozen-lockfile` (EAS would have failed); new one passes. Ships: backend Deploy + eas update. NEXT: Batch 7 Campaigns + My Tools (campaigns/dashboard, settings/date-triggers, jessie, (tabs)/team), then Batch 8 Set Up.

- **Tools QA Batch 7, Campaigns + My Tools (June 2026) - DONE, iteration_302 pass**: campaigns/dashboard, settings/date-triggers, jessie (Ask Jessi), (tabs)/team (Team Chat) on ScreenHeader. Team Chat: deleted its file-local COLORS palette (blue accent + hardcoded blacks) in favor of theme colors, chat view header = channel name + member subtitle + ellipsis, own bubbles gold/black text, expo-image avatars. Dashboard Ready-to-send card gold/black. Date Triggers toggles + pills + saves gold. Frontend only -> eas update. Tester's "blue numbers on Team Sales" = Referrals category color by design (matches Home tiles), not a regression. LESSON: `yarn install` under a running Metro stales its file map -> web 500 "Importing native-only module ... FullWindowOverlayNativeComponent"; fix is rm -rf frontend/.metro-cache + supervisorctl restart frontend. NEXT: Batch 8 Set Up (settings/store-profile, settings/brand-kit, settings/messaging-channels, admin/users + [id], settings/invite-team, settings/integrations), then Admin screens marked `later`.

- **Tools QA Batch 8, Set Up (June 2026) - DONE, iteration_303 pass + backend em dash fixed same turn**: settings/store-profile (touch-ups), settings/brand-kit, settings/messaging-channels, admin/users + admin/users/[id], settings/invite-team, settings/integrations on ScreenHeader with gold accents. Real bugs fixed: Brand Kit logo upload was web-only (silently did nothing on the phone) -> expo-image-picker; Team Members "Add User" modal never rendered on native (Platform branch returned null) -> real Modal; stale JESSI_BAR padding removed from admin/users. Role category colors unified across screens: super_admin red, org_admin orange, manager green, rep gold. Backend: messaging_channels.py WhatsApp description em dash removed. Ships: backend Deploy + eas update. Every rep/manager Tools screen is now standardized; only super-admin `later` screens (TOOLS_QA rows 54, 62-74) remain.

- **Tools QA Batch 9, Admin screens (June 2026) - DONE, iteration_304 pass**: 27 super-admin screens (admin/index, onboarding-hub, account-health, organizations, stores, individuals, pending-users, lead-tracking, hot-leads, partner/dashboard, partner-agreements, white-label, billing, forecasting, quotes, create-quote, discount-codes, shared-inboxes, bulk-transfer, app-directory, brand-assets, error-reports, bug-reports, sops, system-logs, manage-training (4 views), training-reports) converted with a codemod (/app/memory/admin_codemod_batch9.py) onto ScreenHeader; blue chrome -> gold with black text on gold fills; category palettes kept. HeaderTextButton gained an optional `color` prop. Real bugs fixed: Account Health period toggle active style never showed (style order); discount-codes getDiscountColor used undefined `colors` (crash for <10% codes); bulk-transfer fetched with user_id=undefined before auth hydrated (403); lead-tracking/hot-leads had no SafeAreaView; stores rendered empty-string org names as bare text nodes (150 console errors). TOOLS_QA.md: all 74 rows now `fixed`; the standardization project is complete. Frontend only -> eas update.

- **Phone regression sweep (June 2026) - DONE**: automated 390x844 pass over all 88 Tools routes (overflow, missing back, blank, console errors). Fixed: Help Center, Training Hub and Phone Numbers (admin/twilio-numbers) moved onto ScreenHeader (missed by every batch); Invite Team + Revenue Forecast TextInputs overflowed (minWidth 0); Accounts store name truncation; empty-string text-node children in Individuals/Shared Inboxes/Quick Send; SOPs list key. Real-device verification still needs the user on production after eas update.

## Account Health Alerts (weekly Monday email) + Rep Welcome Video (June 2026) - DONE (backend tests 16/16, UI verified via in-pod Playwright)
- **Account Health Alerts (super admins)**: routers/account_health.py: `_health_rows()` (shared with /overview), `run_weekly_account_health_alert(force_to=None)` compares the current 30-day grades with last week's snapshot (`account_health_snapshots` {week_key, taken_at, grades{uid:{score,grade}}}; baseline = newest snapshot taken 6+ days ago, else newest) -> slipped_critical / slipped_risk (Healthy->At Risk) / recovered / still_critical. Monday job writes the snapshot, emails super admins (opt-out `notification_settings.account_health_alerts=false`) only when something changed, dedupes via `account_health_alert_log` {week_key}. Email: light theme, 4 count tiles, per-account rows (score prev -> now, last login, texts, contacts) with gold "Open" -> APP_URL/admin/account-health/{user_id}. Scheduler `weekly_account_health_alert` Mon 15:10 UTC. Endpoints (require_role super_admin): GET /account-health/alerts/status, PUT /account-health/alerts/settings {enabled}, POST /account-health/alerts/preview (emails the caller, "[Preview]" subject, writes a baseline snapshot if none exists). UI: components/admin/HealthAlertCard.tsx at top of /admin/account-health Overview (toggle, "Send me a preview", schedule/last sent line; testids health-alert-card/-toggle/-preview-btn/-result/-meta). Test: backend/tests/test_account_health_alert.py.
- **ENV FIX (preview)**: every Resend email had been failing since April with "send.imonsocial.com domain is not verified" (verified domains are imonsocial.com and imosapp.com). backend/.env SENDER_EMAIL changed to notifications@imonsocial.com; preview email now delivers. PRODUCTION must set the same SENDER_EMAIL (or verify send.imonsocial.com in Resend) or all emails (health reports, bug digest, invites, CRM push, forgot password) silently fail there too.
- **Rep Welcome Video**: /app/marketing/video/welcome.html (app-pixel Home mock at 390x693 zoomed to 1080x1920: status bar, header, Do This Next, Your 3 for Today, Needs a Reply, FAB + speed dial, tab bar; scenes intro/next/my3/reply/sold/tabs/end with focus glow, dimming, tap rings, sheet, SOLD burst) + specs/rep-welcome.json (voice nova, 7 VO lines). render.py now honours spec "page". Output marketing/build-preview/videos/rep-welcome-9x16.mp4 (62.8s, 4.8 MB, crf 25 re-encode) + rep-welcome-poster.jpg (promo_videos router now serves .jpg); listed on /api/promo-videos/. frames.py = debug frame grabber. Re-render: `cd /app/marketing/video && python render.py rep-welcome 9x16`.
- Frontend: components/home/WelcomeVideo.tsx (expo-av Video, poster until playing, web starts muted with "Tap for sound", replay overlay, error message, Let's go, "Prefer to read? Show me the three cards" -> WelcomeTour). home.tsx: first-time simpleHome reps now get the VIDEO (same welcome_tour_v1 gate; closing marks welcome_video_v1 + welcome_tour_v1). Help Center: new row "Watch the welcome video" (help-welcome-video) above "Show me around Home". services/api.ts exports API_BASE_URL. NOTE: headless Playwright Chromium has no H.264 so the player shows the poster in automated tests; verify playback on device. SHIP: backend Deploy (mp4 + endpoints + scheduler) THEN `eas update`.

## Marketing Homepage Swap (June 2026) - DONE, user approved to ship
- marketing/build/index.html is now the Relationship OS story (hero "Your CRM remembers the deal. i'M On Social remembers the person.") with the FULL mega-nav (Products / Solutions / Resources / Pricing + Book a Demo + Sign In), get-app strip, footer, demo modal and chat widget carried over from the old home. Hero has a quiet "See everything the platform does -> /platform/" link and a "The full platform" grid section links into /platform/ and feature pages.
- Old homepage moved to marketing/build/platform/index.html (title "The Full Platform - i'M On Social"). Every site page that linked to /relationship-os/ now points to / (the old copy at build/relationship-os/ still exists; vercel.json + netlify.toml 301 /relationship-os -> /).
- One-off transform script: marketing/swap_home.py (already run; do not re-run). generate_pages.py remains OUT OF SYNC with built pages - never regenerate blindly.
- Verified locally (python http.server on build/): / 200, /platform/ 200, /features /pricing /help /demo 200, no em dashes, screenshot of new hero + nav OK.
- SHIP: static site only. User saves to GitHub; Vercel/Netlify redeploys the marketing site. No backend or eas update needed.

## Thread "No Phone" on Call Fix (June 2026) - DONE, verified in preview (USER-REPORTED from prod screenshot)
- Bug: opening a lead thread from the Home lead alert / push / inbox queue / anywhere that pushes plain `/thread/{id}` and tapping Call showed "No phone number for this contact." even though the intake text had just been sent.
- Root cause: thread/[id].tsx derived `contactPhone` ONLY from the `contact_phone` URL param; ~20 entry points open `/thread/{id}` without it.
- Fix: new `loadedContactPhone` state filled from GET /messages/conversation/{id}/info (contact_phone) or the contact record fallback (phone/phone_mobile); `contactPhone = param || loaded`. Header phone line, Call pill, composer call icon, share links all use it.
- Verified on preview: /thread/6a9bac3b62b000abb6c965ee opened with no params -> header shows (500) 555-0377, Call -> /call-screen?phone=+15005550377 (no alert).
- SHIP: frontend only -> `cd frontend && eas update --branch production --message "Fix Call button No Phone on lead threads"`

## Jessi Off-Topic on Website Demo Leads FIX (June 2026) - DONE, verified in preview (USER-REPORTED from prod: "demo on that Mojave")
- ROOT CAUSE (4 parts): (1) AI prompts for lead threads never received the lead's inquiry (website form = software demo, company, business type, page, message); (2) clone prompt defaulted to "the dealership" and the rep's VA bio is automotive, plus relationship intel injected the contact's vehicle history + cross-thread old messages; (3) intent detector prompt hardcoded "automotive dealership CRM" ("schedule a demo visit" push); (4) inventory search fired on any lead.
- FIX: new services/lead_context.py (build_lead_inquiry / get_conversation_inquiry / inquiry_prompt_block / is_vehicle_inquiry). Inquiry stored on inbound_leads + conversations at intake; legacy lead threads derive it from attribution + inbound_lead and cache it (so the existing production thread is fixed on next reply). Block injected into auto-reply, follow-ups, Jessi suggests, first message, Home draft, intent detector. Non-vehicle inquiries skip inventory/shopping detection and vehicle history; intel reads only the lead thread's messages. "the dealership" -> "our team" default; intent prompt vertical-neutral. Lead Source > Response Workflow gained "What Are These Leads Asking About?" (inquiry_context, PUT/GET /lead-sources/{id}/workflow, admin/lead-sources/[id].tsx, testid inquiry-context-input).
- VERIFIED: tests/manual_lead_inquiry_probe.py with real LLM: BEFORE "pics can make a rig look way worse... check the condition" vs AFTER "For the demo, do you want a quick walkthrough focused on the texting side or the AI side?" (legacy + new threads, auto-reply + suggests). ai_routing_matrix 33/33. Workflow field round-trip via curl + screenshot. Intent: hot_summary "Customer wants to schedule software demo Thursday afternoon".
- SHIP: backend Deploy (all AI fixes) + `eas update` (Lead Source field only). USER TODO: VA bio still says automotive sales; recommend adding that he sells i'M On Social.

## Inventory Narrowing ("what trucks do you have?") (June 2026) - DONE, verified (matrix 41/41 + real-LLM probe)
- User: Jessi must narrow by price range / type before pushing every truck. NEW in routers/ai_reply.py: broad body-type asks with 4+ fits -> Jessi states the exact count and asks ONE question for budget + use (no list, no photos, no links); the next customer message is treated as an inventory turn and merged with the remembered subject; she then presents 2-3 best fits and asks which one they want pics/details on. Subject memory (conversations.inventory_narrowing, 24h) also fixes "under 30k" after a short truck list drifting to sedans. Never more than 3 options in any reply. Details in /app/memory/AI_ROUTING_RULES.md.
- Verified live: "What trucks do you have in stock?" -> "Right now I've got 10 trucks on the lot. What kind of truck are you after and about what budget range...?"; "Probably under 40k, mostly for work and towing" -> F-150 XLT $36,500 / Ram 1500 $34,900 / Ranger $33,400, "Which one do you want pics and details on?"
- SHIP: backend Deploy only.

## Contact Dedup + Phone Normalization (June 2026) - DONE, verified (routing conflict resolved this session)
- User: "Why is it trying to create a second conversation when it knows who I am... merge those contacts". services/contact_match.py (E.164 `+1XXXXXXXXXX` normalization, last-10-digit tolerant match, store-scoped owners, exact-name flag `possible_duplicate_of`) wired into lead_intake + twilio_webhooks so inbound texts/leads attach to the existing contact instead of spawning a new one. Startup phone consolidation job runs once per boot.
- ROUTING FIX: old routers/contact_merge.py (single-rep only, no store-owned leads) was registered BEFORE contacts.py and silently shadowed the new endpoints. DELETED contact_merge.py + its import/include in server.py. contacts.py now serves: GET /contacts/{uid}/duplicates (rep + store-owned leads via `_dup_owners`, `store_owned` flag, name-based flags), GET /{uid}/merged-history, POST /{uid}/merge (migrates 13 collections, merges tags, copies missing fields, re-owns store lead to the rep, marks dup `status=merged, merged_into`), POST /{uid}/repair-merge, POST /contacts/admin/{uid}/normalize-phones.
- VERIFIED: tests/test_dedup_routes_live.py (18/18 live API checks vs preview: store lead grouped with rep contact, merge migrates events/email/tags, history, repair, normalize -> E.164, pair disappears). Duplicates screen (/contacts/duplicates) screenshot shows rep + "Lead (store)" grouped with gold "Merge into" button. Seed data cleaned.
- SHIP: backend Deploy only (no frontend change).

## Customer Calendar Invites + Morning-of Reminder (June 2026) - DONE, verified (37/37 live checks + screenshots)
- User asked: "easiest solution to push out a calendar invite to customers when an appointment is set" -> picks: (1) auto-send, (2) morning-of reminder, (3) email too.
- services/calendar_invite.py: eligible = appointment_type in (appointment, test_drive, delivery, meeting) + has_time + contact. Auto-send for source manual + call_extraction (text-extracted appointments wait for the rep tap; Jessi may have misread). `schedule_invite` waits 45s (invite_dirty_at token) so a quick time fix sends ONE text. Text from the rep's Twilio number: "Hi Bud, you're all set: Thu, Jun 12 at 2:00 PM with Forest at Store. Tap to add it to your calendar: {short}" + "Reply here if anything changes." Logged as a `user` bubble in the (rep_phone, contact_phone) conversation (created if missing) + contact_event sms_sent. Email via Resend when contact.email: HTML + appointment.ics attachment (METHOD:REQUEST, ORGANIZER rep email -> Gmail/Outlook Accept/Decline). Reschedule (PATCH edit due_date on an invited task) -> "moved to" text, SEQUENCE bump, reminder reset. Task fields: invite_token, invite_short_url, invite_sent_at, invite_channels, invite_sequence, invite_sms_status, invite_email_status, customer_reminder_sent_at/skipped.
- routers/public_appt.py (no auth, /api/public/appt/{token} + {token}.ics): dark/gold page, day + time w/ tz abbrev, rep + store, address -> Apple Maps, buttons Apple (.ics inline) / Google (render?action=TEMPLATE) / Outlook (.ics download); Android reorders Google first; sms:/tel: to rep number. 404 friendly page for unknown/dismissed. Short link type `calendar_invite` (OG title "Your appointment with {rep}", click event "Opened Calendar Invite").
- Reminder: scheduler `customer_appointment_reminders` every 15 min -> first run at/after 8 AM local (user tz -> store tz -> Denver), same-day timed appointments booked BEFORE today, due >= now+30m: "Good morning Bud, quick reminder from Forest: you're set for 2:00 PM today at Store. Reply here if anything has changed." Booked-today -> customer_reminder_skipped=booked_today.
- Rep toggles (users.notification_settings.calendar_invites / calendar_invite_reminder, default on): settings/notifications.tsx "TEXTS TO YOUR CUSTOMERS" (testids calendar-invites-toggle, calendar-reminder-toggle). Manual/resend: POST /api/tasks/{uid}/{tid}/send-invite (force, ignores toggle) -> ContactTasksCard chip "Text calendar invite" / "Calendar invite texted (+ emailed) · Resend" (testid contact-task-invite-btn / -status).
- Test: backend/tests/test_calendar_invite_live.py (Twilio 500-555 test number + delivered@resend.dev; all data cleaned).
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Customer calendar invites + morning-of reminders"`.
- LESSON (recurrence): ContactTasksCard has an early `return null` before the JSX; any new useState MUST sit above it (hit "Rendered more hooks" once).

## Phone Numbers Tile Hidden Fix (June 2026) - DONE, verified on preview
- USER: "im looking in admin and don't see phone numbers?" ROOT CAUSE: app/(tabs)/more.tsx Set Up items used permKey 'admin' (Phone Numbers) and 'notifications' (SMS Notifications); neither key exists in any role template in backend/permissions.py, so `perm('admin', key)` was false for EVERY role incl. super_admin -> tiles never rendered. FIX: Phone Numbers -> permKey 'accounts' (org_admin + super_admin), SMS Notifications -> no permKey. Both now show inside Tools -> **Set Up** folder (not the Admin folder). /admin/twilio-numbers screen verified: 5 numbers, 1 assigned / 4 pool, $5.75/mo, webhook health, Assign/Release/Buy.
- RULE: any new hub item permKey MUST exist in permissions.py templates or it silently disappears for everyone.
- SHIP: `cd frontend && eas update --branch production --message "Show Phone Numbers + SMS Notifications tiles in Set Up"`.

## Workflow Rep Picker Scoped to the Source's Store (June 2026) - DONE, verified preview
- USER (prod screenshot): "why are Kira and I the only two available on this workflow? Matt and Jesse are on this account too." ROOT CAUSE: admin/lead-sources/[id].tsx loaded the "Notify These Reps" list from GET /admin/team/users?user_id={caller} which scopes to the CALLER's account_id/org/store (Forest = super_admin with no store -> only his account_id matches: himself + a couple of users). Store members who aren't on the caller's org/account never appeared.
- FIX: new GET /api/lead-sources/{source_id}/reps (require_manager): users where store_id or store_ids contains the SOURCE's store (str or ObjectId) + org_admins of the store's organization + the caller; excludes deactivated/inactive; returns {reps:[{id,name,email,role,phone,has_number,photo_url}], store_id, store_name} sorted reps -> managers -> admins. Frontend uses it; each row shows an orange "no texting #" flag when the rep has no Twilio/MVP number; hint under the list: "Showing everyone on {store}. Don't see someone? Team Members -> tap their name -> Assign to Store." (testids workflow-reps-hint, rep-no-number-{id}).
- Verified: as Forest OLD endpoint = 3 users vs NEW = 8 (all 7 store members + self); screenshot of Response Workflow shows the full team. Preview repro of the prod bug.
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Lead workflow rep list now shows the whole store"`.

## Phone Contact Import + Profile Photo Hardening (June 2026) - DONE, verified preview
- USER (Kira, 2nd phone, prod): "Import 627 Contacts" -> "Failed to import contacts"; profile picture won't save (Edit Profile). No error text given.
- IMPORT ROOT CAUSE (reproduced): frontend sent year-less birthdays as 1900-MM-DD; a Feb 29 birthday -> "1900-02-29" (1900 not a leap year) -> pydantic datetime error -> 422 for the WHOLE List[ContactCreate] batch. Also 627 sequential find_one+insert_one.
  FIX: POST /contacts/{uid}/import now takes a raw list, validates each row alone (drops unparseable birthday/anniversary first, then other bad fields, keeps the contact; junk rows counted as failed), normalizes phones to E.164, dedupes vs. existing last-10-digits in ONE query, insert_many. Returns {imported, skipped, failed, dates_dropped}. 627 rows in 0.24s. Frontend: year-less birthdays use 1904 (leap year, still <1930 so no age shown), sends batches of 150 with "Importing 150 of 627" progress, passes source=phone_import (personal ownership), error alert shows the REAL reason + how many already saved; CSV path also shows real detail. contactsAPI.importContacts(userId, contacts, source) timeout 120s.
- PHOTO (could not reproduce; hardened every plausible failure): utils/image_storage.put_object re-inits on 401/403 (stale cached storage key -> previously every upload failed forever with "invalid or corrupted image file" until restart); ImageDecodeError separated from storage errors; NEW inline_fallback (512px WebP data URI on users.photo_url, paths cleared, photo_storage_fallback=True) when object storage is unreachable so the photo ALWAYS saves; friendly 400 "That photo couldn't be read..." for real decode failures; real error logged as [ProfilePhoto]. NEW chunked path POST /profile/{uid}/photo/chunk {upload_id,index,total,data(base64<=700KB),content_type} -> Mongo photo_upload_chunks (TTL 1h index) -> assembled on last chunk -> same _save_profile_photo. ProfilePhotoUpload.tsx: multipart first (90s timeout); on 413/502/504/no-response/timeout automatically retries via chunks using expo-file-system File.base64() (already in the binary; no new native module).
- Tests: backend/tests/test_profile_photo_live.py (multipart 3000px, 13-chunk assembly, garbage file, simulated storage outage -> inline, simulated 401 -> re-init). Import verified via curl (mixed batch incl. 1900-02-29, emoji, blank name, dup format, junk; 627-row batch).
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Contact import + profile photo fixes"`.
- OPEN: if Kira still fails after both ships, get the EXACT alert text; "couldn't be read" = the photo file itself (HEIC/Live Photo without conversion).

## Import Preview + Reschedule From Reply (June 2026) - DONE, verified (34 live checks incl. real inbound webhook + screenshots)
- IMPORT PREVIEW: contacts.py `_prepare_import_rows` shared by POST /contacts/{uid}/import/preview (counts only) and /import. Stats {total,new,already_have,failed,dates_dropped,with_birthday,with_email,no_phone}. import.tsx: both "Import N Contacts" buttons -> openPreview(kind) -> bottom sheet "BEFORE YOU IMPORT / 589 new contacts / out of 627 selected" + rows (already have, birthdays, email, no phone, unreadable date, unreadable) -> "Import N contacts" runs the batched import (phone) or /import-csv/confirm (csv). testids import-preview-sheet/-title/-row-{icon}/-confirm-btn/-cancel-btn, csv-import-btn.
- RESCHEDULE FROM REPLY (user left design to defaults -> one-tap REP approval, consistent with the existing "Jessi never locks in a time" rule): services/appointment_changes.py. Webhook (`_sched_hooks` in twilio_webhooks.py) runs detect_appointment_change FIRST for any inbound text matching CHANGE_HINT or a schedule hint; task extraction only runs if no change request was created (prevents a duplicate 3 PM task). Detection: contact's nearest upcoming timed appointment (INVITE_TYPES, due >= now-1h) + gpt-5.2 prompt with existing appointment local time -> {action reschedule|cancel|none, local_datetime, confidence>=0.5}. Creates appointment_changes {task_id, action, old_due, new_due, old_label, new_label, customer_text, contact_first, status pending}, sets task.pending_change_id, conversation needs_assistance, notifications type appointment_change, push "Bud wants to move Thu 2:00 PM to 3:00 PM" -> /thread/{conv}. Verified LLM: "can we do 3 instead" -> same day 15:00; "push it to Friday" -> Fri same clock time; "running about 20 min late" (appt in 40 min) -> +20m; "can't make it" -> cancel; "are we still on for 2?" -> none.
  Routes (tasks.py): GET /tasks/{uid}/changes?conversation_id&contact_id&status; POST /tasks/{uid}/changes/{id}/approve (body optional {new_due}); POST .../decline. Approve reschedule = move task (+reset customer reminder) + send_calendar_invite(reason=updated, force) "moved to" text; approve cancel = task status dismissed (dismiss_reason customer_cancelled) + text "No problem Bud, I've taken {when} off the books..."; both cancel Jessi's held ai_reply_queue drafts (no double text), dismiss you_are_needed/appointment_change notifications, clear needs_assistance. Decline = status declined, task untouched. GET /tasks/{uid}/contact/{cid} tasks include `pending_change`.
  UI: thread/[id].tsx orange card "BUD ASKED TO MOVE THE APPOINTMENT / old -> new / quote" with "Approve & send update" + "Suggest another time" (cancel: "Take it off the books" / "Reply myself"); Jessi draft card hidden while a change is pending; polled with loadPendingDraft every 4s (testids appointment-change-card/-summary, approve-change-btn, decline-change-btn). ContactTasksCard chip "Bud asked to move it to ... tap to approve" -> thread (testid contact-task-change-chip). Also fixed pre-existing thread bug: `colors.card` undefined in light mode -> colors.surface.
- Tests: backend/tests/test_reschedule_live.py (cleans 500-555-0188 leftovers first; older conversations with the same test number hijack the webhook match).
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Import preview + reschedule from reply"`.

## "Authentication required" on Import for Self-Signed-Up Users (June 2026) - FIXED, verified
- USER (Jess, prod screenshot): Import 625 Contacts -> "Could not preview: Authentication required" (401 from enforce_user_ownership middleware in server.py).
- ROOT CAUSE: store/authStore.ts `signup` stored a FAKE token `token_${user._id}` (backend /auth/signup never returned a JWT). verify_jwt_token rejects it -> every endpoint under PROTECTED_PREFIXES with an ObjectId in the path (contacts, tasks, messages, campaigns...) returned 401 "Authentication required"; unprotected screens worked so the app looked fine. The 401 interceptor only tried the cookie restore and then gave up -> stuck forever. Reproduced: fake token -> 401; real token -> 200.
- FIX: (1) /auth/signup now returns {**user, user, token: create_jwt_token(...)} + sets imonsocial_session/imonsocial_uid cookies like /login (password stripped, feature_permissions merged). (2) authStore.signup uses res.token (fallback: authAPI.login) and never fabricates a token. (3) services/api.ts 401 handler: if detail == "Authentication required" and the in-memory token is missing/not a 3-segment JWT/starts with token_ -> logout() (tabs layout redirects to /auth/login); a real JWT rejected twice -> logout too. IMMEDIATE WORKAROUND for affected users before the OTA: log out and log back in (login issues a real JWT).
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Fix sign-up sessions: real token, auto sign-out on dead session"`.

## Face ID Without Re-Entering Password (June 2026) - DONE, verified backend (7 cases) + screen renders
- USER: "If I'm already logged in, why do I have to re-enter my password to turn on Face ID? I don't remember it." OLD: biometrics stored email+PASSWORD in SecureStore and replayed /auth/login.
- NEW (token-based): POST /api/auth/refresh (Bearer or body token) -> fresh 30-day JWT + user + cookies via shared `_session_response` (also used by /auth/me). Accepts tokens expired <= 90 days (biometric unlock is the 2nd factor); rejects token_/mock_, bad signature, deactivated users, >90d ("It's been a while. Please log in with your password once."). Frontend: BiometricCredentials {email, token?, password?(legacy)}; Security toggle = Face ID confirm only, stores current store token; login.tsx/cs-login.tsx biometric login -> authStore.loginWithToken(token) (legacy password entries still honored); refreshBiometricToken() silently re-saves the newest token after every login/refresh so it never goes stale; post-login "Enable Face ID" stores the token, not the password. Security note copy updated ("Your password is never stored...").
- NOTE: preview JWT_SECRET is only 12 bytes (pyjwt warns <32). Production may differ; changing it logs everyone out, so left alone.
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Face ID setup without password"`.

## Schedule Status Pill "Active until 12:08 PM" (June 2026) - FIXED, verified (API + screenshots)
- USER (prod): "why does it say I'm only available till 12:08 when my schedule is set to six? wrong time zone?" NOT a timezone bug: `(Active until 12:08 PM)` was the MANUAL "+2 hours" override (Override: Available Right Now) tapped at 10:08, shown as if the schedule ended then; it also kept showing after expiring (label used raw available_override_until + device toLocaleTimeString). Also "Respect My Schedule" was OFF in the screenshot -> schedule not enforced at all (always reachable).
- FIX: GET /schedule/status/{uid} (and POST /schedule/me/override) now return `availability_detail()` computed server-side in the REP's resolved tz: {available, enforced, timezone, local_time, today_hours "9:00 AM–6:00 PM", available_until (block end, or override end if later / off-hours), override_until (only if still in future), override_until_label, next_window (formatted, e.g. "Tue 9:00 AM")}. next_available_window + until_end_of_day override now use resolve_user_tz (saved tz "UTC" no longer computes in UTC). schedule.tsx pill: "Available now · Today 9:00 AM–6:00 PM / Available until 6:00 PM (manual override on) · 10:43 AM Denver"; not enforced -> "Respect My Schedule is off, so you are always reachable"; off shift -> "Back Tue 9:00 AM". Orange banner "Manual override keeps you available until 12:42 PM, then your weekly hours take over. [Turn off]" only while an override is active (testids schedule-status-pill/-detail, schedule-override-banner/-clear).
- Availability math for routing/pushes (is_user_available) was already tz-correct; unchanged.
- SHIP: backend Deploy + `cd frontend && eas update --branch production --message "Clear schedule status: available until, override banner"`.
