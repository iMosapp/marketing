# i'M On Social — Relationship OS (PRD)

## Original Problem Statement
Phase 2 "Relationship OS" UX enhancements: robust backend security, intent-based detection engine, live inventory tracking, ADF/XML lead intake. Native iOS/Android UX (Expo), Twilio voice/SMS routing, long-term campaign tracking, AI personalization, strict RBAC, and "no brain power" UI layouts for sales reps.

## Environments
- Preview: REACT_APP_BACKEND_URL in /app/frontend/.env (backend under test)
- Production: https://app.imonsocial.com — user pushes frontend via `eas update --branch production --message "msg"` (give ONLY the one-liner). Backend goes live when user hits Deploy.
- IMPORTANT: EXPO_PUBLIC_BACKEND_URL in frontend/.env must point to https://app.imonsocial.com before any `eas update` (it gets baked into the OTA bundle). Temporarily point it at the preview URL only for screenshot testing, then revert + restart frontend.

## Implemented (key items)
- AI auto-replies (per-chat + global AI ON/OFF master switch), Twilio routing, voice memos → AI profile extraction (newest memo wins)
- Live inventory webhooks, keyword auto-tagging, call playback, ADF/XML lead intake
- Contact profile refactored into /app/frontend/components/contact/*
- Touchpoints swipe actions (right=Done, left=Snooze) + Undo snackbar (backend undo in tasks.py)
- Send Photo to many (quick-send/photo.tsx), industry-neutral naming
- QR everywhere: login screen "My Card QR", Home header 1-tap QR, Card tile long-press
- Wallet pass pipeline (backend/routers/wallet_pass.py): Apple .pkpass signing + Google save-link — returns 503 until certs are in .env (APPLE_TEAM_ID, APPLE_PASS_TYPE_ID, APPLE_PASS_P12_B64, APPLE_PASS_P12_PASSWORD, APPLE_WWDR_PEM_B64)
- **QR Scan Counter (June 2026, this session)**: `card_scans` collection — unique visitor/day dedupe (sha256 of IP+UA), owner self-views excluded via Bearer token check. `GET /api/card/scan-stats/{user_id}` → {week, total}. Displayed as "X scans this week · Y all-time" pill in UniversalShareModal QR view (home header QR / Card QR). Tested: dedupe, self-view exclusion, UI render all verified on preview.
- **Contact Picker 50-Cap Fix (June 2026)**: All contact pickers previously loaded only the first 50 contacts and filtered locally — contacts beyond 50 were unfindable (user hit this on Voice Note picker). New shared hook `/app/frontend/hooks/useContactSearch.ts` does debounced server-side search (mirrors dialer's proven pattern; handles numeric queries + multi-word names). Wired into 9 pickers: home.tsx (ContactActionModal call picker, action picker/Voice Note, Send-a-Card picker), quick-send/photo.tsx, touchpoints/add-task.tsx, tasks/new.tsx, inbox.tsx new-conversation, contact/new.tsx referral, contact/[id].tsx referral. Verified: contact #62 "jimmy" found via Voice Note picker; clearing search restores default list.
- **"What to Send" Draft Sheet (June 2026)**: Home "Do This Next" / My-3 taps previously dumped users on the contact page with no guidance. Now they open a bottom sheet showing full "Why now" reason + an AI-drafted ready-to-send text (GET /api/home/draft/{user_id}/{contact_id}?reason=<reason_key> in home_intelligence.py, gpt-5.2 via Emergent LLM key, per-reason template fallbacks). Buttons: "Use This Message" (opens thread with composer prefilled, marks My-3 complete), "Different Message" (regenerate), "View Contact". Done My-3 cards still open contact page. Verified e2e on preview: sheet, AI draft (~2s), thread prefill all working.
- **Inbox tab reorder (June 2026)**: Waiting chip moved next to Hot (Hot → Waiting → All → AI → Active → Closed) per user request. Verified on preview.
- **Em-dash ban (June 2026)**: User NEVER wants em/en dashes in AI output (dead AI giveaway). Hard filter `no_em_dash()` in /app/backend/utils/text_sanitize.py (em/en dash → comma; digit ranges → hyphen) applied to EVERY AI text path: ai_reply.py (auto-reply + 2 follow-up writers), ai_campaigns.py (generate/reply/preview), messages.py (suggested reply), chat_widget.py, jessie_service.py, ai_outreach_service.py (suggestions + fallbacks), home_intelligence.py draft (+ prompt rule + fallbacks cleaned), va_profiles.py preview, home.tsx frontend fallback. RULE FOR FUTURE AGENTS: any new AI text generation MUST wrap output in no_em_dash(). Verified: unit tests + 3 live draft generations all clean.
- **Draft Everywhere / Write It on Touchpoints (June 2026)**: Draft sheet extracted to shared /app/frontend/components/DraftMessageSheet.tsx (home.tsx now uses it too). Touchpoints task cards gained a gold "Write It" button (Call | Write It | Text) that opens the sheet with reason 'touchpoint' (or birthday/anniversary by task type) + task title/description passed as `context` to the draft endpoint (new optional param). Backend: DRAFT_BRIEFS/DRAFT_FALLBACKS gained 'touchpoint' key. Verified e2e on preview: overdue task "Bud" → context-aware draft referencing his 2024 Tahoe; home sheet regression OK.
- **Auto-Complete on Send (June 2026)**: Draft sheet passes task_id → thread ?taskId=; thread/[id].tsx completes the task (PATCH /tasks action=complete, once via ref) after a successful send (both personal-SMS and regular flows). ALSO FIXED pre-existing bug: opening /thread/{contact_id} set actualConversationId to the contact id because GET /messages/thread/{contact_id} returns [] (200) — threads showed "No messages yet" despite history and sends failed "Conversation not found". Fix: if no messages AND conversation info 404s, fall through to find-or-create conversation. Verified e2e: Bud's history loaded, message sent, task auto-completed in DB.
- **Banned Words (June 2026)**: New persona.banned_words field (UserPersona model + persona wizard step 3 UI box, red label). Enforced two ways: injected into AI clone prompt + hard-stripped via strip_banned()/clean_ai_text(user_id) in utils/text_sanitize.py, applied at ai_reply (3), ai_campaigns (3), messages suggest, jessie, outreach, home draft. Verified: filter strips phrases (3/3 clean live drafts), settings box renders + loads saved value.
- **My VA rename + go-to phrases optional (June 2026)**: "Virtual Assistant" renamed to "My VA" everywhere (VA screen header, card name, Hub menu, my-profile button, persona wizard link). custom_phrases REMOVED from VA completeness scoring (now X of 11 — user's real missing item was go-to phrases, NOT payment link; payment link never counted). Root cause of user deleting phrases: AI overused them — clone prompt now says use sparingly, never force. persona.tsx labels go-to phrases "(optional)". Verified on preview.
- **Global iOS Dynamic Type cap (June 2026)**: User's iPhone large system font blew up layouts everywhere ("Next" wrapping to "Nex/t", phone numbers wrapping). Text.defaultProps is DEAD in RN 0.81/React 19. Fix: custom Babel plugin /app/frontend/babel-plugin-max-font.js registered in new /app/frontend/babel.config.js (babel-preset-expo + plugin) — injects maxFontSizeMultiplier={1} (user chose full 1.0 lock: app ignores phone text-size entirely) into EVERY <Text>/<TextInput>/<Animated.Text> at compile time; explicit props and spreads still win. All older per-element maxFontSizeMultiplier props (leads, home, dialer, report-bug, bug-reports, date-recipients, inventory, NotificationBell) removed via sed so nothing overrides the global 1.0. Verified via babel transform test + web smoke + home renders post-edit. NOTE: babel config changes require Metro cache clear (rm -rf node_modules/.cache) + frontend restart. Real effect only visible on iOS device after eas update.

## Pending / Backlog
- P0 Wallet Activation: user gathering Apple certs — step-by-step instructions provided (Pass Type ID + Team ID + .p12 + WWDR G4 PEM). When received: base64 into backend .env, test /api/wallet/{id}/download-token flow.
- P1 Google Play .aab: user to upload build from expo.dev to Play Console (user action).
- P1 HomeNet/vAuto live inventory feed (awaiting dealer API credentials).
- P2 Rotate production secrets (Twilio, Stripe, Resend, Mongo, VAPID, OpenAI) before contractor handoff.
- P2 Zapier integration for generic lead payloads.
- P3 WhatsApp, Stripe partner invoices, enterprise DMS (CDK, Reynolds, Dealertrack).

- **Bulk CRM Push (June 2026)**: Contacts tab select mode gained a purple cloud-upload header button → "Push N Contacts to CRM" sheet (intake email + remember toggle). Backend POST /api/crm-push/{user_id}/bulk (max 50, one ADF email per contact via Resend, tags + activity per contact, returns {sent, failed}); GET /api/crm-push/{user_id}/settings returns saved crm_intake_email. Bulk route registered BEFORE /{user_id}/{contact_id} to avoid shadowing. Verified: validations (400s) + full UI flow on preview; live email delivery same caveat as single push.

## Notes for agents
- **AI ROUTING (June 2026) - MANDATORY**: before finishing ANY change to `routers/ai_reply.py`, `routers/twilio_webhooks.py`, `routers/messages.py` or `scheduler.py`, read `/app/memory/AI_ROUTING_RULES.md` and run `cd /app/backend && python tests/ai_routing_matrix.py` (28 cases; must be N/N). The app is live; the user was burned by regressions here. Never simulate the Twilio webhook with real numbers in preview (fires real SMS).
- **ADF alignment + Push to CRM (June 2026)**: (1) INBOUND fixes in lead_intake.py — ADF <street> parsed, contact now gets address_street/address_city/address_state/address_zip (was wrongly city/state/zip), `vehicle` set (was only vehicle_interest), VIN/Stock/Trade-in appended to notes, portal <id> → external_id (fixed ElementTree falsy-element gotcha: never use `el.find(x) or el.find(y)`, childless elements are falsy). (2) OUTBOUND new /app/backend/routers/crm_push.py — GET /api/crm-push/{user_id}/{contact_id}/preview returns ADF 1.0 XML + saved crm_intake_email (on user doc); POST sends the XML by email via Resend to any CRM intake address, tags contact "Pushed to CRM", logs contact_events. Frontend: components/contact/CrmPushSection.tsx in DetailsTab (anyone can push; email input + remember toggle + Copy ADF XML). Round-trip tested: ADF in → contact fields correct → ADF out valid. Actual Resend delivery NOT live-tested (user has no CRM intake address yet; same Resend pattern as 5 working email paths). Also fixed pre-existing server.py lint bugs: duplicate "$ne" dict keys (referral queries matched None) and shadowed get_db in startup index block. Test contact: Sally Tester 6a9079eff2de796445f2a0e9 (preview).
- **Scan Trend Graph (June 2026)**: /api/card/scan-stats/{user_id} now also returns `days` (last 7 days, {day, label, count} via aggregation on card_scans.day). UniversalShareModal QR view renders mini bar chart under the scan pill (today highlighted, counts above bars). Verified: backend buckets + UI render on preview with seeded data (seed removed after).
- User language: English. Keep replies short; for production pushes give ONLY `eas update` one-liner.
- Metro bundler can serve stale bundles — restart frontend supervisor if changes don't appear.
- RN-web may not expose data-testid on TouchableOpacity in header; click by coordinates in screenshot tests.
- Admin login: forest@imosapp.com / Admin123! (see /app/memory/test_credentials.md)
- **Contact Profile Redesign + Auto-Updating Intel (June 2026, this session)**: User approved mockup blend A ("Briefing First") + C (quick actions row). (1) BACKEND: GET /api/contact-intel/{user_id}/{contact_id} now returns `stale` flag — compares cached generated_at vs newest of messages.created_at / contact_events.timestamp / voice_notes.created_at (_latest_activity + _to_utc_dt helpers handle str/datetime mix). No cache + activity → stale:true; no activity → stale:false. (2) FRONTEND contact/[id].tsx: cached intel loads instantly on open; if stale, refreshIntel() silently regenerates (intelBusyRef guard, "Updating…" spinner → "Updated just now"). Removed dead GET /api/intel/ 404 effect (was the pre-existing console 404), old generateIntel/showIntel/intelGenerating. (3) NEW components/contact/IntelBriefingCard.tsx: gold-bordered card under hero — RELATIONSHIP INTEL header + live status, parsed Quick Take (bold), 2 Key Facts bullets, gold "Next:" flag row (from Before Your Next Interaction), "Full briefing" expand → IntelRenderer + data points + manual Refresh. Empty state: "Intel builds itself…" + Generate now. (4) NEW components/contact/QuickActionsRow.tsx: 5 round gold-icon buttons Text/Call/Email/Note/Task (Note = voice memo record toggle, red while recording; Text/Email set composer mode + focus via new inputRef prop on ComposerBar; Task opens AddTaskModal). (5) HeroSection: removed blue call + green mic buttons (now in QA row) and italic intel one-liner. FeedTab: removed old intel pill + Update button. Verified e2e: stale→auto-regen→fresh briefing on Bud, empty state on Sally, Task modal, Email mode switch, expand/collapse.
- **AI Auto-Reply Suppression Bug FIXED (June 2026, this session — PROD RCA)**: User reported AI auto-replies not sending in PRODUCTION despite "Jessi is handling this". Deployer debug confirmed: NOTHING was queued (no ai_reply_queue docs), "You're Needed" escalation fired instead. ROOT CAUSE (reproduced in preview): triple suppression in twilio_webhooks.py + ai_reply.py — (1) `conv_ai_off` treated stale `ai_enabled: False` as AI-off even when ai_mode='auto_reply' explicit; (2) broken `enrollment_ai_queued` any() comprehension forced campaign=None and read stale enrollment ai_assist_mode, wrongly skipping the conversation-level AI block; (3) same stale-flag bug in process_ai_reply_queue's rep-override check cancelled queued items with cancel_reason='ai_disabled_by_rep'. FIXES: explicit active ai_mode (auto_reply/draft_only/auto_with_approval) now ALWAYS wins over stale ai_enabled=False in all 3 places; conversation-level 'auto_reply' also overrides campaign mode in the enrollment loop (no approval gate); enrollment_ai_queued now tracked from actually-fired modes; added diagnostic logs ("No AI queued for conv...", "AI suppressed...", "Mode off — not queuing"). Verified e2e in preview via simulated Twilio webhook (POST /api/webhooks/twilio/incoming, no signature check): before fix = 0 queue docs (bug reproduced); after fix = queued auto_reply → scheduler sent REAL Twilio SMS (mocked:False) → ai message logged. You're-Needed escalation still fires in parallel (unchanged). Also HARDENED the Home master AI switch (was a suspect, not the cause): confirm dialog before pausing, red "AI PAUSED" pill (was subtle gray "AI OFF"), amber "All AI is paused (Home switch)" banner in thread with Resume AI button, and backend now sends notification+push when a customer replies while master-paused (was a silent drop in queue_ai_reply). NOTE: FIX IS IN PREVIEW ONLY — needs production Deploy to take effect. Test data cleaned up, forest's default_campaign_id restored.
- **Home Lead Alert + Thread Gold Polish + Weekly Wins (June 2026, this session)**: (1) HOME LEAD ALERT: GET /api/leads/awaiting/{user_id} (lead_intake.py, before ROI section) returns {count, oldest:{conversation_id, contact_name, received_at}} for active is_internet_lead convs with no human reply. components/home/LeadAlertCard.tsx = red card top of Home ("1 lead waiting 6m" + Respond btn → oldest thread), refetch on focus + 30s. (2) THREAD GOLD CONVERSION: thread/[id].tsx file-local COLORS.accent AND light/dark theme branches → '#C9A962'; userBubbleBg gold + BLACK text; header call/mic → colors.surface circles w/ gold icons (recording stays red); avatar initials gold on surface; Take Over banner, send button (gold when twilio number, black icon), intel refresh, SMS-prompt (green) all de-blued. MessageBubble.tsx media text uses colors.userBubbleText. (3) WEEKLY WINS: GET /api/home/weekly-wins/{user_id} (home_intelligence.py top) = prev Mon-Sun counts {sold: contacts.date_sold, texts: messages sender=user, scans: card_scans.scanned_at, new_contacts: contacts.created_at}. components/home/WeeklyWinsCard.tsx shows ONLY on Mondays (or /home?wins=1 override; hidden when all zeros), placed after quick-action tiles. (4) SPEED LEADERBOARD: already existed as 'Speed' tab in leads.tsx (uses /api/leads/analytics/response-times) — verified renders; converted ALL leads.tsx raw data-testid → testID+dataSet. (5) FIXED phantom bubble bug (testing_agent find): GET /messages/thread excludes sender='ai_draft' + timestamp:None docs (sibling-conv drafts rendered as live-clock inbound bubbles). (6) conversations list now also returns lead_received_at. Tested via testing_agent iteration_287 (backend 10/10, frontend 90%) + all reported issues fixed and re-verified (thread header fully gold, drafts filtered, gold bubble + black text confirmed). Test data cleaned up.
- **Speed-to-Lead Timers (June 2026, this session)**: User-selected scope: Inbox rows + Thread banner (5/15 min thresholds, HUMAN-reply-only stops the clock — AI/auto texts sender='ai' never stop it; matches existing _first_human_replies logic in lead_intake.py). Push alerts already existed (instant rep push + workflow blast + 15-min manager slow-lead alert). NEW: (1) messages.py get_conversations enriches is_internet_lead convs with `awaiting_first_reply` (batch via _first_human_replies); get_conversation_info returns is_internet_lead/awaiting_first_reply/lead_received_at/lead_source_name. (2) components/LeadWaitTimer.tsx: self-ticking LeadWaitChip (30s tick, inbox nameRow) + LeadWaitBanner (15s tick, thread under search bar) — green <5m / amber <15m / red 15m+. (3) thread/[id].tsx: leadWait state set from convInfo, banner hidden on send success (both personal-SMS and regular flows setLeadWait(null)); backend flips flag permanently once human message stored. Verified e2e on preview with seeded 8-min-old lead: chip amber "Waiting 10m" in inbox, banner in thread, AI auto-reply did NOT stop timer, human send cleared banner + awaiting→false. Test data cleaned up. NOTE: /inbox URL on web serves a MARKETING page — the app inbox tab must be reached via tab bar navigation in tests. Home alert card built in the following task (see Home Lead Alert entry).
- **Contacts Page Redesign (June 2026, this session)**: Full modern rebuild inspired by Attio/Clay/Dex (user approved "blend A+C" from 3 generated mockups). Smart List cards with live counts (Needs Attention = last activity 14-90d ago, Hot Leads = tag 'hot', New This Week, Birthdays next 30d) via GET /api/contacts/{user_id}/smart-lists + smart_list= filter param (route defined BEFORE /{user_id}/{contact_id}). Rows: recency dot on avatar (green<=7d/amber<=21d/red>21d from last_activity_at), relationship subtitle "2d ago · 2023 F-150" (phone fallback), status pill (VIP/Sold/Hot/Referral/Prospect), gold sparkle AI-draft button -> DraftMessageSheet. Swipe left = Text/Call/Email (native: RNGH Swipeable, web: WebSwipeableItem + wasRecentSwipe() press guard). Filters collapsed into bottom sheet (Mine/Team, Recent/A-Z, CRM status, tags) with gold count badge on search-bar icon. Tag + CRM filters now SERVER-SIDE (tag=/crm= params) fixing count mismatch; A-Z uses case-insensitive collation; last_activity_at enriched in ALL branches; ran POST /api/admin/backfill-last-activity on preview (MUST run once on production after deploy). Smart bar hidden in team view. RN-web drops raw data-testid props -> use testID + dataSet={{testid}} pattern (KEY LEARNING for all future components). Files: app/(tabs)/contacts.tsx (rewritten), components/contacts/{SmartListBar,ContactRow,ContactFilterSheet}.tsx (new), routers/contacts.py. Tested via testing_agent iteration_286 + all reported issues fixed and re-verified. (Old GET /api/intel/ 404 console noise FIXED in Contact Profile Redesign session — dead effect removed.)
- **Lint cleanup (June 2026, this session)**: deleted dead server_old_backup.py/server_v1_backup.py, fixed missing get_db/os imports in scheduler.py, removed shadowing asyncio import in voice_notes.py, added missing useRouter in app/opt-in/[cardId].tsx.
- **Birthday Capture (June 2026, this session)**: PATCH /api/contacts/{user_id}/{contact_id}/birthday (lightweight set/clear, stores BSON date, 400 on bad format). Contact profile Details tab: "Important Dates" section now ALWAYS renders with an orange "Add birthday — unlocks auto birthday texts" row when missing (opens BirthdayModal in DetailsTab.tsx: web <input type=date>, native DateTimePicker spinner, Save/Clear); existing birthday row is tappable to edit. Contacts list: purple gift chip "🎁 Xd / Today!" on rows when birthday <=30d away (daysUntilBirthday exported from ContactRow.tsx); sparkle AI-draft auto-switches to 'birthday' reason ("Why now: Birthday in X days", gift icon) — verified real LLM birthday message. Once birthday is set, the pre-existing "Birthday text + card" auto-send opt-in toggle appears. Demo birthdays set on preview: Sally Tester, Bud, John Dealer. Verified e2e via curl + screenshots.
- **Birthday Import + Campaign Split (June 2026, this session)**: (1) Device import now pulls birthdays: added Contacts.Fields.Birthday to expo-contacts fetch in app/contacts/import.tsx, maps {day,month(0-based),year||1900} -> ISO, shows purple gift line in preview + "auto texts stay OFF" note, sends in import payload (backend ContactCreate already had birthday; stores BSON date; tags stay [] so NOTHING auto-sends — user explicitly required this). CSV/VCF already parsed birthdays (csv_import.py _parse_birthday/BDAY). (2) Split view: smart_list values birthdays_on (birthday + tag ~birthday case-insens) / birthdays_off (birthday + no tag) added to _smart_list_filter + counts. Contacts page: tapping Birthdays card shows sub-chips "Upcoming | Auto-text ON · n | No auto-text · n" + gold "Manage" link -> existing /settings/date-recipients bulk opt-in screen (backend GET/POST /{user_id}/date-optins[,/bulk] pre-existed). ContactRow gift chip shows green bell when enrolled. (3) FIXED recency bug: stored last_activity_at could be stale (some event writers bypass utils/contact_activity.log_contact_event) — page enrichment in get_contacts now always takes max(stored, computed-from-events) for the page ids, and backfill_last_activity_at also repairs stale (not just missing) values ($lt filter). Re-ran backfill on preview (repaired 7). REMINDER: run POST /api/admin/backfill-last-activity on production after deploy. All verified via curl + screenshots (split chips, Manage nav, import w/ birthday tags=[]).
- **Dates Calendar (June 2026, this session)**: User-approved hybrid design (mockups first this time — user was frustrated previous feature was built without mockup signoff; ALWAYS mock up before building for this user). New screen app/dates-calendar.tsx reachable from Hub > Manage > "Dates Calendar". Backend GET /api/contacts/{user_id}/dates-calendar?year&month returns all birthday/sold/anniversary events for the month (skips anniversary rows duplicating date_sold same month+day; years computed when source year 1930<y<now). Screen: month nav chevrons, custom month grid with colored dots (purple birthday / green sold / pink anniversary), tap day filters agenda (banner to clear), legend, filter chips with counts (All/Birthdays/Sold/Anniv), day-grouped agenda rows (type icon + avatar + "Birthday · Sep 2 · turns 41" / "Sold 2023 F-150 · Jun 9 · 2 yrs"), TAPPABLE "AUTO ON/OFF" pill = optimistic enrollment toggle via existing POST date-optins/bulk (occasion birthday|anniversary; sold events map to anniversary occasion), row tap -> /contact/{id}. Verified e2e: month nav, dots, day select, pill toggle round-trip persisted, contact navigation.
- **Home Redesign Hybrid (June 2026, this session)**: User approved via mockups (option C hybrid). app/(tabs)/home.tsx: (1) Greeting header "Good morning/afternoon/evening, {first_name}" + left-aligned Home title. (2) HomeSmartBar (components/home/HomeSmartBar.tsx, same style as contacts SmartListBar): Today X/3 (scrolls to Your 3 via scrollRef+my3Y onLayout), Overdue (-> touchpoints), Hot Leads (-> /contacts?smart=hot; contacts.tsx now accepts `smart` route param), Sold This Mo (-> /sales-list?type=sold). Hot count from contactsAPI.getSmartLists. (3) Old 6-tile grid + Make a Call bar REMOVED; user then requested visible quick actions back -> compact 1-row tiles under hero: SOLD! (gold hero tile), Contact, Card, Review. (4) QuickActionsFab (components/home/QuickActionsFab.tsx): gold + FAB bottom-right (MUST have zIndex:100 — styles.scroll has zIndex:1 which otherwise covers it) opening speed dial: Dates Calendar, Send Photo, Voice Note, Make a Call (user: "quick action dropdown is a killer idea", wants users kept on Home/Contacts/Inbox, out of Hub). All verified via screenshots: tiles render, FAB opens 4 actions, Dates Calendar navs, hot card navs to filtered contacts. USER PREFERENCE: keep daily tools reachable from Home/Contacts/Inbox, avoid burying in Hub.
- **Calendar + Tasks/Appointments (June 2026, this session)**: "Dates Calendar" renamed to **Calendar** (Hub + Home FAB labels already said Calendar). app/dates-calendar.tsx REBUILT: merges date events (birthday/sold/anniv) with tasks from GET /api/tasks/{user_id}/calendar?year&month (tasks mapped UTC→local day client-side; gold dots on grid; Tasks legend + "Tasks · n" filter chip; agenda task rows show time · type · contact with gold-bordered card, complete-circle → PATCH /tasks {action:'complete'} optimistic w/ strikethrough). Gold + header button + empty-state "Add a task" open components/CalendarTaskModal.tsx (title, ApptTypeRow, DateTimeField date+optional time, debounced contact search). VERTICAL-AGNOSTIC types per user (revised twice, final): APPT_TYPES = **call | text | appointment | task** (user: "meeting is a lot like an appointment" — delivery/meeting/test_drive removed from UI+AI prompt; legacy stored values still display their labels, test_drive shows as "Appointment"). AI extraction enum call|text|appointment|task, invalid → 'task'. Backend: get_calendar_tasks now EXCLUDES type campaign_send/campaign_step (was polluting calendar with 16+ auto campaign sends). AI call extraction (tasks.py extract_appointment_from_call, hooked in twilio_webhooks + calls.py) verified live: transcript "Wednesday at 2pm works" → gpt-5.2 → task Wed 2:00 PM local (user tz from login Intl auto-detect), dedupe ±45min, push+notification. send_task_reminders (scheduler, 60s): 15-min-before + at-time pushes for has_time tasks, reminded_15/reminded_due flags. AddTaskModal (contact page) de-blued to gold. RN-web NOTE recurrence: raw data-testid on TextInput gets DROPPED — must use testID+dataSet (fixed in CalendarTaskModal). All flows verified e2e on preview: add w/ contact+time+type, agenda render, complete, month nav, AI extraction. Test data cleaned.
- **Alerts Center Redesign + Cleanup (June 2026, this session)**: User picked agent's blend (mockups generated + approved "whatever is cleanest"). BACKEND notifications_center.py REWRITTEN: feed split — GET /api/notification-center/{uid}?feed=for_you|activity&category=all|urgent|leads|replies|appts. FOR_YOU_TYPES map classifies db.notifications (you_are_needed/slow_lead→urgent; jump_ball/new_lead/lead_assigned/new_demo_request/engagement_signal/keyword_alert→leads; customer_reply*→replies; appointment_extracted/task_reminder→appts) + overdue/due-soon tasks (campaign types EXCLUDED)→appts + unread convs→replies. ACTIVITY feed = contact_events (sms/email sent, link clicks, cards), campaign pending sends, flagged convs, date_trigger/milestone/ai_outreach/call_recorded/etc. UNREAD COUNT FIX: /unread-count now derives from same feed build (cached 30s per-user, filtered per request) — badge ALWAYS matches visible For You unread. read-all no longer sets dismissed (items stay visible as read). FRONTEND app/notifications/index.tsx REWRITTEN as "Alerts": gold Read All pill, For You·n | Activity·n segmented control (gold active/black text), smart filter cards (All/Needs You/Leads/Replies/Appts w/ icon circle+count, HomeSmartBar pattern), day-grouped (TODAY/YESTERDAY/date) gold uppercase headers, rounded card rows w/ gold left border + dot when unread. NotificationBell.tsx restyled gold (title "Alerts", chips/dots/markAll de-blued, new categories) — dropdown auto-clean via default feed=for_you. Verified e2e: feed split, counts, mark-read round trip, read-all→count 0, bell dropdown render (bell needs coordinate click in tests ~354,32).
- **Reply Health Check (June 2026, this session)**: GET /api/home/reply-health/{user_id} (home_intelligence.py) counts ai_reply_queue status='failed' last 24h ($or assigned_user_id/user_id). components/home/ReplyHealthCard.tsx = amber warning card on Home under LeadAlertCard ("N AI replies didn't send · tap to review" → thread of latest failure or inbox). Hidden when 0 failures. Verified with seeded failure.
- **"All Good" Waiting Clear (June 2026, this session)**: User couldn't exit the Waiting queue without Take Over→re-enable AI dance. NEW: green "All Good ✓" button (1) on inbox Waiting-tab rows next to YOU'RE NEEDED/Take Over badges, (2) in thread gold "Jessi is handling this" banner (shown when convInfo.needs_assistance). Calls PUT /api/messages/conversations/{uid}/{convId} {needs_assistance:false} — backend (messages.py update_conversation) now on explicit needs_assistance:false also zeroes unanswered_customer_replies + dismisses you_are_needed notifications for that conversation; AI mode/enabled UNTOUCHED. get_conversation_info now returns needs_assistance + unanswered_customer_replies. Verified: authed PUT round-trip (flag+counter cleared, ai_mode auto_reply preserved, notif dismissed), UI buttons render on waiting rows.
- **Waiting Digest (June 2026, this session)**: db.waiting_clear_log audit {user_id, conversation_id, reason 'satisfied'|'expired', cleared_at} written by BOTH auto-clear paths (twilio satisfied block + scheduler expire job). weekly_wins endpoint returns waiting_cleared (last Mon-Sun count); WeeklyWinsCard shows green footer "N waiting alerts cleared themselves — no action needed" (testid wins-waiting-cleared); Monday wins push body appends "N waiting self-cleared" (doesn't gate the push). Verified: endpoint returned 3 w/ seeded logs, card footer rendered on /home?wins=1, seeds cleaned.

- **Waiting Auto-Expire (June 2026, this session)**: scheduler expire_stale_waiting_flags (hourly IntervalTrigger, id expire_stale_waiting): stamps you_are_needed_at=now on flagged convs missing it, then clears needs_assistance+counter on convs where you_are_needed_at < now-3d and dismisses their you_are_needed notifs. Quiet — no pushes. Verified: preview run cleared 3 stale flags immediately, stamped 11 legacy unstamped (expire in 3d). Backend-only → next Deploy.

- **Auto-Clear Waiting on Happy Reply (June 2026, this session)**: twilio_webhooks.py — new _is_satisfied_reply(text) heuristic (≤80 chars, no '?', no _NEG_WORDS pushback; _SAT_WORDS tokens via regex word-split + _SAT_PHRASES substrings; "no problem/worries" stripped before neg check; 15/15 unit cases). In /webhooks/twilio/incoming after unanswered increment: if satisfied AND conv AI active (ai_enabled≠False, ai_mode not off/draft_only/assisted) → needs_assistance=False, unanswered=0, dismiss you_are_needed notifs, conv_unanswered=0. Escalation block gated `and not is_satisfied`; generic notify-rep block: notif_type/priority/push icon follow satisfied state (happy reply logs normal "X replied" not urgent "needs you"). Verified via live webhook simulation (curl POST /api/webhooks/twilio/incoming, From=forest's cell +18016349122 To=+14352203414): flag+counter cleared, ai_mode auto_reply preserved, 0 new you_are_needed, AI reply still queued (cancelled in test cleanup to avoid real SMS). Backend-only → ships with next Deploy.

- **Alert Quiet Hours (June 2026)**: Overnight push silencing w/ morning summary. BACKEND: user_schedules gained overnight_quiet/overnight_start("21:00")/overnight_end("07:00") (ScheduleUpdate + PUT/GET /api/schedule/me + serialize). New is_quiet_now(user_id) in user_schedule.py = overnight window (local tz, handles cross-midnight) OR legacy notification_quiet off-shift; available_override_until wins. send_push_to_user (push_notifications.py) now HOLDS pushes into db.held_pushes (cap 50/user) during quiet instead of silently dropping (old is_user_available gate REPLACED — schedule quiet mode also gets summaries now). scheduler deliver_held_push_summaries (every 10 min): quiet ended → 1 held = original push as-is; 2+ = "While you were away — N alerts" w/ first 3 titles → /notifications; marks delivered; prunes delivered >7d. FRONTEND settings/notifications.tsx: gold "QUIET HOURS" card (moon icon, Silent overnight toggle, Quiet from 8/9/10/11 PM + Until 6/7/8/9 AM gold chips) — saves via PUT /schedule/me with Intl-detected timezone. Verified e2e: quiet window → 2 pushes held → window ended → summary job delivered both (web-push key errors in preview = forest's stale test subscriptions, not pipeline). Forest's schedule state restored, test held_pushes cleaned.
- **Weekly Wins Push (June 2026)**: scheduler send_weekly_wins_push — Mondays 14:45 UTC (~8AM MT), pushes "Your week in review is ready" (body = "X sold · Y texts · Z card scans · N new contacts last week") → /home?wins=1 (opens WeeklyWinsCard forceShow). Skips all-zero weeks; dedupe via weekly_wins_push_log {user_id, week}. Job ran clean in test (1 push logged, log cleaned after).
- **Digital Card Photo + Black Bar + Inbox Bubble Fixes (June 2026, this session — USER-REPORTED w/ screenshots)**: (1) vCard "Save My Contact" now EMBEDS the profile photo as base64 JPEG (digital_card.py generate_vcard fetches bytes from http://127.0.0.1:8001{/api/images path}?format=jpeg via httpx, ≤900KB; iOS ignores PHOTO;VALUE=URI — that was 'photo not attaching'). (2) card/data absolutizes photo_url/cover/logo via _abs_url (PUBLIC_FACING_URL/APP_URL); card/[userId].tsx Image has onError→placeholder (was empty gold box when image 500/404'd). (3) Black bar on public card page: document.body/documentElement backgroundColor now synced to theme.bg on web (browser chrome/overscroll showed mismatched #000). (4) Inbox rich-card bubbles (thread MessageBubble.tsx): text was colors.userBubbleText (DARK) on dark contactBubbleBg → unreadable; now isRichContent uses colors.text; Digital Card label #5856D6 → #6FA8FF. SHIP: card page + vCard = backend/web Deploy; bubble fix = eas update. Verified: curl (abs URL + PHOTO;ENCODING=b in vcard) + screenshots (body bg synced, placeholder fallback, thread renders).
- **Broadcast Delivery Results (June 2026, this session)**: NEW `GET /api/broadcast/{id}/recipients?user_id=` (broadcast.py) — per-recipient results from campaign_pending_sends: name/phone/status(sent|failed|queued|unconfirmed)/error/deferred_reason; ownership-checked. broadcast/[id].tsx: 'Delivery Results' card (shown for sending/sent/failed) with color badges, failed rows show exact Twilio error, queued rows show 'Held: quiet hours — sends at 9 AM', refresh button (refresh-results-btn), rows tap → contact page; Sent/Failed stats now show for sending+failed statuses too (was sent-only). 'unconfirmed' = legacy pending_user_action docs (pre-fix, outcome unknown). Verified: curl endpoint + screenshot with seeded sent/failed/queued docs (cleaned up after).
- **Broadcast MMS Failure Fix (June 2026, this session — USER-REPORTED, PRODUCTION)**: "Blast to Jessica and Forest didn't go out." Deployer RCA: Twilio error 21620 — the composer pushed the RAW LOCAL iOS file:// path into media_urls (upload code was a stubbed TODO: `uploadedUrls.push(img.uri)`), Twilio can't fetch device paths; both sends attempted+rejected, docs marked pending_user_action with NO error stored, broadcast stuck status='sending' forever. FIXES: (1) broadcast/new.tsx now actually uploads photos to POST /api/images/upload (public, auth-exempt, Twilio-fetchable) and uses original_url; upload failure aborts with a clear alert. (2) broadcast.py send endpoint sanitizes media_urls (keeps http(s), prefixes /api/ paths with PUBLIC_FACING_URL/APP_URL, DROPS file:///data: junk) so old drafts self-heal to text-only. (3) scheduler.py: Twilio failures now persist status='failed' + error text on the pending doc, skip fake message/event logging; new _update_broadcast_progress() increments broadcasts.sent_count/failed_count and finalizes status sent/failed + completed_at when queue drains; broadcast sends marked 'sent' (was mislabeled pending_user_action since delivery_mode 'auto'≠'automated'). E2E-verified in preview with unroutable +19999999999 (no real SMS): file:// dropped, quiet-hours gate confirmed (deferred at UTC midnight → tz temporarily Auckland to test), Twilio 21211 → doc failed w/ error, broadcast finalized 'failed', all test data cleaned + tz restored. PRODUCTION NOTE: their stuck 'Test' broadcast stays status='sending' (harmless); after backend redeploy + eas update, user should DUPLICATE it and resend (sanitizer will drop the bad URL; re-attach photo for MMS).
- **Timezone Month-Rollover Fix (June 2026, this session — USER-REPORTED)**: "3 August sold deals vanished from Home (0) but sold screen shows them." ROOT CAUSE: `/users/{id}/sold-performance` anchored 'current month' to SERVER UTC (rolled to Sept 1 while user's phone was still Aug 31); sales-list uses client-local date → mismatch. FIX: sold-performance + sold-monthly-summary accept optional `month`/`year` params anchoring the series to the CLIENT's local date; home.tsx (2 call sites) + sales-list.tsx now pass local month/year. Verified via curl: unanchored=Sep 0, anchored Aug=correct counts. RULE: any 'this month/this week' server counting must accept a client-local anchor. NOTE: needs BOTH backend redeploy AND eas update to fully fix production.
- **Twilio Broadcast Replaces Quick Broadcast (June 2026, this session)**: User: "Quick Broadcast sends from my phone natively, not my tracking number; want to attach a list + blast to tags or hand-picked contacts." CHANGES: (1) Entry points rerouted — Hub 'Send a Blast' tile → /broadcast/new, My Brand card + my-profile row → /broadcast ('Mass text from your tracking number'); Quick Broadcast (/broadcast-message) has NO entry points now (file kept). (2) NEW 'Pick Specific Contacts' section in broadcast composer (search+checkboxes+chips, testids pick-contact-search/pick-result-*/picked-*); backend get_filtered_contacts: picked-only = just those ids, picked+tags = UNION; preview accepts contact_ids param; malformed ids → 400 (blast-radius guard). (3) CSV list upload: /contacts/import CSV flow has 'Tag this list' input (list-tag-input) → confirm endpoint ?list_tag= creates the tag doc + tags new AND matched-duplicate contacts ($addToSet via get_data_filter scope) so whole list is targetable in broadcast; composer refetches tags on focus. (4) Contacts search now matches full 'First Last' queries (contacts.py get_contacts). (5) Save Draft: canGoBack fallback → router.replace('/broadcast'). (6) Exclude-tags list collapsed by default (toggle-exclude-tags). (7) TESTER FIX kept: import.tsx was hardcoding prod URL + raw axios (CORS/401 on web) → now web-relative base + shared api instance; same guard applied to help.tsx and admin/brand-assets.tsx. Stagger stays 10s; sends go from rep's Twilio number via campaign_pending_sends. Tested: testing_agent iteration_289 (backend 10/10 pytest, frontend E2E all flows, NO real sends) + all follow-up fixes self-verified (400 on bad id, full-name search, exclude collapsed, pytest re-run 10/10). Needs `eas update` for phones. WARNING for future agents: never call POST /broadcast/{id}/send in tests — real SMS.
- **Sold Units Tracker (June 2026, this session)**: User asked for month-by-month sold history with MoM + YoY comparisons, all clickable from Home. Picture avatars added: sold list rows render contact photo_thumbnail over the gold initial (initial shows if photo missing/fails; avatar has overflow:hidden). iOS FIX (June 2026): photo_thumbnail is now ABSOLUTIZED server-side in sold-contacts (relative /api/... paths work on web but NOT native iOS — no origin to resolve against); backend-only fix, needs Deploy not eas update. BACKEND (server.py): new `GET /api/users/{id}/sold-monthly-summary?filter_type=&scope=me|team` (24-month aggregation on date_sold + year_totals; team scope for MANAGER_ROLES = super_admin/admin/manager/store_manager/org_admin — admins see all stores, managers their store via shared `_team_user_ids` helper); extended `GET /users/{id}/sold-contacts` with `scope=team` + `rep_name` per contact. FRONTEND: rewrote `app/sales-list.tsx` → ‹month› arrows with big total, compare chips (vs last mo +/-, same-month-last-year "15 → 20 (+33%)", year total), tappable 12-month bar chart (jump to month), Me|Team segmented toggle (managers only), team rows show rep name, contact tap → profile. Home entry points: SmartBar "Sold This Mo", "August Sales" Sold/Referrals/Repeats tiles (already routed), + WeeklyWinsCard "Sold" stat now tappable → /sales-list. Self-tested: curl both endpoints (me/team/list w/ rep names) + browser E2E (month nav, team toggle, contact open). Needs `eas update` to reach phones.
- **Copy-Link Fix Everywhere (June 2026, this session — USER-REPORTED BUG)**: "Copying links (error report / showcase / digital card) doesn't copy the actual link." ROOT CAUSE: ~20 copy handlers were WEB-ONLY (`navigator.clipboard`/`execCommand` gated behind Platform.OS==='web') — on the native iOS/Android app they copied NOTHING but still showed "Copied!". FIX: new shared helper `/app/frontend/utils/clipboard.ts` → `copyToClipboard(text)` (expo-clipboard `setStringAsync`, works native+web, web execCommand fallback). Rewired 22 files: UniversalShareModal (Copy Link + Share Link — native now opens real RN Share sheet with the URL), admin/error-reports (Copy All Reports), more.tsx, my-profile share chips (native = share sheet w/ actual link, was SMS-only), brand-assets, discount-codes, lead-sources x2 (webhook URL copy), ai-outreach, imos hub+app-directory, my-account, partner/onboard, quick-send copy method, settings/link-page (was OPENING the url on native instead of copying), broadcast-message, broadcast-card, settings/create-card success share, UniversalComposer, PresenceLinks, StoreManagement, contact/[id]. RULE FOR FUTURE AGENTS: NEVER write `navigator.clipboard` directly — always `import { copyToClipboard } from 'utils/clipboard'`. Tested via testing_agent iteration_288: 8/9 flows verified with real clipboard READ-BACK on web (correct URLs). ALSO: removed 597 lines of DEAD CODE in more.tsx (3 unreachable share modals — review/showcase/birthday — plus their handlers/states/styles/match-modal; no trigger ever opened them; Hub verified renders after); fixed latent crash: Delete Account used showAlert without import; added testID+dataSet to share-copy-link/share-via-link/error-reports-copy/header-qr-btn/my-profile share chips. KNOWN COSMETIC: RN-web console warning "Unexpected text node" on quick-send card screen (couldn't locate statically, no user impact). Frontend-only → needs `eas update` to reach phones.
- **Hub UI Formatting Cleanup (June 2026, this session)**: Clicked through Hub screens fixing theme inconsistencies in BOTH light+dark modes: email-signature.tsx, seo-health.tsx, geo-health.tsx, admin/hot-leads.tsx, admin/team-engagement.tsx, settings/schedule. Fixed hardcoded text/background colors + the recurring `colors.background` vs `colors.bg` bug (theme store exposes `colors.bg` — colors.background is undefined and renders white/transparent). Verified via screenshots both modes. USER CONFIRMED closed. Frontend-only → user runs `eas update --branch production` to ship.
- **Inbox Polish (June 2026)**: User approved hybrid via mockups. app/(tabs)/inbox.tsx: ROOT CAUSE of old clash was file-local COLORS_DARK.accent '#2E5CFF' (bright blue) -> changed to '#C9A962' gold (also COLORS_LIGHT.accent + borderFocus) which auto-fixed compose btn, checkboxes, toggle, empty states. Avatars: muted '#2C2C2E' bg + gold initials (was solid blue). Unread: gold left border on row (conversationItemUnread) + gold count bubble w/ black text. Rows: borderRadius 16, no bottom border. My/Team toggle: gold active + black text/icons. Tab pill bar REPLACED with smart-card filter bar (icon circle + count + label, same as Home/Contacts): Hot, Waiting, NEW 'unread' filter (added to activeTab union + filter switch + counts), All, AI, Active, Closed. AI-mode chip on rows: subtler ('off' = transparent + hairline border; assist = gold not blue). Converted conversation-item / my-inbox-toggle / team-inbox-toggle data-testid -> testID+dataSet. Verified: filters work, unread filter, row->thread nav, team view, AI toggle tap. NOTE: renderTeamConversation styles inherit the gold accent automatically.

## Stripe Integration (Resolved — June 2026)
- Stripe was already fully implemented (checkout, quote payments, /checkout/status, webhook, pricing UI).
- Root cause of "Payment system not configured": backend process (port 8001) did not have STRIPE_API_KEY in its env.
- Fix: added STRIPE_API_KEY="sk_test_emergent" (Emergent managed claimable sandbox) to /app/backend/.env and restarted backend.
- Verified live: POST /api/subscriptions/checkout returns a real https://checkout.stripe.com/... URL.
- Go-live: user claims the sandbox into their own Stripe account (no code change needed).

## iOS Avatar Rendering Fix (June 2026)
- Root cause: several screens used React Native core `Image` for REMOTE photo avatars. Renders on web, unreliable on iOS with newArchEnabled=true. Working iOS avatars (inbox/thread/more) all use `expo-image`.
- Switched to `expo-image` (contentFit + memory-disk cache): app/sales-list.tsx, app/l/[username].tsx, app/p/store/[slug].tsx.
- Left on core Image intentionally: local require() logo assets (imos/_components.tsx), import-guide screenshots, white-label (Image import unused).
- DELIVERY: frontend/JS change — requires `eas update --branch production` (+ app reopen). Backend deploy alone does NOT update the installed app's JS.

## Keyboard Covers Date/Time Picker Fix (June 2026)
- Bug: New Task form (calendar) auto-focuses title TextInput; tapping date/time button opened spinner picker WITHOUT dismissing keyboard -> picker hidden behind keyboard. iOS-only (web uses HTML date inputs).
- Fix: added Keyboard.dismiss() to date/time picker open handlers in:
  - components/DateTimeField.tsx (covers CalendarTaskModal + AddTaskModal)
  - components/AppointmentModal.tsx (date + time buttons)
  - app/contact/sold-wizard.tsx, app/sold-quick.tsx (sale date buttons)
- DELIVERY: JS change -> requires `eas update --branch production` + app reopen.
- Not reproducible in web preview / automated tester (native iOS keyboard behavior).

## Last Week's Wins — Tappable Drill-downs (June 2026)
- Before: only the "Sold" tile navigated (to /sales-list?type=sold); Texts/QR Scans/New were disabled.
- Now: all 4 tiles tap to a new screen /weekly-wins-list?type=sold|texts|scans|contacts.
- Backend: GET /api/home/weekly-wins/{user_id}/list?type=... (home_intelligence.py) — same Mon-Sun window as /weekly-wins so counts match.
  - sold: contacts with date_sold in window
  - contacts: contacts with created_at in window (shows source)
  - texts: distinct conversations user messaged in window (shows msg count), resolved to contact name via conversations.contact_id
  - scans: card_scans events in window (ANONYMOUS — no contact identity captured; listed by date)
- Frontend: app/weekly-wins-list.tsx (clone of sales-list pattern, expo-image avatars); WeeklyWinsCard.tsx tiles wired.
- Verified: backend curl all 4 types OK; frontend bundles clean. NOTE: screenshot tool cannot render this Expo web app headless (blank) — device confirmation after eas update.
- DELIVERY: backend deploy + `eas update --branch production`.

## Relationship Intelligence — Scope C (June 2026)
Built the "IMOS remembers the person" layer. 5 items + score:
1. Relationship Profile at top of contact card — ALREADY existed (IntelBriefingCard @ contact/[id].tsx line ~2539); verified in place.
2. People to Talk To Today feed — NEW screen app/people-today.tsx + endpoint GET /api/home/people-to-engage/{user_id}?limit (widened get_my_3 via top_n param). Home "See all →" link added to the "Your 3 for Today" header.
3. Relationship Health Score + Book of Business rollup — NEW router routers/relationship_health.py:
   - GET /relationship-health/{user_id}/summary  (bucket rollup)
   - GET /relationship-health/{user_id}/contacts?bucket=  (drill-down, worst-first)
   - GET /relationship-health/{user_id}/contact/{contact_id}  (badge, scoped by user_id)
   Buckets: opportunity(🔥 recent engagement/sold<30d) / at_risk(🔴 90d+) / cooling(🟡 45-90d) / advocate(💙 review or referrer) / connected(🟢 <45d).
   Last-touch derived from contact_events (real event types), NOT the never-written last_contacted_at. Advocate = review events + advocate/referral tags + name appears as a referrer (referred_by_name reverse lookup). 30s TTL cache.
   Frontend: app/book-of-business.tsx, components/home/BookOfBusinessCard.tsx (home), components/contact/HealthBadge.tsx (contact card).
4. Human-readable timeline — Feed tab already renders human event titles/descriptions; no change needed.
5. Touch tagging (Transactional/Promotional/Relationship) — GET /api/home/touch-mix/{user_id}?days heuristic classifier; surfaced as "X% real relationship touches this week" line in BookOfBusinessCard.

SECURITY FIXES (from test iteration 290 — BOLA/PII leak found & fixed):
- server.py enforce_user_ownership: added '/api/relationship-health/' to PROTECTED_PREFIXES AND changed extraction to scan the path for the FIRST ObjectId segment (covers /api/home/people-to-engage/{uid} & /touch-mix/{uid} & weekly-wins which were previously unauthenticated).
- health_one now filters db.contacts by user_id (was cross-tenant readable).
- Query validation: days ge=1 le=365, limit ge=1 le=50 (422 on bad input).
Verified: tests/test_relationship_health_290_auth.py 10/10 PASS (401 unauth, 403 non-owner). Functional contracts verified via curl with token.

DELIVERY: backend deploy + `eas update --branch production`. Frontend not visually testable in headless preview (Expo web renders blank in automation) — confirm on device.

## Next Action Items build (June 2026)
2. Voice "Remember This" auto-follow-up: services/voice_intel.py process_voice_note_intelligence now calls _ensure_followup_from_voice — after a voice note is transcribed+extracted, auto-creates ONE follow-up task (due +3 days, type=follow_up, source=voice_note) if none pending. Smart title from extracted interests/spouse/referral. (One-tap mic already existed in QuickActionsRow.)
3. Smarter feed reasons: home_intelligence.get_my_3 now projects personal_details and adds a `hook` field via _personal_hook() (e.g. "Loves Golf", "Spouse: Sarah"). Rendered in app/(tabs)/home.tsx My-3 cards and app/people-today.tsx.
4. Advocate Spotlight: GET /api/relationship-health/{user_id}/advocates (auth-protected). New screen app/advocates.tsx ("Say Thanks" flow), linked from book-of-business.tsx via a "You have N advocates" banner.
Verified: advocates endpoint 401 unauth / returns 5 with token; people-to-engage hook="Loves Golf"; frontend bundles clean (full rebuild after metro-cache purge). Voice auto-follow-up verified by parse + logic (not E2E — needs a real recording).
DELIVERY: backend deploy + eas update --branch production.

## Marketing: Relationship OS deck (June 2026)
- New static presentation at frontend/dist/relationship-os/index.html (marketing site, imonsocial.com — NOT the Expo app).
- Reuses existing marketing chrome (nav/footer/demo-modal/reveal-scroll, Inter font) with gold (#C9A962) accent.
- Narrative: hero ("CRM remembers the deal, IMOS remembers the person") -> problem (74% vs 44% loyalty) -> 4 CSS phone mockups (Book of Business, People to Talk To Today, Voice Remember This, Advocate Spotlight) -> flywheel -> stats -> CTA. Demo form posts to app.imonsocial.com/api/demo-requests.
- Linked from marketing.html Resources dropdown + deck footer.
- Screenshots are hand-built HTML/CSS (accurate text), not AI images. Verified renders 200 via local static server.

## STYLE RULE (June 2026) — NEVER USE EM DASHES
- User directive: NEVER use em dashes (—, &mdash;, &#8212;, &#x2014;) anywhere — marketing site, app UI, or generated content. Use a spaced hyphen " - " or restructure with commas.
- Swept clean on this date: all of marketing/build/*.html (49 files), marketing/generate_pages.py (28), and the new app screens (book-of-business, people-today).
